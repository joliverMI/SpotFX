fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.0
spotipy>=2.24.0
httpx>=0.27.0          # async HTTP for LedFX / HA calls
websockets>=12.0
sounddevice>=0.4.7     # audio capture (Windows / Linux / Pi)
numpy>=1.26.0          # audio analysis
scipy>=1.13.0          # signal processing for audio shape
aiofiles>=23.2.1       # async file I/O
pydantic>=2.7.0        # data models (FastAPI uses this)
pydantic-settings>=2.3.0  # settings from .env
python-multipart>=0.0.9   # form data support
redis<7                   # spotipy dep; redis 7.x has Python 3.14 ABC metaclass bug
anthropic>=0.40.0         # SPECTRA settings-console agent (spectra/services/settings_agent.py)
mcp==2.0.0                # settings-console CLI backend's tool surface (spectra/services/settings_mcp_server.py) -- pinned: MCPServer's API moved between major versions (no mcp.server.fastmcp.FastMCP in 2.x)
librosa>=0.11.0           # musical feature extraction
markdown>=3.7             # renders docs/SPECTRA_SPEC.md as a phone-readable page (spectra/services/spec_viewer.py)
soundfile>=0.13.0         # WAV read/write

# fx/ — vendored LedFX render pipeline (see fx/VENDOR.md)
voluptuous>=0.14.1        # effect/virtual config schemas
aubio-ledfx>=0.4.11       # onset/tempo detection (melbank pipeline)
samplerate-ledfx>=0.2.3   # audio resampling for melbank input
pillow>=10.1.0            # 2D effect rendering (Twod)
sacn>=1.9.0               # E1.31 device driver (wled.py fallback path)
pyserial>=3.5             # device registry imports (adalight-class drivers)
netifaces2>=0.0.22        # fx.utils network helpers
pyfastnoiselite>=0.0.7    # noise2d effect
requests>=2.31.0          # fx.utils WLED helper + hue.py REST (already transitive via spotipy)
python-mbedtls==2.9.2     # Hue entertainment DTLS (fx/devices/hue.py)
