# SPECTRA spec — the live reconciliation surface

This is the thing that did not exist before 2026-08-15, and its absence is why the Admiral kept
finding capability gaps himself minutes after someone reported nothing was missed: there was
nothing to reconcile against. This file is that reference. It is not a design doc, not a
tutorial, not a status report — it is a **ledger**: every SPECTRA-relevant capability, its
honest current state, and every open decision that blocks something.

**Read [Maintenance protocol](#maintenance-protocol) before editing this file.** Everyone who
touches SPECTRA touches this file when one of three trigger conditions fires — that is what
keeps it live instead of becoming the next stale artefact nobody trusts.

Origin: Admiral-authorised 2026-08-15 (`data/backlog.md` `spectra-live-spec`), built from
`data/spotfx-capability-audit/report.md` (46 capabilities, code-cited, dated 2026-08-14),
`data/captain.md` (his three spec answers), and `data/captain-shared.md` (standing order 6:
write for agents).

---

## Maintenance protocol

**Who:** any agent working on SPECTRA — crew, Captain DJ, firstmate. Nobody owns this file
exclusively; whoever hits a trigger condition below edits it in the same change.

**When — exactly three trigger conditions, no others:**
1. **A PR proving a capability lands** (merges to `master`) → flip that row's state, cite the
   PR number, delete any now-stale "unverified" note.
2. **The Admiral answers an open question or states a new decision** → see *Translating an
   Admiral edit* below. This is the one that needs care.
3. **An audit or code check finds this ledger wrong** — a row claims a state the code no longer
   matches → fix the row, tag it `(code-verified <date>)`.

Do not edit this file to tidy prose, re-narrate the audit, or record day-to-day PR review/CI
status — that churn belongs in `data/backlog.md` (fleet-managed, changes hourly). This file
tracks **capability state and decisions**, which change far less often than task-queue status.

### Translating an Admiral edit (read this before touching his text)

When the Admiral edits this file directly — a sentence dropped into a row, a correction, a
comment — **his words are a statement of intent, not a patch to merge as-is.**

1. **Do not leave his sentence sitting next to the structured parts unconverted.** It will rot:
   nobody will translate it later, and the next agent will either misread it as binding prose at
   the wrong altitude, or ignore it and lose the intent entirely.
2. **Find or create the correct structured slot for it** — a ledger row's State/Notes, a new row
   in [Open questions](#open-questions), a new or amended line under
   [Standing decisions](#standing-decisions) — and write the intent into that slot, in the
   document's own vocabulary (the state legend, the table columns). Translate, don't transcribe.
3. **Record that his edit drove it.** Append `(Admiral edit, <date>: "<short paraphrase or
   verbatim clause>")` inline at the point of change. This is provenance, not decoration — it is
   what lets a later agent tell "he decided this" apart from "we inferred this from code."
4. **If his sentence doesn't cleanly resolve into an existing slot** — it's ambiguous, or answers
   a question nobody had asked — don't force-fit it. File it as a new row in
   [Open questions](#open-questions) naming what's ambiguous, with a note that it came from a raw
   edit, and ask him directly rather than guessing.
5. **Never delete his raw sentence before doing 1–4.** If you genuinely can't resolve it in the
   same sitting, move it verbatim into
   [Unprocessed Admiral notes](#unprocessed-admiral-notes) at the bottom rather than losing it or
   leaving it stranded mid-document. That section should be empty in steady state — its presence
   at all is a signal something is owed.

---

## Landed vs. deployed (a real gap this document does not close)

Every state below describes what's in git (`master`), not what `spectra.service` is actually
running on his box right now. **Those two can diverge, and it already has.** PR #57 (§44) was
proven live on his real hardware — 11,877 triggers migrated, UPDATE glide confirmed — **while
the PR was still unmerged**; PR #69's success-case proof (§51) ran against commit `bb1b759` on
his live room before that commit belonged to a merged PR at all. In both cases, a routine sync
or resync of the deployed box at that moment would have silently reverted a capability this
ledger would otherwise call `BUILT`.

**What this means for anyone reading a `BUILT` row: it says the capability exists in `master`'s
history. It does not say it's what's currently running live.** Checking actual deployed state
means checking the live box itself (`systemctl --user status spectra`, the commit it's running),
not this file and not `git log`. This document tracks landed state only and does not attempt to
track deployment state — that is a distinct, real gap, named here rather than silently assumed
away.

---

## Standing decisions

### D1 — The fidelity rule

> "Exact where there is a direct equivalent, free where the mechanism differs in kind."
> (Admiral, 2026-08-14, `data/captain.md`)

Where SPECTRA has a like-for-like control — a button, a mode, a thing doing the same job as the
old SpotFX one — **any difference he can feel is a defect, not a design choice.** Read the old
implementation's real numbers before writing. Where the mechanism is genuinely different in kind
(continuous colour drift replacing on/off triggers), build for the same intent and let him
correct it after living with it.

This is why the ledger's `BUILT` state carries a fidelity qualifier (`full` / `extended` /
`partial`): `full` claims exact fidelity to a direct legacy equivalent; `extended` claims the
free half of the rule (same intent, different — and here, richer — mechanism); `partial` is not
a fidelity claim at all, it's an honest "narrower, and nobody is currently building the rest."

Worked failure case on file: the ambient release shipped 2026-08-14 copied legacy's fade numbers
exactly but omitted its second phase, an ~8s eased catch-up — he caught it by watching. Fixed in
PR #61; see §6.

### D2 — The day-one switchover bar is four items, not three

> "Add colour-set authoring and the trigger migration to the bar."
> (Admiral, 2026-08-14, widening his own earlier 3-item bar)

Blockers to switchover: **dark/light mode (§9), colour-set and group authoring
(§10), the trigger migration (§44)**. Force Scene (§31) and Ambient
(§6) are DONE. Current state of all four: [Day-one bar](#day-one-bar) below.

### D3 — Step-through triggers: closed, not building the mini-timeline

> "The flare-and-return moment is what I valued." (Admiral, 2026-08-14)

The ordering/choreography of legacy's multi-step triggers was incidental; what he valued is the
moment everything flares and snaps back. Extend the cheap flare-and-snap-back mechanism (named
flare kinds, momentary hold — §11) instead of building the Sequence/Parallel
mini-timeline-inside-a-scene. **Closed as answered — do not raise it again unless he does.**

### D4 — Legacy retirement is gated, not scheduled

The legacy SpotFX authoring world (events/triggers/choosers) retires **only** on the Admiral's
satisfaction with SPECTRA plus his explicit destructive word on the day
(`data/backlog.md` `spotfx-legacy-retirement-gate`, corr=821f1ec08f6cb466). Every `GAP` and
`OLD-WORLD` row below is safe today only because legacy is still the thing actually driving his
room — S3 hardware handover ([`docs/SPECTRA_HANDOVER.md`](SPECTRA_HANDOVER.md)) is built and
proven but gated off. Nothing in this ledger implies urgency beyond what the day-one bar (D2)
states.

### D5 — Release doctrine: SPECTRA fades and lets go; Home Assistant owns the restore

Settled 2026-08-15 (reported via firstmate, following PR #67's live-fixture proof — see §49).
Release-to-HA's job is to relinquish the room cleanly, not to hand it back lit: SPECTRA fades
each Hue bulb on the bridge, then powers it off, and its contract ends there. Bringing the room
back up afterward is the Admiral's Home Assistant automation's job, not SPECTRA's. **Do not
build a "restore to previous state" step into release** as a future "fix" — a dark bulb after
release is the correct, intended outcome, not an incomplete one.

### D6 — A success response from his Hue bridge proves acceptance, never application

Measured 2026-08-15/16 (`data/spectra-hue-burst-drop-and-false-unlit/`, PR #93 merged — full
detail at §61). **What is established**: 30 captured raw responses during deliberately-forced
load show his bridge returns a clean HTTP 200 with an empty `errors` array and no rate-limit
header of any kind, whether or not a command actually took effect — the bridge cannot signal a
silent discard at the protocol level, at all. **What is NOT established, and must not be
repeated**: an earlier draft of this finding claimed a specific burst-rate cliff (fast writes
dropped, paced writes survive). That claim traced to a bug in the investigation's own probe
script, was retracted in full once caught, and a corrected 48-trial sweep across both bridges
found zero drops at any pace tested — **there is no known drop-rate threshold, and do not imply
his hardware drops commands under load.** The practical consequence stands regardless of pacing:
read-back verification (`ambient.py`'s `_hold_and_confirm`, §51; `ambient_music_gate.py`'s
`verify_now()`, §58) is necessary, not cautious, because a 2xx alone proves nothing. What actually
caused the real incidents that prompted this investigation remains an open question — see
[OQ-8](#open-questions) — separate from the (now-fixed) detect-but-don't-repair and false-unlit
defects §61 closes. **§64 is a full audit of every SPECTRA site that writes to the Hue bridge
against this rule** — most already complied (ambient.py, or were structurally exempt); one gap
was found and closed (the release path's off-write, §64).

---

## Day-one bar

The four items D2 names, and nothing else, block switchover. This table is the one thing worth
checking before saying "ready."

| Item | State | Evidence |
|---|---|---|
| Force Scene | **DONE**, one asterisk | PR #54 merged; room-proven (`tests/test_room_controls.py::test_force_scene_redirects_every_automatic_pick`). Asterisk: §79 (2026-08-18) fixed a real live gap — the redirect alone never FIRES anything on a song with no triggers, so pressing Force Scene read as dead; it now also fires the pin immediately on the enabling/repinning edit. Offline-verified only, room-unproven per this task's own hard limits. |
| Ambient (incl. release fidelity + confirmed-read-back + music precedence) | **DONE**, three honest asterisks | PR #56, #61, #69, #73, #76 all merged (§52/§53, room-proven live 2026-08-15, all three modes incl. "On during music" — non-Hue devices confirmed unaffected across two full drift-leg cycles, ease-back re-confirmed unflattened at ~16.9s); §51's confirmed-vs-attempted fix has its success case proven live and its failure/retry case proven headlessly only for its OWN mechanism (see §51) — a closely related but distinct straggler case is now proven live via §61/PR #93; D6 (PR #93, merged) found the bridge signals nothing at all when it silently discards a write — NOT that it drops writes under load, a claim D6 itself retracted — which is exactly why every one of these read-back proofs matters, and why §58's status-honesty verifier had a real false-unlit gap of its own, now live-fixed per §61; third asterisk: §66 (2026-08-16) fixed brightness being silently discarded for every colour that wasn't a hue swap — a same-hue-different-lightness pick could never dim anything, code-proven only, room-unproven pending the window |
| Global dark/light mode | **DONE**, three-state rebuild room-unproven | §9; PR #70 merged, **proven at his fixtures** (the original Dark/Default two-state build). 2026-08-16 (branch `fm/spectra-display-mode-build`): the shipped "Light" state was found to be a mislabelled Default (nothing forced) — real Light (a configurable, always-on, immediately-repainted forced background) never existed, so he had no watchable before/after. Rebuilt to legacy's real three states (`display_mode: "default"\|"dark"\|"light"`, load-bearing migration off the old bool — see §9 body); code-proven (`tests/test_dark_light.py`, 18 tests) and phone-UI-proven against an isolated instance, **not yet proven at his fixtures**. One open composition question, unaffected by the rebuild: while a Hue device is frozen under `ambient_mode="always"`, dark mode changes WHAT Ambient holds rather than the device's own `dark_lock` (his own ruling, built and room-proven — see §63, PR #91 merged, glide extended to 3000ms by PR #103 but that specific change is deploy-held pending his own eyes on it); `data/backlog.md` `spectra-dark-light-mode` |
| Colour-set + group authoring | **DONE**, one honest asterisk | §10; PR #87 merged, **proven at his fixtures** 2026-08-16 (PR #95). Rotating/synced-pool mechanics (cycle/wrap, cycle/bounce, weighted+exclude_current, Palette Sync, override-entry merging) all room-proven; Hue itself was frozen under `ambient_mode="always"` for the whole proof window (composition rule holding, untouched by design), so no fire in this pass streamed onto a live Hue bulb specifically — see §10 for detail. `data/backlog.md` `spectra-colorset-group-authoring` |
| Trigger migration | **DONE, firing** (code-verified 2026-08-15) | §44; PR #57 merged. His room's `scene_change_mode` is now `"full"` (`storage/spectra/room_controls.json`, live-read — was `"analysed"` when this row was last written, so the "functionally inert" note above no longer holds) — authored and generated triggers both fire. Coverage as of 2026-08-15: 10,698 authored (312 songs, byte-identical to PR #57's migration — see §44) + 17,631 generated (556 songs) = 28,329 triggers, after `scripts/import_analysed_triggers.py` back-filled analysed triggers onto 514 songs that had none (13 more were excluded as unresolvable, not counted — see §44) |

---

## Capability ledger

### State legend

| State | Meaning |
|---|---|
| `BUILT` | A SPECTRA equivalent exists and runs today. Carries a fidelity qualifier: `full` (D1's exact-match claim), `extended` (D1's free-but-same-intent claim, sometimes richer than legacy), or `partial` (narrower than legacy, no known further work scheduled). |
| `AGREED` | The Admiral has explicitly authorised or asked for this; code may or may not exist yet. Carries a PR number when in flight. |
| `GAP` | No SPECTRA equivalent, and no authorised work is currently scheduled to build one. |
| `OLD-WORLD` | Legacy SpotFX covers this and SPECTRA is not expected to — either because nothing has moved it there yet and it's low-priority, or because it's the kind of thing (device/hardware management) meant to stay in the legacy app even after handover. |
| `RETIRED` | The Admiral's explicit 2026-08-12 retirement decision (`data/spectra-gap-inventory/decision-legacy-retirement-picks.md`). Not being rebuilt anywhere; nothing deleted from legacy either. |
| `N/A` | Architecturally inapplicable to SPECTRA by design — not a gap. |

Numbering follows the audit's own tier order so a lookup there needs no translation. The audit
counts 46 capabilities by also numbering four sub-pieces of §1 separately; this ledger folds
those into §1's note instead of giving them their own rows (no information lost — see §1).
**§43–§73 are SPECTRA-native builds that emerged after the audit's 2026-08-14 cutoff** — not in
the original 46, added here because this ledger is the live surface and they didn't exist yet
when the audit ran.

### Tier 1 — foundational

| § | Capability | State | Notes |
|---|---|---|---|
| §1 | Live audio cross-correlation sync engine | `BUILT` (partial, PR #71 merged) | Admiral's direct ask — "the xcorr sync from SpotFX was very good and should be ported almost directly," plus a reported live degradation. **Live-measured root cause, not "still adapting" as he suspected**: `spectra/services/bridge.py` silently dropped the `timing` sibling spot-effects already broadcasts on every WS "state" message, so `trigger_engine.tick()` fired on the raw, uncorrected Spotify position while spot-effects' own engine has always compared against `effective_now = now_ms + effective_offset_ms`. Measured live against his room, 7 samples over ~40s of confirmed `is_playing=true` playback: a stable `+7052ms` `shape_offset_ms` for the song then playing, not drifting/adapting at all across the window — the correction was simply never wired through, not converging. Fix: `bridge.py` now parses `timing`, exposes `effective_position_ms()` (raw + `shape_offset_ms`, spot-effects' own formula), and `services/engine.py`'s trigger poll feeds that instead of the raw position. **Deliberately NOT ported**: the audio capture + correlation computation itself (stays in spot-effects — duplicating it into SPECTRA's own process would fight the same PipeWire monitor exclusivity `fx/audio_ingest.py`'s own docstring already documents as a starvation problem within ONE process, report §2.2) and `effective_offset_ms`'s `ledfx_trigger_buffer_ms`/`ledfx_rtt_ms` terms (compensate for spot-effects' own LedFX-HTTP write-transport latency, a mechanism SPECTRA's executor doesn't share — genuine incompatibility, not an oversight). Still folds in the 4 audit sub-pieces at partial fidelity: mid-song drift monitor / fast-lock / DJ-mix search escalation are inherited transitively (SPECTRA consumes whatever spot-effects converges to, live) rather than run natively in SPECTRA; the sync-lock status badge is now visible via §23's `LockBadge` port, spot-effects' own state through a SPECTRA page, same reuse pattern as §13/§14. |
| §2 | Scene Groups (rotate/cycle a scene pool) | `GAP` | No authorised build found. Blocks the Drop-into-Group payoff half of §32 and the retired per-trigger overrides in §42. |
| §3 | AI-assisted structural trigger placement | `BUILT` (partial) | `spectra/services/midsong_generator.py` seeds one generic fire-scene trigger per section boundary; no genre-tuned profiles, no per-role placement (Song-Start/Bass-Entry/Lull/Charge/Drop/Quiet/Flare), no bass-onset snapping, no auto-tuning. Deliberate scope per its own docstring, not a bug. |
| §4 | Triggerless & Dinner Party automatic show | `GAP` | Dinner Party is a pause-only signal today (`spectra/services/scene_sequencer.py:411`); the sequencer that could substitute ships dark by design. Explicitly kept, not dropped, per the 2026-08-12 signoff (same one that keeps §12). |
| §5 | Ambient — Hue entertainment-area selection | `BUILT` (full), PR #109 merged (`6ecfbc5`), room-unproven | He asked twice; it sat on a hold waiting for a word he was never told he needed to say — built without waiting for a third ask. Read legacy first, per instruction: `services/ambient_mode.py` held per GROUP (`state.ambient_groups`, one Hue device id per entertainment config/bridge), exposed via a long-press checkbox picker on the front-page Ambient button (`web/src/nowplaying/AmbientButton.tsx`, `GET /control/ambient-groups`, `POST /control/ambient-mode?groups=<id>`) — no separate device-CATEGORY tier existed above that picker in the sense he asked about; legacy's `ambient_target_category` only pre-filtered which devices were eligible groups at all, and SPECTRA has no LedFX-category concept to port that tier to (every live Hue device already IS a candidate group). Ported the group tier only: `RoomControlState.ambient_hue_group_ids` (`[]` = every live Hue device, today's unmodified default — nobody's room changes on deploy), `spectra/services/ambient.py`'s `reconcile()`/`verify_held()` both take the resolved target set — the hold path only touches devices IN scope; the release path additionally releases a device that's currently frozen but fell OUT of scope (a group just deselected while Ambient stays engaged), gated on a new read-only `frozen` property on `fx/devices/hue.py` (VENDOR.md deviation #11) specifically so an out-of-scope device that was NEVER frozen is left completely untouched — `set_frozen(False)` on an already-unfrozen device forces a real stream reconnect it never needed. Whole-room OFF stays unconditional (unchanged behaviour, ignores the current selection — turning Ambient off releases everyone). UI: `AmbientGroupsPicker.tsx` (checkbox list, reuses `FavoritesPicker.tsx`'s proven phone-safe modal shape) behind a "Hue areas" button on `RoomControlsBar.tsx`, `GET /api/room-controls/ambient-groups` for names. **The picker's data source is config-backed, not a live runtime call — this is a structural difference from legacy, not a cosmetic one**: legacy's own `resolve_groups()` (`services/ambient_mode.py:343`) round-trips a live HTTP call to LedFX on every open and has its own documented 2026-08-12 incident scar — "a genuinely-empty target category and 'LedFX unreachable' are indistinguishable here... during a transient outage" — worked around by serving stale cached names past their TTL rather than showing an empty picker. `spectra/services/ambient.py::list_groups()` has no equivalent failure mode to work around: it reads `live_host.live.host.devices`, an in-process dict SPECTRA populates from `storage/spectra/fx-live/config.json` at activation, not a network call that can time out or 404 — "No cache (unlike legacy, which round-tripped an HTTP call to LedFX) — reading `live_host.live.host.devices` is a free in-process dict lookup, not worth caching" (the function's own docstring). The picker is legitimately empty in exactly one case — SPECTRA doesn't currently own the live stack, or the room has no live Hue device at all (`list_groups()`: "a UI picker with nothing to show, not an error") — which is an ownership-state fact, not a transient-fetch failure, and needs no cache to paper over. Verified by read-back, same discipline as the rest of Ambient: `tests/test_ambient.py`'s new group-selection section proves a selected group's REST writes never reach an unselected device's bulb, an unselected-but-never-held device receives zero calls of any kind, a group deselected mid-hold is genuinely released (fade → catch-up → unfreeze) while the other stays held, and `verify_held()` scoped to a selection never reads back the other device's lights. Phone-verified at a real 390×844 viewport against an isolated instance (spare port, two fake Hue devices, no real bridge credentials, real spot-effects/LedFX ports pointed at nothing so the process can't reach them) — screenshot + a live checkbox-uncheck-then-Save round trip through the real API, confirmed via `GET /room-controls`. **Not yet room-proven against his actual two bridges** (`hue-lights` 10 bulbs, `dining-hues` 7) — his room stayed off-limits for this task per its own hard limits. |
| §6 | Ambient — release/handback fidelity (the OFF path) | `BUILT` (full) | PR #61 merged. Colour-matched fade → verified unfreeze → live-pixel-buffer catch-up tween, matching legacy's two-phase handback (`spectra/services/ambient.py`). Room-proven 2026-08-14, `docs/spectra-room-window-proofs-2026-08-14.md` Proof 2 (PASS). **DELTA since audit**: the audit read this as a gap; PR #61 closed it the same day. See §51 for the ON-path companion fix (confirmed-vs-attempted light hold), a distinct defect found the next day. |
| §7 | Device Categories (organise/import lights) | `OLD-WORLD` | `spectra/api/registry.py:21-36` reads categories read-only; nothing writes to `device_categories.json` from SPECTRA. No authorised build found — use the legacy app. |
| §8 | Shape Maps (odd-shaped matrix geometry) | `OLD-WORLD` | Render engine understands shape maps (`fx/virtuals.py`); no router/dialog under `spectra/`. No authorised build found — use the legacy app. |
| §9 | Global Dark/Light Mode | `BUILT` (full) | Day-one bar item (D2). PR #70 merged (`7aaf01d`). The per-trigger `display_mode` field on the ported legacy-proxy dialog (`spectra/web/src/timeline/types.ts:18`) is untouched and still unread by `spectra/*.py` — that field belongs to the retired per-node Light Mode Chooser (§36), not this feature; don't conflate them. What shipped: `RoomControlState.dark_mode_enabled` (room bar checkbox) toggles the SAME LedFX-side `dark_lock` clamp legacy's own `services/display_mode.py` drives (the mechanism is vendored into `fx/` verbatim), over two new ownership-routed primitives on `spectra/services/fx_seam.py` (`get_virtuals`/`set_virtual_config`, alongside `apply_writes` — same HTTP-pre-handover/in-process-post-handover routing, so this needed no ownership logic of its own). `spectra/services/dark_light.py` is the reconcile logic and carries the full fidelity reasoning, incl. two deliberate D1 "differs in kind" calls made from the legacy code, not re-asked of him: (1) a bool, not legacy's three-state Default/Dark/Light cycle, because "Default" only ever meant "defer to a lower cascade level" and every lower level (trigger → scene group → scene → Set Color action → colour cards) is the retired §36/§42 machinery — with nothing left to defer to, Default and Light are the same state; (2) restores from a live pre-dark snapshot (captured via `fx_seam.get_virtuals()`, persisted to survive a restart, replayed via `fx_seam.apply_writes()`) rather than legacy's "re-fire the last Color Set," since that concept belongs to the same retired authoring world; (3) added post-review 2026-08-15, prompted by a live Ambient incident on a different feature that surfaced the same class of mistake: the light-transition repaint is gated on `bridge.is_playing()` — while music plays, the stale pre-dark snapshot is deliberately not forced back (`dark_lock` still clears; nothing stays forced black), left to the room's own live show instead, surfaced as `repaint_skipped: "music_playing"` and a room-bar badge (same lesson §52/§53's music-precedence fix teaches for Ambient, reached independently). Shielding (`dark_light_shield_categories`, default `["Singles"]` — legacy's own default; `dark_light_shield_virtuals`) is ported verbatim via the shared read-only category registry. **Composes with `ambient_mode`'s three settings (§52/§53), not a boolean** — rebased 2026-08-15 onto PR #76: whenever a Hue device is actually frozen right now (`ambient_mode="always"` unconditionally, or `"auto"` while confirmed not-playing), it's driven by direct bridge REST and invisible to `dark_lock`; the moment it isn't frozen (`"off"`, or `"auto"` while confirmed playing), it's LedFX-rendered and responds normally — a property of the write path, not a rule either feature encodes about the other. Also composes with §10's Colour Set Groups (PR #87, merged the same day): dark/light's `dark_lock` clamp and repaint act on whatever effect a virtual is CURRENTLY running, regardless of whether a scene fire that landed it there resolved a plain Colour Set or a Group member — the two features never reference each other's fields, same "compose for free by construction" shape as the Ambient case. Measurement note (mirrors AGENTS.md's "Reading real Hue bulb state" entry): dark/light's own mechanism is LedFX-side, verified via `fx_seam.get_virtuals()` (`dark_lock` + effect config on `/api/virtuals`) — never the Hue CLIP light resource, which is Ambient's own instrument for a different subsystem. Verification reads real state back from LedFX after every push (`unconfirmed` in the API response), never trusts the write. Offline proof: `tests/test_dark_light.py` (11 tests), incl. a frame-level test against a real headless dummy host (`fx/headless.py`) proving the actual vendored clamp engages, the restore repaints the exact pre-dark background, and the music-aware gate holds both ways (confirmed True skips; confirmed False or unresolved/None proceeds) — not mocked. **PROVEN AT HIS FIXTURES** (`data/backlog.md` `spectra-dark-light-mode-bar`, checked off 2026-08-15: "Done requires his-room end-to-end proof per standing order 11" — met). **The composition gap this same invisibility created, found and now ruled on**: while `dark_mode_enabled` is on and a Hue device is frozen (`ambient_mode="always"`), `dark_lock` correctly has no effect on that device (it isn't LedFX-rendered) — but from where he stands, toggling something he calls GLOBAL dark/light leaving his main Hue lights bright reads as broken, not as two features each doing exactly what he asked (`data/backlog.md` `spectra-dark-light-vs-ambient-ruling`, filed 2026-08-15, three readings offered: dark overrides the hold / ambient-always wins / the two compose). **His ruling, verbatim, matches the third reading**: "dark mode during ambient should choose a different color that I pick so we have one color for regular ambient light mode or hybrid mode and then one color for dark mode for now make them the same but let me be able to pick it with the color picker." Dark does not bypass Ambient and Ambient does not block dark — **dark changes WHAT Ambient holds**. See §63 for the fix built to his ruling (PR #91, merged and room-proven). `data/backlog.md` `spectra-dark-light-mode`. **THREE-STATE REBUILD (2026-08-16, branch `fm/spectra-display-mode-build`, room-unproven)**: a scout investigation (`data/spectra-display-mode-three-state/report.md`) found the two-state build above was a mislabel, not an honest smaller version — `dark_mode_enabled=False` was called "Light" in the code, the API `status` field, and the help text, but behaved as legacy's Default (nothing forced); legacy's actual Light (a configurable, always-on, immediately-repainted forced background) was never built, so he had no dark-vs-light before/after to test against (his scenes all author dark colour sets, so Default and Dark look identical). Rebuilt to all three states: `RoomControlState.dark_mode_enabled: bool` → `display_mode: "default" | "dark" | "light"` (his word "hybrid" = `"default"` internally, labelled "Hybrid" in the UI — his standing ruling). **Hybrid confirmed, plainly**: Hybrid is the state that leaves backgrounds alone and lets each scene's own authored colours show — SPECTRA forces nothing, the same "nothing to force" reasoning the earlier two-state build's own Default already rested on (above: "with nothing left to defer to, Default and Light are the same state" — that observation about the OLD build is exactly why the new build's `"default"` needed no new mechanism, only a rename to the word he actually uses). Dark forces `dark_lock`; Light forces `display_light_bg_color`/`_brightness`; Hybrid forces neither. **Migration, load-bearing** (`room_controls.load_room_controls`): `dark_mode_enabled: true` → `display_mode: "dark"`; `dark_mode_enabled: false` → `display_mode: "default"`, **never** `"light"` — the old field was named light but behaved as default, so mapping false→light would have silently turned on a forced background on his live room the moment this migration ran. Nobody's room changes on this deploy; Light is a new option he can pick, never something he's moved into. New Light mechanism (`spectra/services/dark_light.py`'s `"light"` branch): `display_light_bg_color`/`display_light_bg_brightness` (new `RoomControlState` fields, legacy defaults `#201830`/`0.3`, same `ColorGradientPicker` the ambient swatches use) are written as `background_color`/`background_brightness` onto every non-shielded virtual's CURRENT live effect config (read via `fx_seam.get_virtuals()`, merged, written via `fx_seam.apply_writes()` — running effect type and every other/foreground param untouched), **unconditionally** — not gated on `bridge.is_playing()`, unlike the Default/Hybrid repaint, because a fresh forced write is authoritative the instant it lands (same reasoning as Dark's own unconditional clamp) and gating it would have defeated the point: watching it work while his music plays. Shielding applies to Light exactly as it already did to Dark. The pre-dark snapshot is cleared on every transition away from `"dark"` (to `"light"` or `"default"` alike), since Light's own write makes the snapshot stale. `DarkLightResult.status` and the room-bar badges now read `"default"`/`"dark"`/`"light"` honestly (the old build's `status: "light"` meaning default is retired). UI: `RoomControlsBar.tsx`'s checkbox replaced with a Hybrid/Dark/Light select plus the light-bg colour/brightness picker, phone-verified at 390×844 against an isolated instance (spare port, no live devices — not his room). Tests: `tests/test_dark_light.py` (18, incl. new Light frame-level + migration-mapping proofs), `tests/test_room_controls.py`, `tests/test_ambient_music_gate.py` updated for the field rename; full suite 550 passed / 3 pre-existing environmental failures unrelated (needs his seeded live storage, see `AGENTS.md`'s environmental-failures note) / 2 skipped. Not yet room-proven — his room is untouched by this build. |
| §10 | Colour Set Groups (rotating/synced pools) | `BUILT` (full) — **room-proven** 2026-08-16, one honest asterisk | Day-one bar item (D2) — built and headlessly proven (25 tests, `tests/test_color_set_groups.py`; full suite otherwise green — one pre-existing environmental failure unrelated to this change, needs his live-seeded `storage/spectra/scenes.json`, absent in any disposable worktree). Read his real data before designing, per the task's own instruction: `storage/color_sets.json` (2026-08-15) has 8 authored groups — 100% `mode="cycle"`, `exclude_current=True`, `palette_sync=True`; 5/8 carry real override entries; none use weighted mode or the dark/light mode-lane fields, confirmed not assumed. `spectra/services/color_sets.py`'s `ColorSetCard` now carries `members`/`mode`/`cycle_behavior`/`exclude_current`/`palette_sync` (previously `extra="ignore"`-dropped — structurally invisible to every consumer); `spectra/services/color_set_groups.py` is the ONE resolution point — cycle (wrap/bounce)/weighted+exclude_current picking, Palette Sync anchored on `color_journey`'s own room state (D1's free case: reuses SPECTRA's existing `active_set_id`/`wheel_position_deg` rather than a parallel legacy-style tracker), the group's own override `entries` merged onto the picked member per virtual — ported from spot-effects' `trigger_engine._select_color_set_member`/`_execute_set_color`. Wired into every explicit choke point that was silently mishandling a Group id (`scene_sequencer.fire_scene_by_id` — THE single scene-fire path both the sequencer and SPECTRA-native triggers funnel through; `trigger_engine._default_select_color_set`; `POST /room-color/apply`; the editor's baseline endpoint; `SpectraTriggerDialog.tsx`'s colour-set pickers, which excluded groups outright) — those `kind == "set"` filters were load-bearing, per the task's own warning, and are now widened rather than removed. Deliberately NOT ported: dark/light "mode lane" variants (§36 RETIRED, zero real usage) and the scene-group colour-ref sentinels (§42 RETIRED, depends on §2 which doesn't exist in SPECTRA). Deliberately NOT wired: the sequencer/journey's own automatic wheel-travel roll (`color_wheel.wheel_positions`, `scene_sequencer._default_eligible_sets`) — a Group has no chromatic wheel position of its own, so that mechanism stays set-only by design (D1's free half — the mechanism genuinely differs in kind: legacy drove groups by per-trigger advance/direction, SPECTRA's automatic picks are curve/wheel-travel-driven and no real group needs to be reached that way). Authoring UI: `spectra/web/src/colorsets/ColorSetsPage.tsx` (`/colorsets`, new nav "Colours") — create/edit/delete either kind; "▶ Apply to room" is the live proof surface (fires the same `POST /room-color/apply` a trigger would, toasting which member a Group picked — press repeatedly to watch it rotate). Writes go straight through spot-effects' own already-general `/api/color-sets` (the same pattern the existing opt-out toggle already used) — SPECTRA's own backend still only ever reads this storage, never writes it. **Room-proven, 2026-08-16, against his real deployed checkout** (PR #87 already merged and running as `spectra.service`; this pass, PR #95, authored, fired, and cleaned up through the live authoring surface, not a spare instance). Method: a throwaway Group (`ZZ-Proof-Group`) plus three throwaway solid-colour Sets (Red/Green/Blue), all created through the real `ColorSetsPage.tsx` at `/spectra/colorsets` — form fills and the real colour picker, not a raw POST — scoped to `tv-mapper` (a real WLED-backed virtual: `sconce-kitchen-left`, `sconce-kitchen-right`, `tv-backlight`; confirmed by reading `storage/spectra/fx-live/config.json`'s segment list before picking it, specifically to avoid `radial-dummy`, which shares `tv-mapper`'s category but is a `type: "dummy"` placeholder with no physical light behind it). Fired repeatedly via the same "▶ Apply to room" button → `POST /room-color/apply` a trigger would use. **Measurement**: `tv-mapper` is WLED, not Hue, so entertainment-configuration status doesn't apply to it — the instrument used was `GET /spectra/api/engine/status`'s `executor.recent_writes`, read fresh after every fire, matched against the toast's own "picked X" text. All five documented mechanics confirmed by deterministic or statistical sequences of real writes, not by trusting the toast alone: cycle/wrap (Red→Green→Blue→Red→Green, exact); cycle/bounce, same group switched live (Blue→Green→Red→Green→Blue, exact reversal at both ends); weighted+`exclude_current` (Blue weight=10 vs Red/Green=1, 16 fires → 7 captured picks, Blue×3/Red×2/Green×2, zero consecutive repeats); Palette Sync, both anchor directions (applying Green then firing the group landed Blue — anchor(Green)+1; applying Red then firing landed Green — anchor(Red)+1 — matching `_pick_member`'s anchor-then-advance behaviour exactly, not just "a plausible colour"); override-entry merging (a group-level `background_brightness: 0.05` stayed fixed across two different picked members — `#0000ff`+0.05 then `#ff0000`+0.05 — while each member's own foreground colour kept varying, proving the merge layers per-field rather than replacing the whole entry). **Deliberately playback-independent**: `/room-color/apply` is a manual room-control surface, not a trigger/track-position path — his music stopped mid-proof (`bridge.is_playing()` went false partway through) and every fire after that landed identically, confirming the mechanism has no dependency on a live track. **The one honest asterisk**: Hue sat frozen under `ambient_mode="always"` for the entire proof window (his own resting state, never touched) — by the composition rule (§9/§52/§53: a frozen Hue device is driven by direct bridge REST, invisible to whatever LedFX/SPECTRA scene logic would otherwise land on it), no fire in this pass could have streamed onto a live Hue bulb regardless of target, so Group resolution specifically reaching a Hue-scoped virtual was not independently observed this pass — the resolution code path (`color_set_groups.resolve_for_fire`) is scope-agnostic and identical regardless of device type, but that's an architectural argument, not a room observation, and is named as such rather than folded into "proven." **Room left exactly as found, not as assumed**: all four throwaway cards deleted (`DELETE /api/color-sets/{id}`, confirmed back to the pre-test count of 58); the room's `active_set_id`/`wheel_position_deg` — which the proof's own direct set-applies had pointed at throwaway ids — were restored not by replaying a value captured at the start of the session, but by completing the drift conductor's own already-in-flight, already-declared destination leg (read live: `journey.destination` was mid-transit toward "Mid - Purple Blue Waves" at the moment cleanup began, having already drifted on its own from 290° to 207.5° across the proof window under its normal ~20s-leg pace, unprompted) — applying that same destination directly finishes the transition the room was already committed to rather than injecting an arbitrary "original" value that had since gone stale. Verified after cleanup: `GET /liveness` — `state: "live"`, zero `activation_gaps`; `GET /room-controls` — `ambient_mode/ambient_color/scene_change_mode` byte-identical to the values read before touching anything; `ambient` status — still `held: true`, 17/17 lit, freshly reverified. `data/backlog.md` `spectra-colorset-group-authoring`. **TIERED-LIST REWORK (2026-08-17, branch `fm/spectra-colour-groups-as-overrides`, not yet merged)**: his ask, verbatim: "in our color group list let's tier it, so that color sets are listed under a color group that contained them. Now what color groups are, are just overrides for the values of all the color sets below it. this gives me a way to bulk edit groups of color sets. Apply the current overrides that I have from back when it made more sense in spot FX." Four things established from his real data before building, per his own instruction: **(1) live layer, not destructive write** — `resolve_for_fire`'s override overlay was ALREADY non-destructive (never rewrites a member Set's own stored `entries`, verified in this module and legacy's `_execute_set_color` alike), so "bulk edit" names the EFFECT, not a mechanism to build; his legacy overrides are already live in `storage/color_sets.json` and already wired — there was nothing to migrate. **(2) what breaks: nothing** — 0 of his ~21k SPECTRA triggers/scenes target a Group id today (his "not really calling color groups like we were in SpotFX" is literally true here, though legacy's `storage/events.json` shows 5/8 groups WERE fired as pools there), so the pool-pick mechanism is kept fully intact (`SpectraTriggerDialog.tsx`'s select-colour-set action still lists Groups; `POST /room-color/apply`/`baseline/{id}`; ▶ Preview) and only visually de-emphasized under a "Rotation" heading in the editor, never removed. **(3) a Set CAN sit under >1 Group** — his real data has 4 that do (First Group ∩ Blues) — so the tiered list is a many-to-many index (a Set renders under every Group listing it, cross-referenced "also in: …"), not an invented single-owner tree. **(4) applying his overrides changes nothing rendered today** — they're already applied, already scoped exactly as before (only when the Group itself is the resolved fire target — verified this has NEVER applied to a Set fired by its own id, in legacy or here); broadening that to every direct Set reference was considered and explicitly rejected — computed against his real data, it would silently change rendered output for 27 of 28 (group, member) override pairs, with zero precedent in either codebase to justify it as a "port." Built: `ColorSetsPage.tsx`'s left list nests every Set under its Group(s) (▾/▸ collapse, "Ungrouped" section for the 8 sets under none), UI-only — no backend/storage/API change; the right-pane Group editor reorders to Overrides (foregrounded, "bulk edit for this group's colour sets") → Colour sets in this group → Rotation (demoted, with an honest note on when it fires). Phone-first: the page previously had no responsive breakpoint at all (`docs/SPECTRA_SPEC.md`'s own §67/§68 already named this gap) — added the same `useIsPhone()` drawer pattern `ScenesPage.tsx` already uses, verified at a real 390×844 viewport (chrome-devtools-axi, isolated instance on spare ports 8200/8210, his real `storage/color_sets.json` copied read-only into the worktree's own storage — his live `:8000`/`:8010` untouched): no page-level horizontal overflow, tiering/collapse/multi-group cross-refs/search-flattening/independent per-Set editing all confirmed working. **DIRECT-SET OVERRIDE BROADENING (2026-08-19, PR fm/spectra-group-overrides-on-direct-call)**: his explicit follow-up, verbatim — "[the overrides] need to apply when the colour set is called. It still needs to use the overrides from its parent group." This is exactly the broadening the tiered-list rework above investigated and rejected as invented behaviour with no precedent — he has now asked for it directly, so it's authorised. `color_set_groups.resolve_for_fire` on a `kind=="set"` card now looks up every enclosing Group carrying non-empty override `entries` (`group_ids_by_set`'s reverse index) and chains them onto the Set's own entries — same `_overlay` merge, same "later layer wins per field per virtual" rule the Group-fire path already used; a Set in no overriding Group returns unchanged (identity, the common case). Wired at every existing choke point for free (all already call `resolve_for_fire`/`resolve_for_fire_mode_gated`/`resolve_ref` on whatever kind they're handed: `scene_sequencer.fire_scene_by_id`, `trigger_engine._default_select_color_set`, `POST /room-color/apply`, the editor's baseline endpoint) plus one fix: `room_preview.py`'s own `_resolve()` had a redundant `if card.kind != "group": return card` short-circuit predating this change that would have silently kept Preview on the old behaviour for a direct Set — removed, so ▶ Preview on a Set now shows its Group's override too, same as a real fire. **Multi-group precedence** (his real data still has exactly 4 sets under both First Group and Blues): chains every enclosing override-bearing Group rather than picking one — picking a single "owning" Group would invent the exact primary-owner concept the tiered list explicitly rejected ("a many-to-many index, not an invented single-owner tree"), and would contradict the "never one wins" precedent §76's likelihood-curve chaining already set for this identical "Set in >1 Group" shape. Order is deterministic — ascending by Group name (case-insensitive), tie-broken by id — so the alphabetically-last enclosing Group wins a field conflict; a Group with no override entries contributes nothing. Today's data renders identically under any tie-break (Blues carries no entries, so its 4 shared members' real overrides all come from First Group regardless of order) — the order only starts to matter the day he authors conflicting fields on two Groups sharing a member. Still a LIVE overlay only, never a destructive write, unchanged from the Group-fire path. **Re-measured the tiered-list rework's own "27 of 28" consequence figure against live data** (fetched over the network from the running `:8010`/`:8000` processes — this worktree's `storage/color_sets.json` and `storage/device_categories.json` are both gitignored and were stale copies from worktree creation, confirmed by size mismatch against the live GETs before trusting either): **still 27 of 28 (group, member) override pairs change rendered output, unchanged from the original measurement two days on.** The 28th (unchanged) pair is `Lines` → a member id (`2deeaa37-c86f-491b-9a4f-f96e53da34ec`) that no longer exists in his live `color_sets.json` at all — a stale membership reference, not a "no visible difference" case (already surfaced in the UI as "⚠ missing set" and unaffected by this change either way). Under the actually-shipped chained resolution, all 27 of the 27 real sets sitting in ≥1 override-bearing Group now render differently when fired directly. No case looks unrecognisable: checked field-by-field, no Group override entry ever sets `color_kind`/`color_value` on a scope a member's own entries already cover, so a Set's own authored foreground colour/gradient is never replaced — only brightness/background/`bg_mode` fields shift, and/or additional virtuals the Set never scoped gain colour from the Group (e.g. firing any of the three `Calm - *` sets directly now also lights the Singles category in the Group's shared `#ffd37d` fill, which none of the three Sets' own entries ever touched — the single most visible consequence of this change, worth watching for on the room). Tests: 7 new cases in `tests/test_color_set_groups.py` (direct-call broadening, no-enclosing-group passthrough, no-override-entries passthrough, reaching virtuals the Set doesn't cover, non-destructive storage, multi-group chaining with alphabetically-last-wins, multi-group non-conflicting fields both landing) — full suite 712 passed / 3 pre-existing environmental failures (identical on pristine master, unrelated to this change) / 2 skipped. **The list-nesting half of his ask was already fully built** by the tiered-list rework above (PR #117, merged) — nothing new needed. Confirmed still live and correctly rendering at 390×844 through the real proxy path (`:8000/spectra/colorsets`, byte-identical bundle whether fetched via the proxy or directly from `:8010`): collapsible Group headers, indented member rows, "also in: …" cross-references for the First Group/Blues overlap, the "⚠ missing set" stale-reference warning, and the "UNGROUPED" section all present and rendering with zero page-level horizontal overflow. |

### Tier 2 — real, would show up within a few nights

| § | Capability | State | Notes |
|---|---|---|---|
| §11 | Override Blend (dynamic ramp-to-next-trigger) | `BUILT` (extended) | Legacy computes a live stretch-to-next-trigger; SPECTRA has no forward fire schedule to compute that against, so it authors a fixed per-scene ramp instead (`PhaseBlend`, `SceneV2.entry_ramp_ms`). Admiral confirmed keep-as-is 2026-08-14 (`data/backlog.md` decision, done) — this is the D1 "free" case, not an oversight. |
| §12 | Genre Blending (suppress Song-Start on same-genre transitions) | `GAP` | Explicitly kept, not dropped, same 2026-08-12 signoff as §4. **Trap**: the ported legacy Builder's "Genre" chip still visibly toggles a real legacy setting — it looks live for SPECTRA's own triggers but has zero suppression logic there (`spectra/services/trigger_engine.py` has none). |
| §13 | Editor Preview (test-fire an unsaved draft) | `BUILT` (partial) | Present via the legacy proxy at `/spectra/timeline` (same `POST /api/events/preview`). SPECTRA's own native `SpectraTriggerDialog.tsx` has no preview/test-fire of an unsaved draft. |
| §14 | Profile Builder authoring UX depth | `BUILT` (partial) | Present via the legacy proxy. SPECTRA's own native surface (`SpectraTriggerBar`/`SpectraTriggerDialog`) is a tick-bar plus a form — no palette keys, drag gestures, waveform layers, shift-all, or multi-select. |
| §15 | Morph Steps (cross-effect nudge/absolute changes) | `GAP` | No authorised build found. |
| §16 | Value bindings (live-signal / random-roll fields) | `BUILT` (partial) | `ParamTarget` (absolute/offset/random) covers response/drift-kind parameters only, not legacy's near-anywhere authoring reach. |
| §17 | GIF asset picker with missing-upload flag | `GAP` | Equivalent field is a bare text box, no dropdown, no validation (`spectra/web/src/scenes/tabs/InitialSetTab.tsx:355-397`). No authorised build found. |
| §18 | Effect parameter tuning as standalone action | `BUILT` (partial) | Shared param registry used inside scene device rows; no standalone "nudge one param, one ramp, as a burst" action type — SPECTRA's trigger actions are fire-scene / fire-response / select-color-set only. |

### Tier 3 — diagnostics & reliability

| § | Capability | State | Notes |
|---|---|---|---|
| §19 | Drift diagnostics & recapture workflow | `BUILT` (PR #71 merged) | Was `OLD-WORLD` pending §1; §23's Debug page port carries the shape status card + Recapture/Force-recapture buttons over verbatim, reusing spot-effects' own `/api/audio-shape/*` endpoints same-origin (same reuse pattern as §13/§14) — no new backend, so this closes alongside §1/§23 rather than needing its own build. |
| §20 | Per-song lock history & grading | `BUILT` (PR #71 merged) | Was `OLD-WORLD` pending §1; §22's Timing page port carries `LockHistoryPanel.tsx` over verbatim (grade chips, time-to-lock, Δ needed, per-song search), reusing spot-effects' own `/api/lock-history/*` endpoints same-origin — same closure as §19. |
| §21 | Systemic offset learner | `OLD-WORLD` | Off by default even in legacy; low priority. |
| §22 | Timing Viz diagnostics page | `BUILT` (PR #71 merged) | **Superseded the audit's own `N/A` call** — that read "no fingerprint-offset system in SPECTRA's architecture to visualise" as reason not to port; the correct read (per D1's fidelity rule, and the Admiral's direct ask alongside §1) is that spot-effects' own `/api/timing-viz/dump` still exists and runs regardless of which process the ask is about, so the page is a same-origin verbatim copy (`spectra/web/src/timingviz/`, one import path changed) — no new backend needed. Visually verified live against his room (dev-server smoke test, read-only: navigated + screenshotted, no mutating clicks) before playback stopped. |
| §23 | Debug page (live sync capture, LedFX health, recapture tools) | `BUILT` (full, PR #71 merged) | **Upgraded from partial**: a real `/debug` route now exists (`spectra/web/src/debug/`), a verbatim port of spot-effects' own page built on SPECTRA's already-ported builder canvas stack (`timeline/{canvas,components,hooks}`, which diffed byte-identical against spot-effects' `builder/` modules). One real gap closed with a new file rather than a redesign: spot-effects' shared cross-page `live/liveStore.ts` (Now Playing + Debug's WS feed) has no SPECTRA equivalent, so `spectra/web/src/debug/liveTiming.ts` is a trimmed port scoped to what these two pages read, subscribing to the same same-origin WS `state` broadcast independently — same pattern spot-effects itself already uses per-page. Visually verified live (read-only) before playback stopped; `tsc --noEmit` + `vite build` clean. |
| §24 | LedFX write-plane health watchdog | `BUILT` (extended) | Architecturally different, appropriately so: `spectra/services/frame_watchdog.py` + `ownership_reconciler.py`, since SPECTRA drives Hue/WLED directly once she owns hardware rather than through LedFX's HTTP API. No visible Debug-style UI page yet — minor, non-urgent while she isn't driving live hardware. |
| §25 | One-tap "Restart SpotFX" button | `GAP` | Automatic self-healing exists (frame watchdog pings systemd); no owner-facing manual reset control in SPECTRA's UI. |
| §26 | Guest source takeover (Snapcast Guest/AirPlay) | `OLD-WORLD` | Legacy still handles it; SPECTRA has no special awareness (runs on the neutral 0.5-intensity floor via the shared bridge). Low urgency — legacy covers the house today. |
| §27 | Import LedFX scene / import analyzed triggers on demand | `OLD-WORLD` | The one-time bulk migration script (`scripts/seed_spectra_from_v2.py`) is not the same as this on-demand workflow tool. Low-medium priority convenience gap. |

### Tier 4 — confirmed solid (no gap)

| § | Capability | State | Notes |
|---|---|---|---|
| §28 | Scenes (SceneV2) | `BUILT` (extended) | Adds intensity-conditional effects, real drift, named flare kinds, phase choreography legacy's own SceneV2 defined but never wired into an execution engine (`spectra/models/scene.py`). |
| §29 | Scene Sequencer | `BUILT` (full) | Re-homed, same algorithm, now shares the room's live wheel position (`spectra/services/scene_sequencer.py`, `selection_kernel.py`). |
| §30 | Energy gates/tilt on random scene picks | `BUILT` (full) | Spec-proven identical to legacy's formula (`selection_kernel.py:64-84` vs `services/trigger_engine.py:2338`, checked in `scripts/check_sequencer.py`). |
| §31 | Force Scene | `BUILT` (full) | Ported verbatim to the single choke point every automatic SPECTRA pick funnels through (`scene_sequencer.fire_scene_by_id`); PR #54, room-proven. Caveats: can only redirect to one scene, not a Scene Group — depends on §2; the redirect alone is passive (only fires on a pick something else was already making) — §79 (2026-08-18) added an immediate fire on the pin/repin edit itself, closing the gap where a song with no triggers left the button looking dead. |
| §32 | Charge/Lull/Drop phase choreography | `BUILT` (full core) | Two real sub-gaps already counted separately: dynamic ramp-to-next-trigger timing (§11) and Drop-into-Scene-Group payoff (depends on §2). |
| §33 | Color Sets (individual named palettes) | `BUILT` (full) | Shared storage, read by both apps. |
| §34 | Palettes / Gradients libraries | `BUILT` (full) | SPECTRA's ported Builder and scene editor read the same shared endpoints. |
| §35 | Spotify connection & Last.fm fallback | `N/A` | SPECTRA never talks to Spotify/LedFX directly by design — inherits resolved track/genre data from legacy via the read-only bridge. Correct architecture, not something to build. |

### Tier 5 — retired on the Admiral's word (2026-08-12)

Per `data/spectra-gap-inventory/decision-legacy-retirement-picks.md`: no SPECTRA rebuild is
planned; nothing deleted from legacy either.

| § | Capability | State | Notes |
|---|---|---|---|
| §36 | Light Mode Chooser + all per-node dark/light authoring (card modes, mode lanes, group variants, per-trigger forces) | `RETIRED` | **Does not cover the global Dark/Light mode** — that's §9, a `BUILT`, fixture-proven day-one-bar item, explicitly carved out as NOT retired. Don't conflate the two. |
| §37 | Beat-quantized sequences | `RETIRED` | |
| §38 | Device-settings action (raw brightness cap/frequency-target action type) | `RETIRED` | Distinct from the Devices page's light-management UI (§7), which is not retired and is a real gap. |
| §39 | LedFX scene action (`ledfx_scene`) | `RETIRED` | |
| §40 | Trigger label filtering (positive/negative label matching) | `RETIRED` | |
| §41 | Set Lists authoring, incl. per-song alternate "trigger slot" feature | `RETIRED` (SPECTRA rebuild) / **legacy runtime code-disabled 2026-08-17** | Two separate axes, don't conflate: (1) this Tier-5 `RETIRED` marking means no SPECTRA rebuild of Set Lists authoring is planned — unchanged, still accurate. (2) **OQ-5, resolved (Admiral edit, 2026-08-17: "don't delete the data but yes, retire that function for now")** — the "slots" per-song alternate trigger list is now disabled *in legacy's own running code*, not just un-rebuilt in SPECTRA. Runtime/xcorr-timing side of Set Lists (`Setlist` model, `active_setlist_id`, `setlist_offsets` — a completely different field) is **not** retired, still fully live in both legacy and SPECTRA. **Where his data lives (untouched, not moved, not migrated):** `storage/profiles/*.json`, one file per song profile, field `setlist_triggers: dict[str, list[MusicTrigger]]` (`models/song_profile.py:77`, keyed by `Setlist.id`) — readable/restorable exactly as before, nothing deleted, nothing rewritten. **What was disabled (a flag flip away from restoring, not a rebuild):** `services/trigger_engine.py::TriggerEngine.SETLIST_SLOTS_ENABLED = False` (class attr right above `_user_triggers()`) — when `True`, that method resolves `state.active_setlist_id` against `self._profile.setlist_triggers` and returns the slot's list if the active Set List has one; with the flag `False` it always returns `self._profile.triggers` (Default), so the engine never fires slot timestamps. `routers/control.py::active_triggers()` (`GET /api/control/active-triggers`, the Now-Playing-marker mirror of the same logic) gates its own "setlist" source branch on the identical `engine.SETLIST_SLOTS_ENABLED` flag, kept in lockstep so the frontend never reports a source the engine isn't honouring. Authoring UI disabled the same way, in **both** copies (`web/src/builder/` — spot-effects — and its byte-identical SPECTRA port `spectra/web/src/timeline/`, since they read/write the same backend): each of `components/ModeBar.tsx` (the slot picker `<select>` + its sticky-slot restore effect), `components/ImportDialog.tsx` (slot entries in the "Copy from" list), and `components/ShiftAllControl.tsx` ("Commit ALL" writing shifted timestamps into `setlist_triggers`) carries its own local `const SETLIST_SLOTS_ENABLED = false;` gate — flip all of them (1 backend flag shared by `trigger_engine.py`+`control.py`, plus 3 frontend flags per tree × 2 trees = 7 flags total) back to enabled/`true` to restore in full; every gate is a boolean, no code was deleted, no migration or rebuilt UI is needed to bring it back. **Checked and confirmed nothing else depends on `setlist_triggers`**: the only other runtime users of `state.active_setlist_id` (`trigger_engine.py`'s xcorr-offset resolution at lines ~207/252, `auto_offset_service.py`, `capture_alignment.py`'s post-recapture Δ-shift migration, `websocket_manager.py`, `timing_viz_router.py`, `audio_shape_router.py`) all key off `AudioShapeMeta.setlist_offsets` — a different field entirely, xcorr timing not trigger content — and are untouched; `capture_alignment.py` deliberately still keeps `setlist_triggers` timestamps shifted in sync on a recapture (data hygiene, not the retired feature) so a future re-enable doesn't inherit stale-by-recapture data. Help text (`web/src/help/helpContent.ts`'s "Set Lists" entry) updated to describe the current state instead of the retired instructions. |
| §42 | Per-trigger scene-group-color/drop-group/display-mode overrides | `RETIRED` | Depends on §2, itself gone from SPECTRA. |

### §43–§81 — SPECTRA-native builds since the audit (2026-08-14 → present)

Not in the original 46; added because they exist now and this ledger is the live surface.

| § | Capability | State | Notes |
|---|---|---|---|
| §43 | Device preview strip (favourite devices, live) | `BUILT` (full), room-unproven against his real hardware — PR #85, #86, #96 (gap found), fm/spectra-preview-facade-source (fix), PR #115, fm/spectra-preview-smoothness (choppy-render fix, canvas paint) | **New SPECTRA-only build, not a legacy port** — confirmed absent from legacy by the audit's own dedicated search. Approved 2026-08-14, his own words: *"Great, move forward, but add a pause button to pause the preview and conserve resources."* Plan: `data/spectra-device-preview-plan/report.md` (+ a pause-requirement section appended 2026-08-15). PR #85 merged. Built exactly as planned: `spectra/services/device_preview.py` (one upstream WS client to LedFX's own `visualisation_update` feed, throttled to 8fps at the relay independent of LedFX's own config, favourites store with genuinely-driven-virtual auto-population capped at 4), `spectra/api/device_preview.py` (favorites GET/PUT, status, pause/resume, a dedicated `/device-preview/ws` fan-out — NOT the general `/api/ws` channel, so pixel frames never load pages that aren't looking at them), frontend `DevicePreviewStrip.tsx` + `FavoritesPicker.tsx` mounted in the shared `TopBarStrip.tsx` beside §50 (outside `<Routes>`, confirmed by direct navigation in a smoke check that the strip and its WS connection survive a route change without re-subscribing). **The pause requirement is the part that was checked hardest, per his own stated reason (conserve resources), not asserted**: `DevicePreviewRelay._consume`'s receive loop polls a cleared `_active_event` on a 0.2s timeout instead of blocking forever on `ws.recv()`, so pause breaks out of `async with websockets.connect(...)` — the block whose exit is what actually closes the socket — promptly; proven against a REAL fake-LedFX WebSocket server (not just a local flag) in `tests/test_device_preview.py::test_pause_actually_closes_the_upstream_socket_not_just_the_display`: the fake server's own live-connection count drops to zero on pause and `frames_received`/`frames_relayed` both freeze, not just the display. Re-proven live in a chrome-devtools-axi smoke check against an isolated instance (spare ports, fake local LedFX — his real room/8010/8888 untouched): `GET /api/device-preview/status` read `connected:false` immediately after clicking Pause and stayed there, frame count frozen, badge read "paused" with dark swatches (not stale colour). **One real bug found and fixed by that same smoke check, not by the tests alone**: a status push only fired from the explicit pause()/resume() API handlers, but the actual reconnect after resume() completes asynchronously — an already-open tab could sit on "reconnecting…" indefinitely despite the server being fully live again. Fixed by pushing a status broadcast on every `connected` transition (`DevicePreviewRelay._set_connected`), not just explicit calls; regression-proven in `test_status_change_broadcasts_on_every_connect_transition_not_just_explicit_pause_resume`. Help: `spectra/web/src/help/helpContent.ts` topic `device-preview`. **OQ-7 decided (2026-08-15) — built the report's own "yes" recommendation**: a hidden browser tab now auto-pauses the feed, auto-resuming with no click needed. Rejected the obvious shortcut of reusing pause()/resume() for this — those are relay-wide, so a hidden TAB pausing would have blacked out every OTHER open tab too. Instead `DevicePreviewRelay._wants_upstream()` gained a third, independent requirement — `has_viewers()`, wired to `preview_ws_manager.client_count() > 0` — so the upstream LedFX connection only stays open while at least one downstream `/device-preview/ws` client is connected; the frontend (`api/devicePreviewWs.ts`) closes its own socket on `visibilitychange`→hidden and reopens it on visible, calling `relay.viewers_changed()` from the API's connect/disconnect paths to re-evaluate demand. This composes correctly with multiple tabs for free (one visible tab keeps the feed alive for all of them) and never writes the persisted `paused` flag, satisfying his own tradeoff note verbatim. **Held to the same bar as the original pause**: `tests/test_device_preview.py::test_last_viewer_leaving_closes_the_upstream_socket_and_a_viewer_returning_reopens_it` proves the fake-LedFX server's own connection count drops to zero when the last viewer disconnects and comes back to one when a viewer returns — not a local flag. **Visibly distinct, both nameable**: the badge shows a fourth state, blue "idle — tab hidden" (`api/devicePreviewWs.ts`'s `tabHiddenPause`, local knowledge that THIS tab closed its own socket on purpose), never the gray "paused" his own sticky Pause button shows — driven independently, so a manual pause and an auto-pause can never be confused for each other. Re-proven live in a chrome-devtools-axi smoke check against a second isolated instance (spare ports 18010/18888, its own storage dir, fake local LedFX — his real room/8010/8888 untouched throughout): with a real tab-hidden signal fired at the page, `GET /api/device-preview/status` read `connected:false` and the fake LedFX server's own `active_connections` dropped from 1 to 0 within the same second, `frames_relayed` froze (424 → 424 → 424 across two 1s checks) while the badge read the distinct blue "idle — tab hidden" (`.badge-blue`) rather than the sticky pause's gray; firing the visible signal back reopened the upstream connection (`active_connections` 0→1, `frames_relayed` resumed climbing) and the badge returned to "live" with no click. Separately proved his own sticky Pause survives a hidden→visible cycle unclobbered — clicking Pause, then toggling tab-hidden and back, left the badge on gray "paused" throughout, never flipping to "idle." **Still unverified against his real browser/hardware** — every proof above ran against the offline fake-LedFX harness and a fake local instance; his room stayed off-limits for this task. **GAP FOUND 2026-08-16 room-proving against his real stack, then FIXED same day (`fm/spectra-preview-facade-source`)**: the relay above always assumed the external LedFX process was the writer. Wrong in his normal S3 operating state — `ledfx.service` is deliberately stopped once light ownership moves to `"spectra"` (his live default since the handover; the ownership reconciler alarms if it comes back), so the strip sat on "reconnecting…" forever against nothing (`AGENTS.md`'s device-preview section has the full incident history). **Fix — ownership-routed source, not always LedFX**: `device_preview._source_mode()` reads the SAME light-ownership record `fx_seam.py` routes writes by — `spot-effects` owns → the original LedFX-websocket relay (`_consume_ledfx`, unchanged, correct whenever LedFX really is the writer); `spectra` owns → `_consume_facade`, a direct in-process subscription to `fx.events.Event.VIRTUAL_UPDATE` on the live `spectra.services.live_host.live.host` — the SAME event the frame-freshness tap already listens for, the literal pixel buffer just flushed to the device, zero network hop, zero LedFX; anything else (handing-over/released/not-yet-active) → honestly `connected:false`, `source:"none"`. Same wire shape both paths (`_facade_frame_payload`), so the frontend decoder needed no change. **The three carry-forward requirements re-proven against the new source, not assumed to transfer**: (1) pause — `_consume_facade` removes the `Event.VIRTUAL_UPDATE` listener from the live host's own registry, proven in `tests/test_device_preview.py::test_facade_pause_removes_the_listener_not_just_a_local_flag` by firing a frame DIRECTLY at the real event bus while paused and showing it never reaches the relay (not just that `connected` reads false) — plus a live chrome-devtools-axi proof against an isolated instance (spare port 18010, a real `fx.headless` dummy render thread, his real :8000/:8010 never touched): clicking Pause froze `frames_relayed` at 58 and held there, swatch went to the dark placeholder, Resume reopened it and frames climbed again; (2) hidden-tab auto-pause — `test_facade_last_viewer_leaving_removes_the_listener_too` plus the same live proof: a dispatched `visibilitychange`→hidden froze frames at 96 with the badge reading the distinct blue "idle — tab hidden" (never touching the sticky `paused` flag), and toggling visible auto-resumed with no click, frames climbing to 106; (3) licence position — re-examined fresh, not inherited from the LedFX-protocol reasoning: `fx/` is already GPL-3.0-vendored and already imported throughout spectra/, so this is no new incorporation; `ledfx/core.py`'s own throttle/serialize logic was explicitly dropped from that vendoring, so it is NOT copied here — `_facade_frame_payload` is an independent implementation reusing this module's own pre-existing SpotFX-authored throttle bookkeeping instead (full reasoning: `device_preview.py`'s module docstring). Favourites resolve identically on both paths (`room_topology.genuinely_driven_virtual_ids()`, transport-independent virtual identity — confirmed, not assumed). Tests: `tests/test_device_preview.py` section 5 (5 new: source-mode resolution, real-frame relay with no websocket, the two genuine-stop proofs, status().source) against a real `fx.headless` render thread — 19/19 pass in this file, full suite 517 passed / 5 skipped / 5 pre-existing environmental failures unrelated (`test_crystal_activation_verify.py` ×3, `test_settings_agent_cli.py` ×2 — missing `mcp` package/activation-gap fixtures, same class already documented at §61/§60). **Still room-unproven against his actual hardware** — the isolated-instance proof above used a dummy device, never his real bulbs/strips; his room stayed off-limits for this task, per its own hard limits. **PHONE LAYOUT FIXED 2026-08-16, his own report after using it once (`fm/spectra-preview-phone-matrix`)**: "the preview stretches out in a line super far I don't see any Matrix for The Matrix previews... Also in collapse mode it still doesn't quite fit." Root cause established first, per his own instruction to say which: NOT a payload gap — `device_preview.py` has always emitted `shape: [rows, cols]` correctly for both the facade and LedFX paths (`_facade_frame_payload` reads `virtual.rows`; the LedFX relay passes `msg["shape"]` through unchanged) — the row count reached the frontend from day one. The defect was entirely in `DevicePreviewStrip.tsx`: expanded mode decoded every device to one flat row of fixed-3px pixel spans regardless of `frame.shape`, so his favourite `crystal-mapper` (a real 72×37 matrix, 2664 pixels, confirmed against his live config) rendered as one 2664-pixel-long line — both of his symptoms, one device. Fixed by reading `shape[0]` (rows): `rows > 1` renders `device-preview-matrix`, a CSS grid (`repeat(cols, 1fr)` columns via a `--cols`/`--rows` custom property, `aspect-ratio: cols / rows`, no JS measurement) that reads as a genuine grid at 100% of the available width; `rows === 1` renders `device-preview-pixel-strip` with `flex: 1` pixels sharing exactly 100% of the container width (not a fixed per-pixel width), so his 7/10/17-pixel strips read as clean lines without looking broken or stretched, and an arbitrarily long strip (e.g. `crystal`, 976px, not currently a favourite) would stay contained too. Expanded devices stack vertically (`device-preview-chips.expanded` switches flex row → column) — "expand" now grows the page downward, never sideways. Collapse-mode's separate complaint was `.device-preview-strip`'s `white-space: nowrap` with no `flex-wrap`, forcing the whole label+swatches+badge+3-button row onto one line that ran off a phone's right edge; now wraps. Verified phone-first, per his own correction that the prior build was evidently never checked at a real phone width with his real device list: a static harness reproducing his exact favourites (`crystal-mapper` 72×37, `hues` 17, `hue-lights` 10, `dining-hues` 7) rendered via chrome-devtools-axi (Playwright Chromium, headless) at 390×844 and 360×780 — `document.documentElement.scrollWidth` equalled `window.innerWidth` at both widths (no horizontal overflow) in both collapsed and expanded states; the expanded screenshot shows `crystal-mapper` as a legible 72-column grid and the three short devices as full-width lines. Not room-proven against his real live pixel colours (his `:8010` instance was read-only off-limits, no writes and no restart) — the harness proves the layout mechanism (shape-driven grid vs. line, containment, vertical growth) against his real dimensions, not live colour fidelity, which was never in question. **UI tightened 2026-08-16, his own words (`fm/spectra-topbar-tighten`)**: "it doesn't need to say the word preview I know what it is... we don't need a button for pause and resume and also an indicator for if it's paused or running or reconnecting. make the button the indicator and don't put text use icons." All subtractive, no behaviour change: the `device-preview-label` "Preview" span is gone; the separate `.badge` status text ("live"/"paused"/"idle — tab hidden"/"reconnecting…"/"unavailable") and the text Pause/Resume button collapse into ONE round icon-only `device-preview-status-btn` that both shows state and toggles it on click — same `togglePause()` call as before. His own manual pause vs. the automatic hidden-tab pause (the OQ-7 distinction above) was named a blocking requirement and stays fully distinguishable: colour AND glyph both differ per state (⏸ purple = live, ▶ gray = his own pause, ⏾ blue = automatic tab-hidden idle, ↻ amber = reconnecting/unavailable), with the full explanation moved into the button's `title`/`aria-label` rather than dropped. Verified with a static visual harness (same technique as the phone-matrix fix above — plain HTML reproducing the component's exact class names against the real compiled `tokens.css`, chrome-devtools-axi/Playwright Chromium at 390×844, his real `:8010` untouched): all four state/colour/icon combinations render distinctly, the accessibility tree carries a distinct `aria-label` per state (confirmed via `snapshot`), and `document.documentElement.scrollWidth === window.innerWidth` (no horizontal overflow) at phone width. `tsc --noEmit` and `vite build` both clean. Help: `spectra/web/src/help/helpContent.ts` topic `device-preview` rewritten to match (no more "Preview" title, badge language replaced with the single-button description). **Choppy-preview fix (2026-08-17, PR #115, `fm/spectra-preview-smoothness`, his own report): "The preview mode in spectra is not anywhere near as smooth as ledfx version. ledfx was very good, and should be the standard. copy as much as makes sense."** Read the real LedFX source first, per standing instruction (`/home/javi/ledfx-src`): the analogue of this component is `frontend/src/components/PixelGraph/*.tsx` (not the audio visualiser `VISUALISER_CONTEXT.md` names) plus `ledfx/core.py`'s `setup_visualisation_events` and `ledfx/api/websocket.py`'s dual-path sender. LedFX ships FIVE preview render variants; the DOM-per-pixel one (`PixelGraphBase.tsx`, `variants: 'original'`) is kept only as a slower legacy fallback behind a settings toggle — `'canvas'` (`PixelGraphCanvas.tsx`: a raw WebSocket subscription callback writing straight to a canvas ref via `ctx.putImageData()`, no React state in the hot path, `imageSmoothingEnabled=false`) is the shipped default (`store/migrate.ts` migration 20 forces it). Backend-side, LedFX throttles per-vis_id to `visualisation_fps` (default 30, config range 1–60) AND caps total transmitted pixels at `visualisation_maxlen` (default **81**, aspect-preserving PIL bilinear resize, `utils.shape_to_fit_len`/`resize_pixels`) before whole-frame base64 encoding; its `WebsocketConnection._sender` uses a per-vis_id single-slot mailbox (latest frame always overwrites an unsent older one) so a slow client is never handed a backlog. **Measured all three layers before changing anything, against his real device shapes** (`crystal-mapper` 72×37 = 2664px, plus 17/10/7px strips) via an offline `fx.headless` render thread (never his real hardware): **SOURCE** — `Event.VIRTUAL_UPDATE` fires at ~62fps regardless of device size (crystal-mapper and the strips alike), nowhere near the bottleneck. **TRANSPORT** — `DevicePreviewRelay`'s facade throttle (`RELAY_TARGET_FPS=8`) relays at a measured 7.66fps, matching design intent for every device size; the one real disparity is payload bytes — crystal-mapper's full undownsampled frame serializes to **~29.4KB**, the strips to **185–296B** (100–150×), because `_facade_frame_payload` (unlike LedFX's backend) never caps total pixel count. **RENDER was the actual bottleneck**: a real-React harness reproducing `DevicePreviewStrip.tsx`'s exact JSX (one `<span>` per pixel, one shared `frames` state object re-rendering the WHOLE strip on ANY favourite's frame, not just the one that changed) measured reconciling the 2664-cell matrix at avg 6.5–8.7ms / p95 26–31ms / max 38–51ms on a idle desktop headless Chromium — and avg 43–47ms / p95 55–65ms / max **86–98ms** under a phone-class CPU-throttle proxy (chrome-devtools-axi `emulate --cpu 4`) — a third to most of the entire 125ms budget at 8fps, before any other page activity. The equivalent `canvas.putImageData()` write measured **0.04–1.2ms across both conditions** (30–1000× cheaper), and JSON.parse + `decodePixels()` of the full 29KB crystal-mapper payload cost <1ms even throttled — confirming payload size, unlike render cost, was never actually load-bearing. **Carried from LedFX**: the canvas-ref-and-`putImageData` render path exactly as `PixelGraphCanvas.tsx` does it (`DevicePreviewStrip.tsx` now paints imperatively via `canvasRefs`/`swatchRefs`, a frame never touches React state, `shapes` state is retained only to pick matrix-vs-strip CSS layout and is change-guarded so it doesn't itself trigger a per-frame re-render), plus `imageSmoothingEnabled=false` + CSS `image-rendering: pixelated` for the crisp per-LED block look. **Named incompatibility, deliberately NOT carried**: LedFX's `visualisation_maxlen≈81` downsample cap — measured decode cost proves it buys no smoothness once canvas removes the render bottleneck, and it would directly conflict with his own prior explicit ask (this same row, the phone-matrix fix: *"I don't see any Matrix for The Matrix previews"*) — capping crystal-mapper to ~81 points would turn Expand into a blur instead of his actual matrix shape, so `spectra/services/device_preview.py` still ships the full frame. Also NOT changed: `RELAY_TARGET_FPS` stays 8 — SOURCE/TRANSPORT measurements confirm 8fps was never the choppiness cause (a deliberate, already-documented resource-conservation choice, module docstring's "a glance preview doesn't need 30fps"), and LedFX's own per-vis_id mailbox-coalescing wasn't ported either since the relay's existing per-vis_id `_throttle_ok` gate already serializes frames before a broadcast task is even created — no observed pile-up to fix. **Live pipeline re-proven end to end** on an isolated instance (spare port, temp storage, `fx.headless` multi-virtual host built to his real shapes, never his live `:8010`/`:8000`): `canvas.width/height` read back as exactly `72×37` for crystal-mapper and `17×1`/`10×1`/`7×1` for the strips (native resolution, confirmed not downsampled); two `getImageData` samples 2s apart on the same pixel returned different real colours (genuine live animation, not a static placeholder); clicking Pause froze the canvas at the dark placeholder `rgb(40,40,40)` and the status button read "Paused, click to resume", Resume brought live colour back — the backend genuine-stop guarantee itself is unchanged and untouched by this fix, still covered by the existing `tests/test_device_preview.py` proofs. Tests: `tests/test_device_preview.py` 19/19 pass unchanged (backend untouched); broader spectra/fx-filtered run 100 passed, 1 pre-existing environmental failure unrelated to this change (`test_mid_group_scenes_animate_on_the_harness`, needs seeded live STAR scene data — documented pre-existing gap, not caused by this PR); `tsc --noEmit` and `vite build` both clean. **Still room-unproven against his actual browser/phone** — every proof above ran against an isolated dummy-device instance; his room stayed off-limits per this task's own hard limits. |
| §44 | UPDATE trigger action + trigger migration completion | `BUILT` (full) | Day-one bar item (D2). PR #57 merged (`09d0992`), builds the Admiral's own definition of UPDATE — "a major change within the scene, bigger than a flare, overrides the drift, ramps in" (`data/spectra-trigger-migration-scoping/RULING.md`, quoted verbatim) — as `SceneV2.update_kind` + `ResponseEngine.on_update`, glide-not-jump on an 800–3000ms ramp. **Live-proven**: 10,698 authored + 1,179 generated = 11,877 triggers migrated on his real hardware; UPDATE proven as an 800ms glide landing on target params (`data/backlog.md` `spectra-trigger-migration-scoping`, done). Ran live **while the PR was still unmerged** — see [Landed vs. deployed](#landed-vs-deployed-a-real-gap-this-document-does-not-close). **Scene-id pool question (formerly OQ-1) is RESOLVED, not open**: his ruling was Option B, `scene_id=None` handed to the room's own chooser for all 3,546 pool-based triggers, accepted with open eyes that the chooser may pick a mismatched-mood scene since it doesn't know his hand-built pools — "if he hears it in his room, he will say so, and it is reversible" (`RULING.md` item 1, implemented in `spectra/services/legacy_trigger_migration.py`). Drop-scene triggers get intensity forced to MAX per the same ruling (`RULING.md` item 2). Dinner Party Scenes (the pool, not the §4 capability) dropped from the migration entirely at his word (`RULING.md` item 4) — do not conflate with §4 above, a different capability with its own separate keep-not-drop decision. **No longer inert (code-verified 2026-08-15)**: his room's `scene_change_mode` is now `"full"` — the note above describing `"analysed"`/inert was accurate when written and has since been overtaken by his own room setting, not by a code change. **Analysed-trigger backfill** (Admiral ask, `data/backlog.md` `spectra-analysed-trigger-import`, "not every song has authored triggers from spotfx, but they should all have analysed triggers that can be imported"; PR #75): the authored migration above only covers the 312 songs the legacy world had triggers for; `scripts/import_analysed_triggers.py --apply` ran `midsong_generator.generate_for_song` (front 3, §3) for every one of the other analyzable songs, back-filling 514 of them (16,345 generated triggers; 5 already had partial coverage from `maybe_auto_generate`'s reactive first-play auto-generation and correctly no-opped; 1 song has a single whole-track librosa section with no internal boundary to seed from — an analysis-quality gap, not a migration gap). Idempotency and authored-corpus integrity proven by re-running the script against the live store a second time (0 added/updated/deleted) and a byte-for-byte diff of every authored trigger against a pre-run backup (0 changed). Room-proven: an imported trigger fired at his real lights during this PR's own review (`JHAYCO, J Balvin, Bad Bunny - No Me Conoce - Remix`, previously zero-authored, repeated `fire_scene` fires observed via `GET /spectra/api/engine/status`'s `triggers.last_fire`). **13 more were caught and excluded, not silently counted** (firstmate review, same day): a handful of `storage/audio_shapes/*.json` sidecars — and their own legacy profile files — carry a synthetic `ledfx:<artist>:<title>` `spotify_uri` because the real Spotify id was never resolved at capture time (12 real Bad Bunny tracks; one, "ScriptTester - OneEventTest", is a 60-second test fixture, not a real song). `trigger_store` is keyed by the exact string the live bridge presents, which is always `spotify:track:...` (828/828 other analyzable songs, 317/317 pre-existing keys, zero exceptions) — a trigger written under the synthetic key can never fire. The script now filters to `REQUIRED_URI_PREFIX = "spotify:track:"` and asserts it again at the write call; the 523 already-written dead triggers were deleted from the live store (verified: zero non-`spotify:track:` keys remain). Current live total: 10,698 authored + 17,631 generated = 28,329 triggers across 831 songs. |
| §45 | Settings console (standing order 5, realised for SPECTRA) | `BUILT` (full) — authority-boundary case, code-verified 2026-08-15 | PR #66 merged (`f002f9f`). **Live-tested against his own words, verbatim, in text form**: sent `"I do not want any of the flares in the scene star to change the number of edges."` — his exact sentence, not paraphrased — to the live console (`POST /spectra/api/settings-console/message`, fresh session). It correctly refused: *"That's outside what I can do here. This settings console only controls five things: brightness multiplier, ambient mode/color, global transition time, and scene-change tier. Flare/effect parameters (like an edge count on a scene's flares) live in the scene/effect editor, not here — I have no tool to touch that."* No `set_setting` call succeeded (`changes: []`), and independently verified against the stored config, not the console's own claim: `storage/spectra/scenes.json` md5 unchanged across the call, `settings_log.json` gained no entry, `room_controls.json` mtime untouched. **This is the honest-refusal case, not the fabrication mode §45 was built to guard against** (a confident reply claiming a change landed when nothing happened) — the reply never claims success and names its own actual scope correctly. One residual gap in this proof: the API response doesn't expose the raw Anthropic transcript, so a `get_settings` (read-only) tool call could not be directly confirmed, only inferred (the reply's five-setting enumeration matches the live registry exactly, which a bare guess is unlikely to reproduce) — a `set_setting` call is ruled out with certainty via the independent file checks above, which is the part that actually matters. **Not yet verified**: the voice/transcription half (`spectra/services/transcription.py`) — this test used typed text standing in for "as he would have typed it," not the mic button; that path ships explicitly unverified against a live transcriber per its own module docstring, and the standing-order-9 worked-example/walkthrough offer is still outstanding. See [OQ-6](#open-questions) for the follow-on scene-edit request this same test was paired with. |
| §46 | LedFX's own colour picker adopted across SPECTRA | `BUILT` (full), **room-unproven** | PR #59, merged (`72a6151`) — the rebase conflict this row once flagged is moot now it's landed in `master`. Built, unverified: no dedicated real colour-pick proof in his room yet (the 2026-08-14 room-window proof exercised the Ambient path, not this picker directly). |
| §47 | Per-device flush cadence (crystal cadence workaround) | `AGREED` | PR #58 open — genuinely still open, do not flip this one. **Failed in his room twice**, measured on real firmware counters both times (crystal dropped 30→20-24fps on the second attempt). Do not merge or deploy as-is. Root cause open: why its tests pass while his fixtures degrade. His ruling stands: the crystal's 30fps cap itself is deliberate and not to be raised as a "fix" — see [OQ-2](#open-questions), a separate, narrower question. |
| §48 | Feedback/Review Stage 2+3 polish redesign | `BUILT`, room-unproven | PR #65 merged. Built, unverified, first time he'll see `/feedback` and `/review` at all — blocked on two open design questions, [OQ-3](#open-questions) and [OQ-4](#open-questions). |
| §49 | Release-to-HA actually restores/neutralises the Hue bulbs (not just the stream) | `BUILT` (full) | PR #67 merged (`e3d754a`). Fixes a defect found live 2026-08-14: panic release closed the entertainment stream but abandoned 17 Hue lights lit at 12% on SPECTRA's last frame. `spectra/services/release_fade.py`: freeze the entertainment stream → bridge-side dim fade → power off, run against every live Hue device across **both** bridges (his two Hue devices sit on separate bridges), best-effort per device. D1 "free" case, reasoned explicitly in the PR: legacy's own fade lands ON a colour because a next state follows; release has no next state, so this fades to off instead. WLED verified unaffected (already releases realtime cleanly via its own on-device fallback — genuinely different in kind from Hue, which has none). **Room-proven live at the fixtures, 2026-08-15** (reported via firstmate). See D5 for the settled division of labour this establishes: SPECTRA's job ends at a clean release, restoring the room afterward is Home Assistant's job, not a gap to close here. |
| §50 | Live section-energy readout (top bar, always visible) | `BUILT` (full) | PR #62 merged. Shows the exact number (`bridge.intensity()`, raw librosa section energy, no smoothing) that already drives the sequencer, the drift conductor, and automatic transition fires — not a display lookalike. Room-proven 2026-08-14, `docs/spectra-room-window-proofs-2026-08-14.md` Proof 1 (PASS): live across `/spectra/scenes` and `/spectra/timeline`, honest `—` dash when no value. First occupant of the shared `TopBarStrip.tsx` mount point §43 will join. |
| §51 | Ambient — confirmed vs. attempted light read-back (the ON path) | `BUILT`, success case only — see note | PR #69 merged (`36198b1`). Fixes a live defect distinct from §6: `Ambient ON: [...] held at #f5da8c, 17 light(s) set` logged identically whether all 17 bulbs actually carried the colour or 3 silently didn't (a 2xx bridge PUT only means the bridge accepted the write — zigbee can still drop it under a write burst). `ambient.reconcile()`'s ON path now reads every light back and only counts a **confirmed** match; stragglers get bounded, spaced retries plus paced initial writes; anything still not holding comes back named in `unconfirmed` (status `"partial"`), surfaced as a red badge on `RoomControlsBar.tsx` — never folded into a bigger count. **Success case PROVEN LIVE** on his room (commit `bb1b759`, deployed before the PR merged — see [Landed vs. deployed](#landed-vs-deployed-a-real-gap-this-document-does-not-close)): 17/17 confirmed at `#f5da8c` across both bridges, room verified back at his exact baseline after. **Failure case PROVEN HEADLESSLY ONLY, not live** — `tests/test_ambient.py::test_reconcile_on_silently_dropped_write_is_never_reported_as_held` contrives the identical failure shape and asserts the honest partial report, but no bulb inside `dining-hues`/`hue-lights` was actually unreachable at proof time to exercise this for real; if that's done later this row should be upgraded explicitly, not silently assumed. **Checked and confirmed clean, not assumed**: the release path (§49, `spectra/services/release.py`, already reads real state back via `_verify_released()`) and the scene-fire path (`spectra/services/fx_seam.py::apply_writes`, raises loud on HTTP failure rather than under-counting) do **not** share this attempted-vs-confirmed gap. |
| §52 | Ambient — music-precedence, i.e. mode "auto" (yields to playback, restores on its own) | `BUILT` (full) — room-proven, all three states | **Field renamed since this proof**: `RoomControlState.ambient_enabled` (bool) below became `ambient_mode: "off"\|"always"\|"auto"` in §53, the same day, once the Admiral corrected "music always wins" — what this row calls `ambient_enabled: true`/`"yielding"`/`"holding"` is exactly today's `ambient_mode: "auto"`; the proof narrative is left in its original words rather than silently rewritten. Fixes a live defect distinct from both §6 and §51: proven 2026-08-15 with `ambient_enabled: true` and a real track playing, `scene_change_mode: "full"`, an active scene, and triggers firing — **all 19 Hue bulbs sat frozen at ambient cream, following none of it**; turning Ambient off by hand released 17/19 to the music within seconds. Ambient didn't merely compete with music, it silently swallowed the whole song — a second, independent cause of his original "I don't see scene changes" complaint, on top of the `scene_change_mode` fix. His ruling: Ambient is the room's RESTING state, not one that outranks music — MUSIC WINS while it plays, and Ambient resumes **on its own** the instant it stops (the restore half of "I want the whole house to restore when a music sync ends", arriving from the other direction). New module `spectra/services/ambient_music_gate.py` is the single choke point every path that can change the live hold now funnels through — a human PUT (`room_controls.reconcile_ambient_if_changed`, also covers the settings-console agent), every bridge "state" broadcast (`engine.py`'s `_on_track_uri`), and process startup/resume (`app.py`) — so none of them can bypass the precedence rule by reaching `services.ambient.reconcile()` directly. `RoomControlState.ambient_enabled` still means exactly what it said ("I want Ambient"); the gate owns only the LIVE hold, driven by a second signal, `bridge.is_playing()`. Precedence rule: a CONFIRMED read always wins, even over an existing hold (playing releases a held room; not-playing re-engages one that wasn't held) — but an UNKNOWN read (bridge never sent a signal) never actively changes anything, it carries the current hold forward; collapsing unknown onto "must not hold" was an earlier, tempting shortcut, rejected because it would make a transient bridge blip flicker-release an already-quiet room, a regression against the ease-back bar below. Every actual write still goes through `services.ambient.reconcile()` unchanged — the same function a human toggling the checkbox always drove — so **§6's release/ease-back fidelity is untouched by construction, not merely re-tested**. Visibility (the fix's second half, his own words: "a control that reads as ON while doing nothing is the exact pattern this project has spent the week killing"): `ambient_music_gate.status()` reports `off`/`holding`/`yielding`/`transitioning`, folded into `GET /spectra/api/engine/status`'s `ambient` key (no new endpoint, existing 3s poll) and shown as a persistent badge on `RoomControlsBar.tsx`, separate from the one-shot PUT-outcome badge. **Offline-proven**: `tests/test_ambient_music_gate.py` (precedence rule including the no-flicker-on-unknown case, no redundant writes, colour-while-holding re-apply, failed-reconcile retry) and `tests/test_bridge.py` (`is_playing()`'s three-way read, survives a reconnect blip). **Room-proven, all three states, 2026-08-15, PR branch deployed live**: with his real music playing ("100 MILLONES"), `ambient_enabled` flipped `false → true` via a full PUT — `ambient_result.status` came back `"yielding"` with **zero Hue REST calls fired** (no freeze attempted at all); `GET /engine/status` read `ambient: {enabled: true, mode: "yielding", held: false}` throughout; both his Hue entertainment sessions stayed **active** on both bridges the whole time, the `hues` virtual kept flushing fresh frames with genuinely changing colour (`#800080` → `#0500ff`), and `trigger_fired`/`responses` counts climbed 17→34 — the exact scenario that sat frozen at cream that morning, now confirmed following the music instead (**state 1**). `ambient.mode` correctly read `"yielding"` at every check — proven at the API field `RoomControlsBar.tsx` renders, not visually confirmed in his own browser (**state 3**). Room restored to the exact baseline read before touching anything, verified by read-back — twice, once after state 1 and again after state 2. **State 2 — Ambient returning by itself with ease-back intact — PROVEN LIVE**, captured on a genuine pause he took at his own discretion (never forced; two prior windows were let pass rather than interrupt his listening — see the conversation record). Continuously sampled both Hue bridges' `/clip/v2/resource/light` and `GET /engine/status` at ~1.2s cadence throughout (script + full JSONL trace in the PR). Observed, not asserted: `is_playing` flipped `true→false` at T+0; `ambient.mode` went `yielding→transitioning`, landing `holding` at T+11.4s with `result: {status: "on", devices: ["dining-hues","hue-lights"], lights_set: 17, lights_total: 17}` — all 17 confirmed at exactly `xy=(0.4191,0.4097)`, the computed target for `#f5da8c`. He resumed at T+20.1s (`is_playing` back to `true`); release then GLIDED, not snapped, over five distinct sampled points on `Kitchen Infuse` (representative of the group): held `bri=100.0, xy=(0.4191,0.4097)` → phase-1 dim-fade lands `bri=35.18`, same `xy` (unchanged colour, brightness-only fade) → phase-2 catch-up begins, `bri=24.11, xy=(0.4399,0.5202)` → `bri=32.02, xy=(0.4291,0.4325)`, holding there through several more samples → `mode` reads `yielding` again at T+36.4s (16.4s wall-clock for the full release, longer than the 9.5s AMBIENT_TRANSITION_MS+AMBIENT_CATCHUP_MS constants alone predict, consistent with two devices processed with real bridge round-trips rather than a single instantaneous step), `result: {status: "off", devices: ["dining-hues","hue-lights"]}`. Room resumed normal driving immediately after (`hues` virtual writes changing colour again, `trigger_fired`/`responses` counts climbing, both entertainment sessions still `active` throughout — never torn down, matching design). One live-instrument note worth keeping: reading `/clip/v2/resource/light` directly during active entertainment streaming shows **zero colour movement even while bulbs are being actively driven** — the CLIP light resource doesn't reflect the streamed frame while a Hue entertainment session owns the bulb, so it looked like a fresh regression until cross-checked against the entertainment-session-active + executor-writes + `ambient.mode` evidence above; don't use a raw CLIP light GET as the freeze/follow signal during a live entertainment stream, use the session-active + executor-write + `ambient.mode` triad instead. |
| §53 | Ambient — "On during music" (mode 2, his own requested third setting) | `BUILT` (full) — PR #76 merged, room-proven live | The Admiral corrected §52's framing the same day: "music always wins" was wrong. His words: "Ambient mode should run even while the music is playing so that my Hue Lights are lit and bright but the other lights are still running the show... it needs to be a third setting where ambient mode can still come into play when the music is still playing." `RoomControlState.ambient_mode` is now `"off"` / `"always"` / `"auto"` — his three settings, in his language, surfaced as a `RoomControlsBar.tsx` dropdown ("Off" / "On during music" / "Auto-return") rather than the old checkbox. `"always"` is mode 2: `ambient_music_gate._desired_hold()` returns `True` unconditionally, never consulting `bridge.is_playing()` at all — not the precedence bug reintroduced, a deliberate choice he asked for. **The composition question he and firstmate asked, settled from evidence before any building, not inferred**: does holding Hue starve or double-penalise the show on other devices via the selection kernel (curve × genre × affinity)? `grep ambient` across `selection_kernel.py`, `scene_sequencer.py`, `sequencer.py`, and `trigger_engine.py` returns zero functional references (one docstring mention only) — scene selection is architecturally unaware Ambient exists. The only "ambient" the engine's own deferral logic reads (`bridge.conductor_deferral()`) is spot-effects' separate LEGACY `ambient_mode_enabled` broadcast flag, not this field. SPECTRA's Ambient hold operates exclusively through `fx/devices/hue.py`'s device-level `set_frozen()` — the LAST step in the pipeline, strictly downstream of everything selection/rendering computes — so mode 2 composes for free: holding Hue can never starve or double-penalise the show elsewhere, by construction. One-way migration (`room_controls.load_room_controls`): a stored `ambient_enabled: true` maps to `"auto"` (what `true` was built and proven to mean throughout §52's own lifetime), `false` to `"off"`. `reconcile_ambient_if_changed`'s changed-check and `settings_console.py`'s registry (`ambient_mode`, kind `"enum"`, choices read live off the model same as every other enum field) both updated to match. **Offline-proven**: `tests/test_ambient_music_gate.py` (new "always" section — holds while confirmed playing, ignores every playback transition once held, only switching the setting itself releases it) plus every §52 "auto" test re-passing unchanged under the new field name; `tests/test_room_controls.py`'s new migration test. **Not yet room-proven**: built under the Admiral's own explicit order to build-and-unit-test first and wait for a room window — his room was mid-listening throughout this build, so no live Hue call has been made for mode 2 yet. The §52 ease-back measurement (two-phase glide, colour-then-brightness, ~16s wall-clock) is the regression baseline any mode-2 room proof must re-confirm survived, not assume. | **Room-proven live 2026-08-15**, same session, his real music playing throughout ("TELEFONO NUEVO" -> scene 'Orbits V2'), continuously sampled via both Hue bridges + `GET /engine/status`'s per-virtual `executor.recent_writes` (never the CLIP light resource for the streamed half, per the AGENTS.md correction). **Measurement 1, the open question, answered positively**: with `ambient_mode: "always"` holding both Hue devices (17/17 confirmed at the target colour, same as always), the drift conductor's own ~20s leg cadence (`leg_s: 20.0`) kept firing on schedule for every NON-Hue virtual (`crystal-mapper`, `radial-dummy`, `single-color-effect`, `tv-mapper` -- covering `tv-backlight`, `porch-rail`, `crystal`, the kitchen sconces, `dining-table`) across **two consecutive full cycles** (fresh writes at both leg boundaries, 19.5s apart, matching `leg_s` almost exactly) while Hue sat held the whole time -- the show never stopped or throttled elsewhere. Mode 2 is a **small gating change**, not a real gap: firstmate's own earlier claim that Ambient suppressed his music entirely was Hue-only evidence and is now corrected -- non-Hue devices were never touched by the old always-hold behaviour either. **Measurement 2, the ease-back regression check: PASSED, not flattened**. Switching `"always" -> "off"` while still playing (the same code path a natural "auto" release takes) reproduced the identical two-phase shape: brightness-only dim fade to the same landing (`bri` 100.0 -> 35.18, `xy` unchanged) held ~3-4s, then a colour+brightness catch-up glide through two more distinct sampled points before fully releasing -- **~16.9s wall-clock** (mode "off" first observed to `held: false`), matching the ~16.4s §52 baseline within measurement noise. **One new, correctly-interpreted observation from a hold this long** (previous holds were brief; "always" has no natural end): both bridges' `entertainment_configuration.status` flipped "active" -> "inactive" roughly 63s into the hold, then back to "active" once the release's REST writes resumed. This is the Hue bridge's OWN streaming-channel idle/heartbeat behaviour -- expected once a channel has carried zero DTLS frames for a while, which is exactly what freezing causes -- **not** a sign anything broke: the held light's actual `xy`/`bri` never moved a fraction through the flip, SPECTRA's own `held`/`mode` bookkeeping never lost track, and the release that followed still glided cleanly. Room restored to the exact baseline read before touching anything (`ambient_mode: "off"`), verified by read-back, his music undisturbed throughout the whole window. |
| §54 | Curve chooser grid — scene sequencing tab | `BUILT` (PR #77 merged, UI eye-checked, not room-proven) | His ask, verbatim: "when choosing a curve i prefer to see a grid that shows a snapshot of each curve" — today's picker made him choose a shape blind (name only) and find out after. `spectra/web/src/scenes/tabs/SequencingTab.tsx`'s per-scene curve `<select>` (named profile / flat / inline / not-sequenced) is replaced with a tile grid; every tile is a `CurveThumbnail` (new, `spectra/web/src/components/CurveThumbnail.tsx`) drawing that curve's OWN points through the identical px/py/clamped-flat-extension path math `CurveEditor.tsx` already uses for the full graph below — a thumbnail is a scaled-down render of the real shape, not a second interpretation that could visually disagree with it. Covers the one live chooser surface he actually uses today (this tab, per-scene attachment — the sequencer's own curve, PR #64's scene-local/shared-profile split folds straight in: named profiles get their own tile, "Inline one-off…" previews what it would start from). **Deliberately left out, and why**: the legacy spot-effects `/app/` twin (`SequencerPanel.tsx`/`CurveProfilesCard.tsx`) is frozen pending SPECTRA superseding it, per this file's own standing rule — not touched. "Flare selection" (`ScenesPage.tsx`'s flare-band `curve` select) is a fixed 4-item easing-type ENUM (linear/ease_in/ease_out/pulse), not a named profile pulled from a growable list — a different kind of curve from `CurveProfile.points`, and SPECTRA's own `ResponseTab.tsx` doesn't even expose it (chip toggles for named `FlareKind`s only) — nothing to convert. `SequencerConfig.flare_entries`/`color_set_entries`/`wheel_travel_curve` and the Drift tab's follow-curve attachment have zero chooser UI at all (agent-only via `PUT`, "by telling the agent" — see this file's Drift/sequencer sections) — no surface exists there to touch. Scale: his live library is **11 curve profiles** (`storage/spectra/sequencer.json`, counted directly, not guessed) — plain SVG per tile is trivially cheap at that count; no virtualization/canvas needed, noted rather than silently assumed cheap. Old SpotFX **did** already draw real curve shapes once a profile was selected (`CurveEditor.tsx`, same component both apps share, shipped together in PR #19/#21) — this PR's fidelity target was matching that felt experience at the CHOOSING step, which neither app had before now: SPECTRA is free here (D1), and took it. **Verified**: `tsc --noEmit` and `vite build` clean; eye-checked live on an isolated `python -m spectra` instance on a spare port (`SPECTRA_PORT=18010`, fabricated fixture storage — never his live `storage/spectra/`, never port 8010) with a bogus `SPOTFX_PORT` so the bridge never reached his real spot-effects process (confirmed "bridge ○ down" in the UI) — screenshotted 8 visually distinct tiles (flat/ramp-up/ramp-down/hump/step/plateau/wheel-travel/not-sequenced), clicked "Mid-song hump", confirmed the full `CurveEditor` below rendered the identical triangle and the attach round-tripped ("Curve attachment saved"). Not room-proven — this is UI-only and touches no device write path. |
| §55 | Sonic — scene/flare authority + Scenes-page chat popover | `BUILT`, unverified live | His own words: "I want the Sonic agent in the settings console to be able to manage things like the flares and the settings within the scenes and creating scenes etc. That means it needs to be accessible from the scenes page." Widens §45's settings-console mechanism (deliberately, structurally — his own test stands: "a settings agent that can restart a service is not a settings agent") rather than replacing it: `spectra/services/scene_console.py` is a SECOND mechanism module, zero import of `settings_console.py`/`room_controls.py`, whose only write surface is `scene_store.save()`. `spectra/services/settings_agent.py` merges both domains' operation dicts into one `ALL_OPERATIONS` allowlist Sonic dispatches against — same exhaustive name→handler lookup as before, just wider. **The architecture is his own follow-up ruling, applied as the load-bearing design choice, not an afterthought**: "the enumerated operation set that ENFORCES the boundary and the catalogue that lets Sonic DISCOVER what to change must be THE SAME DECLARATION" — `spectra/services/sonic_ops.py`'s `SonicOperation` is that one declaration (name/domain/kind/summary/instructions/input_schema/handler), serving the allowlist, the Anthropic tool schema, AND the `list_operations` discovery meta-tool from the identical object, so a capability's guard and its documentation cannot drift apart, and per-operation how-to lives with the operation instead of accumulating in `SYSTEM_PROMPT`. Reads are filtered/narrow throughout (his token-efficiency ask, "a library that keeps growing... will outgrow anything that works by loading everything"): `list_scenes` returns id/name/labels only, `get_scene_settings`/`list_flare_kinds` return one scene's enumerated summary, `get_flare_kind` returns full detail for exactly one named kind — nothing ever dumps a scene's full `devices` list. Scope, by deliberate exclusion (same posture as §45's `force_scene_*`): eight scalar SCENE_SETTINGS_REGISTRY keys (entry blend, charge/lull phase ramps, choreography timing, colour-journey pace, colour-set acceptance — bounds READ off `SceneV2`/`PhaseBlend`/`PhaseChoreography`/`SceneColorJourney`'s own `Field(ge=,le=)`, never re-typed), named `FlareKind` create/update/remove (upsert by name, full-model re-validate so removing a kind a band still references is refused), and `create_scene` (name + labels only, no device/effect authoring — that stays the Initial Set tab's own visual editor). **The structural property that protects his 9 real, authored scenes**: `create_scene` only ever constructs a fresh `SceneV2(name=...)` — id is the model's own `default_factory=uuid4`, so a created scene can never collide with, and therefore never overwrite, an existing one, even given an identical name — and there is no delete/overwrite-by-id operation in the enumerated set at all. Proven, not merely designed: `tests/test_scene_console.py::test_scene_operations_never_touch_a_scene_they_were_not_targeted_at` seeds all 9 real scene names, exercises every write op against a different, freshly-created scene, and asserts every one of the 9 scenes' stored JSON bytes are byte-identical before and after. **The four adversarial refusal categories the widening was told to re-prove (not inherit) against the new surface, each with the server's OWN quoted text**: an operation outside the enumerated set (`{"status": "rejected", "reason": "no such operation: 'delete_scene'"}`), a valid operation with an out-of-range argument (`entry_ramp_ms=999999` → `"999999 is not a legal value for 'entry_ramp_ms'"`, `pydantic_errors: ["Input should be less than or equal to 20000"]`), an attempt to reach a non-scene setting through a scene op and the mirror (a scene key through `set_setting`) — both rejected with `"... is not a {settings-console setting|scene setting}"` — and shell/service-restart/http/file attempts, all falling through the same `"no such operation"` branch untouched. **The fabrication hunt, run against BOTH backends**: the API backend's `run_turn()` was already structurally immune (its `changes` list is built from `_dispatch()`'s own return value, never model prose) and `tests/test_scene_console.py` adds a mocked-client test proving it explicitly for a scene op — a fake model response claiming "I created the scene and set its entry ramp" while the only real tool_result was a rejection (unknown scene id) leaves `changes == []` and no file on disk. The CLI (subscription) backend's `_verify_tool_manifest()` — the mechanism that already caught real fabrication on `claude-haiku-4-5` in §45's own build — is RE-PROVEN against the widened `TOOL_NAMES` (2 → 11) with hand-built, explicitly-labelled-synthetic transcripts (`tests/fixtures/cli_transcript_synthetic_scene_*.json`, no live `CLAUDE_CODE_OAUTH_TOKEN` exists tonight to capture real ones): an unavailable-tool case (empty manifest, model fabricates "I created a new scene called 'Sunset Drift'...") refuses the whole turn before any text is trusted, exactly as loudly as the original two-tool case did; the four PRE-EXISTING real captures from §45's build now correctly refuse too, as a stale/narrower-than-expected manifest — the widening's own regression proof that the check wasn't loosened to keep old fixtures green. Frontend: `spectra/web/src/components/SonicChatPopover.tsx`, a floating 💬 button + panel mounted on `ScenesPage.tsx` (his literal ask: "some kind of pop-up button"), talking to the exact same `POST /settings-console/message` endpoint the Settings page's embedded chat uses — one backend, two chat surfaces. Tests: `tests/test_scene_console.py` (23 tests), `tests/test_settings_console.py`/`tests/test_settings_agent_cli.py` extended for the merged boundary, `scripts/check_settings_console.py` extended with a scene-domain section — full suite 365 passed, 2 skipped (both live-model smoke tests, no `ANTHROPIC_API_KEY`/`CLAUDE_CODE_OAUTH_TOKEN` in this environment), 1 pre-existing environmental failure unrelated to this change (`test_mid_group_scenes_animate_on_the_harness` needs his real seeded `storage/spectra/scenes.json`, absent in this disposable worktree — confirmed failing identically on pristine `master`). **PROVEN HEADLESSLY ONLY, exactly like §45 itself still is** — no live Anthropic API call, no live `claude -p` subprocess, and no room window tonight (built and reviewed overnight, his room and his voice unavailable by the task's own constraint): the UI itself (the popover opening, the request round-tripping to a real running SPECTRA process and returning an honest 503 with no key configured) was verified against a real browser and a real isolated dev instance (`SPECTRA_STORAGE_DIR` repointed to a disposable temp dir, `SPECTRA_PORT` on a spare port, never his live 8010 or `storage/spectra/`) — but Sonic actually managing a flare or creating a scene from a spoken request, in his room, has not been watched by anyone. |
| §56 | STAR — edge count fixed: no flare moves it, initial roll 3–6 held for the run (Admiral order, resolves former OQ-6) | `BUILT` (full), room-unproven | His words, put verbatim to the live settings console first (§45 verification) and correctly refused there — flares/effect params are out of its five-setting scope — then raised and decided as its own change. First sentence: **"I do not want any of the flares in the scene star to change the number of edges."** Investigation found two mechanisms, both tracing to prior authoring in his stored scene, not incidental bugs: the Matrix device's `edges` param was a `signal="random"` binding (dice-correlated with `star`), re-rolled by the "Dice Re-roll" kind on every ordinary flare; and `"Flare patch 0.7–1"`/`"Drop patch 0.7–1"` each explicitly patched `edges` to `6.0`. Filed as OQ-6 rather than silently picked, per the brief's own instruction: authored reliance means stop and report, not decide. Firstmate relayed a recommendation of the narrower option (stop only the Dice Re-roll, keep the two patches) plus the full option set; **he first chose the wider option — freeze `edges` at a static `6`** — then, on reflection, gave a **second, superseding ruling**: **"i will make it very simple just delete whatever portion of any Flair was changing edges. but initial can be 3 to 6."** Mechanism for "3 to 6" (firstmate's own reading, not inferred, since nothing else pins down uniform-vs-weighted): every integer 3/4/5/6, uniform 25% each, independent of `star`'s own roll. Built as: (1) `spectra/models/binding.py` grows `ValueBinding.sticky: bool = False` — a signal="random" binding still resolves fresh at fire time (`scene_compiler.resolve_scene` is unaffected), but `scene_response.py::_reroll` (the "Dice Re-roll" kind's mechanism) now skips any binding with `sticky=True`; default `False` leaves every other binding/scene untouched. (2) STAR's `edges` binding becomes 4 equal steps (0.0/0.25/0.5/0.75 → 3/4/5/6), `dice: null` (no correlation with `star` — nothing asked for that), `sticky: true`. (3) Both patches' `edges` key removed, `spin`/`star` untouched. `scripts/freeze_star_edges.py` (`--apply`/`--restore`, dry-run-by-default, idempotent, house style matching `seed_star_strips.py`) is the migration; `docs/spectra-star-edges-freeze.md` is the durable, git-tracked record of the exact retired values (what's deployed today, not the deeper legacy state — see below) and the one-command restore. `edges` appears in no other scene, checked. **Provenance found along the way, recorded but NOT load-bearing for the 3–6 value**: his *legacy* STAR authored `edges` as a bare static `6` — no binding, no steps, neither patch kind exists in his legacy data at all. The dice binding and both patches were introduced during the SPECTRA rebuild by an agent, never asked for; he was told this and chose 3–6 anyway, which stands regardless. General rule this is evidence for, recorded in `AGENTS.md` ("A scene's stored data is not proof he authored it") per his explicit order, for the next crew to find rather than rediscover. `scripts/check_spectra.py`'s former "dice variants land ONLY as the three authored pairs" assertion (asserting the OLD behaviour as correct) is now split in two: the raw S1-seeder-output check still shows the pre-fix pairs (that fixture is legacy-shaped, unmigrated, by design), and a new in-memory section (never mutating the shared fixture — the `seed_star_strips.py` precedent) proves idempotency, exact restore-inversion, a 4000-draw uniformity proof over 3/4/5/6, and — the actual guarantee, not just the data shape — a full response-engine proof firing flare at 0.1/0.5/0.97 and drop at 0.97 against the fixed scene, confirming `edges` never appears in any executor write while `star` still does (the Dice Re-roll mechanism itself is unchanged, only `edges` is excluded). **PROVEN LIVE, 2026-08-16 (`docs/spectra-star-edges-freeze.md:42-70`) — this row was stale until now.** PR #82 first caught that #79's *code* had merged but the *data migration* (`scripts/freeze_star_edges.py --apply`) had not run against his live `storage/spectra/scenes.json` — a live-fired proof still showed the old binding active (edges 6→5 only, via Dice Re-roll, no 4 ever drawn). The Admiral backed up the pre-migration file and ran the migration himself. Re-proof against his real room the same night: 8 real flare/drop fires at intensity 0.9 — `edges` never appears in any response; 20 dry-run rolls hit all of 3/4/5/6, including 4 (never observed pre-migration). **Both halves of the rule — no flare moves it, and the initial roll is genuinely uniform 3–6 — are now proven live, not just built.** See §63 for a second STAR-adjacent live proof (accent colour) found and closed the same investigation. |
| §57 | Sonic — overwrite/backup/undo/preview/restore authority | `BUILT`, deploy HELD pending a real-model proof | His own follow-up, the same night, verbatim: "I want Sonic to be able to edit scenes and overwrite them just back them up ahead of time and make an easy to undo last agent change button. Scene changes can be followed by a preview and a check in to make sure it is what I wanted. and if it is not just restore the backup for that scene if we can not work it out." Four requirements, all structural, none of them the agent's to skip — his own framing, honoured literally: **(1) backup before any edit, VERIFIED not merely attempted.** `scene_console._write_and_verify_backup()` runs before every write that touches an EXISTING scene (`set_scene_setting`, `set_flare_kind`, `remove_flare_kind`, the new `overwrite_scene`, `restore_scene_backup`) — writes the pre-edit snapshot, then RE-READS THE FILE FROM DISK and compares; a successful `os.replace()` alone is never trusted, and a mismatch raises before `scene_store.save()` is ever called. Proven by making the write silently no-op (no exception, no evidence) and watching the edit refuse — twice, once for the ring, once for the once-only genesis write. **(2) undo-last-agent-change, one action, proven to actually restore.** `undo_last_scene_change()` takes no scene_id — it finds the most recent not-yet-undone log entry carrying a `backup_id` (global across scenes, matching the settings console's own existing "Undo last" UX) and restores it; proven against `scene_store` directly (never the undo call's own return value), including "undo of an undo" round-tripping correctly because restoring is ITSELF a backed-up edit. **(3) preview/check-in reads stored data, never the agent's account.** Every edit's `preview` field and the standalone `get_scene_preview(scene_id)` are both `_diff_scenes()` — a field-level comparison of two REAL SceneV2 snapshots (the backup vs. the saved scene) — proven to stay correct even when a mocked model's reply text claims a completely different, fabricated change. **(4) restore the backup when it's not right.** `restore_scene_backup(scene_id, backup_id)` — `backup_id` may be any ring entry or the literal `"genesis"`. His own two follow-up decisions, not requested but made and stated here as instructed: **retention is the last 10 edits per scene** (`SCENE_BACKUP_RING_SIZE`), oldest evicted first, specifically so an undo of an undo works and a second bad edit can't clobber the one good backup; **PLUS a permanent, never-pruned genesis snapshot per scene**, captured once on that scene's first-ever edit (or at creation, for a scene Sonic itself made) and immune to ring eviction — proven live against a stand-in for his real 'STAR' scene: three edits deep (including a wholesale `overwrite_scene`), `restore_scene_backup(id, "genesis")` reproduces the scene's exact original bytes. `overwrite_scene` itself is the one new genuinely destructive operation — wholesale-replaces name/labels/settings/flare_kinds in one shot (still never `devices`, that boundary is unchanged, not reversed by this ask) — and is subject to the identical backup gate as every other existing-scene write, no special case. Storage: `storage/spectra/scene_backups.json` (bounded ring), `storage/spectra/scene_genesis.json` (permanent), both atomic tmp+replace + read-back verified. Discovery: `overwrite_scene`/`list_scene_backups`/`get_scene_preview`/`restore_scene_backup`/`undo_last_scene_change` are five more `SonicOperation` entries in the same one-declaration allowlist §55 established — nothing new invented for this widening, the mechanism just grew. **The adversarial set the deploy hold named explicitly, all proven headlessly**: an overwrite attempted while the backup mechanism fails REFUSES (`tests/test_scene_console.py::test_dispatch_overwrite_scene_with_failed_backup_refuses_via_the_real_model_shaped_path`, run through `settings_agent._dispatch()`, the same entry point a real model's tool_use reaches); a fabricated "I restored that for you" is caught against stored data at both the mechanism layer (`test_preview_is_never_swayed_by_a_fabricated_model_reply`) and the CLI transcript-parsing layer (`cli_transcript_synthetic_restore_hallucinated_prose.json`, correct 16-tool manifest, zero real tool calls, `changes == []`); undo is proven by actually breaking and restoring a scene, verified against `scene_store`, not assumed from the call succeeding. The CLI backend's tool-manifest fabrication guard is re-proven a second time against this second widening (11 → 16 tools) with three more explicitly-labelled synthetic fixtures — including the SAME unavailable-tool shape that caught real fabrication on the original two-tool surface, now against `overwrite_scene`. Tests: `tests/test_scene_console.py` section 9 (28 new tests, 51 total in that file), `tests/test_settings_agent_cli.py` section 5c (3 new synthetic fixtures + the second stale-manifest regression proof) — full suite 392 passed, 2 skipped (live-model smoke tests, no key/token here), 1 pre-existing environmental failure unrelated (confirmed identical on pristine `master`, see §55). Frontend: `overwrite_scene`'s `preview` field renders as its own visually-distinct chat message (`.settings-console-msg-preview`, monospace/dashed border, never blended with Sonic's own prose) in BOTH `SonicChatPopover.tsx` and `SettingsConsolePage.tsx` — a real read of the saved scene, not a narration, rendered wherever he might see it. **DEPLOY HELD, DELIBERATELY, BY THE CAPTAIN'S OWN ORDER** — since **partially cleared, not fully**: "the refusal proofs ran WITHOUT a live model, and the widened surface is exactly where a real model's behaviour matters most... a boundary proven only against tests is not yet proven against the thing that will actually drive it." **PR #89 (merged 2026-08-16) ran the adversarial set against the real deployed model** (the CLI/subscription backend, live `CLAUDE_CODE_OAUTH_TOKEN`) and found a genuine production defect along the way, not a clean pass: `spectra/services/settings_mcp_server.py`'s `_call(name, **kwargs)` collided with any Sonic tool whose own argument is also named `name` — breaking 6 of the widened tools (`create_scene`, `get_flare_kind`, `set_flare_kind`, `remove_flare_kind`, `overwrite_scene`, `list_operations`) with a TypeError on every real call. The live model reported every failure honestly and never fabricated success — itself a data point in favour of the fabrication-guard design — but the defect meant most of §57's own destructive/undo/backup surface could not have been reliably exercised live before the fix. Fixed by making `_call`'s `name` argument positional-only; a new regression test spawns the real MCP subprocess and calls all six. **Not yet deployed** (fix is merged to `master`, not restarted onto `spectra.service` — deploy is explicitly firstmate's/the captain's step, not this PR's). **What remains open**: no report in `data/` or `data/backlog.md`'s still-open `spectra-morning-deploy-batch` item confirms the REST of the required checklist — an out-of-set op, a bad-argument op, a non-scene-setting reach (both directions), a shell/restart attempt, the unavailable-tool fabrication case, and `create_scene`'s fresh-id guarantee — was re-run against the real model after the MCP fix landed. Treat this row as: **proof partially run live, one real defect found and fixed, deploy plus the remaining checklist items both still outstanding** — not "deploy held, nothing tried" and not "cleared." |
| §58 | Ambient — status honesty: `held`/`mode` gated on a real confirmation, not a replayed write outcome | `BUILT`, room-unproven | Found live 2026-08-15, overnight: `GET /api/engine/status` reported `ambient: {"setting": "always", "mode": "holding", "held": true, "result": {"status": "on", "lights_set": 17, "lights_total": 17}}` while all 36 of his bulbs were physically OFF (he'd switched them off before bed) — a control reading as actively holding a dark room. Root cause, not a guess: `ambient_music_gate.py`'s `_apply()` short-circuits a repeated identical `desired` (deliberate — "no redundant Hue writes"), so under `"always"` mode, once genuinely held, NOTHING ever re-touches the bridge again; `status()` was replaying the last write's read-back forever with no re-check and no notion of its own age. **The sharp point the fix is built around**: PR #69's own read-back (§51/§52) is honest work — it does check `on`+colour+brightness together (`_state_matches`) — but a confirmation proves the moment it was taken, not the moment it's read back hours later; a control that never re-checks turns an honest point-in-time proof into a dishonest standing claim. Two changes, both additive, neither touching the write path's own mechanics: (1) `spectra/services/ambient.py` grows `verify_held(color)` — GET-only, **never a PUT, ever** (the module's own docstring states this as a hard property, matching the deliberate choice not to fight a bulb he turned off himself) — reusing the exact `_resolve_lights_named` cache and `_state_matches` check the write path already trusts, so "lit" means the identical thing whether confirmed by a write or by this independent recheck. (2) `spectra/services/ambient_music_gate.py` gets its own periodic task, `run_supervised()` (`VERIFY_TICK_S=30`, started alongside `frame_watchdog`/`ownership_reconciler` in `spectra/app.py`'s lifespan — same crash-retry discipline), calling `verify_now()` on a clock independent of bridge broadcasts (the overnight scenario is exactly the case where those may not arrive). A NEW pair of module globals, `_verified_ok`/`_last_verified_ms`/`_last_verify`, is kept deliberately apart from the pre-existing `_held`/`_held_color` (the write-INTENT bookkeeping `_apply()`'s short-circuit still needs, untouched): `status()`'s PUBLIC `held` is now `_held AND _verified_ok is not False`, gated on the most recent confirmation rather than the bare intent flag, so it can never keep reading true for a light that's actually off. A fifth `mode` value, `"partial"`, reports "claims a hold, last check found at least one light not lit (or nothing left to hold at all)" — distinct from `"yielding"` (which specifically means "auto-return standing aside for music," a different and wrong explanation for this case). `status()` always reports `verified_age_s` (seconds since whichever check — write or periodic — most recently ran) plus the raw `verify` detail whenever there's one, so a caller can tell "confirmed 4s ago" from "confirmed 20 minutes ago" instead of treating every result as equally live — the requested "say the age of the last one" alternative to a live check on every 3s poll. The SAME write-time honesty gap existed one layer deeper too, caught and fixed alongside: a `reconcile()` landing `"dark"`/`"no-hue-devices"` (SPECTRA not owning the live stack) previously still left `_held=True` with nothing to gate `held` against — `_apply()` now stamps `_verified_ok=False` immediately in that case too, via the same `_record_verify()` helper `verify_now()` uses. Deliberately NOT built, by design, matching the brief's own hard limit: the verifier never re-WRITES to force a broken hold back on — reporting reality, not fighting for control of it (mirrors `services/ambient.py`'s own stated choice not to re-light a bulb he turned off on the release path). **Superseded, narrowly, by §61 (2026-08-16)**: "detects but does not repair" turned out to be a real live defect on its own (two named stragglers sitting wrong for hours, never fixed) — the verifier now DOES re-write an ON-but-wrong-colour straggler it finds (`ambient.repair_stragglers()`), and still never re-writes a light it finds OFF, which is the one case this paragraph was ever actually protecting. UI: `RoomControlsBar.tsx`'s ambient badge grows a `partial` (red) state and a `verified_age_s`-derived "· Ns ago" / "· Nm ago" suffix plus the specific unlit bulb names in its tooltip; `types.ts` grows `AmbientVerify` and widens `AmbientGateStatus.mode`. Verified: `tests/test_ambient.py` (4 new tests: `verify_held` dark/no-devices/fully-lit/unlit-without-writing — the last explicitly asserts zero PUT calls happen during a recheck) + `tests/test_ambient_music_gate.py` (8 new tests: age tracking, short-circuit-doesn't-refresh-age, verify_now no-op/lock-skip, the flagship "a light found off downgrades held" proof, live-stack-gone downgrade, write-time dark-doesn't-report-held) — full suite **405 of 407 passed**, 2 skipped (pre-existing live-model smoke tests, no API key/token in this environment), 1 pre-existing environmental failure (`test_settings_mcp_server_starts_from_a_clean_cwd`, missing `mcp` package in this worktree's venv — confirmed identical on pristine `master`, unrelated to this change). Eye-checked live against an isolated `python -m spectra` instance (`SPECTRA_PORT=18010`, fabricated fixture storage, bogus `SPOTFX_PORT` so the bridge never reached the real spot-effects process — never his live 8010 or `storage/spectra/`): `PUT /api/room-controls` with `ambient_mode="always"` against a stack with no live Hue devices returned `ambient_result: {"status": "dark"}` as before, but `GET /api/engine/status` now honestly reported `ambient.held: false, mode: "partial"` (previously would have read `held: true`) with `verified_age_s` present and growing between polls, and the periodic verifier ticked on its own 30s cadence without error in the process log. `tsc --noEmit` and `vite build` clean. **Room-unproven, by the brief's own hard limit**: he was asleep and his lights were off because he turned them off — building the exact bar-clearing proof (claim a hold, turn a bulb off out-of-band, watch status correct itself) against his real bulbs requires his room, and that was explicitly withheld tonight; the equivalent proof runs offline instead (`test_verify_now_downgrades_held_when_a_light_is_found_off`, `test_verify_held_reports_a_light_turned_off_out_of_band_without_writing`) against the same mocked-bridge harness `tests/test_ambient.py`/`tests/test_ambient_music_gate.py` already use for every other Ambient proof in this ledger. |
| §59 | Per-song, genre-anchored intensity scale (port + headroom-reserve gate + manual mark) | `BUILT` (full), **room-proven 2026-08-16** | PR #88, merged (`f0eda7c`). Built and unit-proven; **room proof now taken (this PR)** — see the dated paragraph at the end of this row. No commit since has touched `intensity_scale.py`, `intensity_scale_marks.py`, or `midsong_generator.py` — this is still the terminal state, not superseded. Admiral's direct ask (2026-08-15): "do we still use the intensity scaling per song and per [genre]... we need to have something like that." SpotFX's own mechanism (`services/intensity_scale_service.py`) had been dropped in the rebuild — confirmed, not re-litigated. Ported to `spectra/services/intensity_scale.py`: genre slider → song-space base (`genre_to_song_scale`, calibrated 2026-07-29 — these are SCALING FACTORS, not target intensities: Dopamine≈1.20, Let It Be≈0.50, Soy Peor≈1.00 (100% = no adjustment, NOT "maximum" — a units error a 2026-08-15 report made, then retracted; see below), EDM 1.85/Rock 0.7/Trap 1.35 sliders) × a per-song bass-rank factor (0.9–1.1) from the same three signals SpotFX used, cached SPECTRA-side (`storage/spectra/intensity_scale_features.json`, never SpotFX's own cache file). Wired at the RENDER choke points only — `trigger_engine._fire`/`_fire_transition` and `scene_sequencer._roll`'s fire call — never at scene/flare/colour-set SELECTION (`selection_kernel.select`'s own `genre_mult` already factors genre into the pick; scaling intensity there too would double-count it, a defect this port deliberately avoids). **Then corrected same-day, his words, closed — not a curve-shape question**: "currently it is just a straight multiplication of a factor" was wrong; his replacement, `final = measured_intensity * 0.6 * song_scaling_factor` (`intensity_scale.combine_measured_and_scale`, the one seam every future equation change touches) — 0.6 is a DELIBERATE headroom reserve, not a fudge factor: "this allows me to gate certain high-intensities by requiring a high scaling factor on a track so we do not get loud hype scenes unless its a hype track." Clamp order is load-bearing and documented at the constant: the only clamp is on the FULL product, last — clamping the scale term early would silently defeat the gate. **His ruling on the ceiling (same day, BUILT, resolves the question this replaces — formerly OQ-6, closed not reused per the numbering convention)**: 0.75 STANDS as the automatic ceiling — `auto_scaling_factor()` never exceeds `SCALE_MAX=1.25`, so nothing automatic ever produces `final` above `0.6×1.25=0.75`. `spectra/services/intensity_scale_marks.py` is the release valve he asked for: a per-track manual mark (clamped `[0, 2.0]`, SpotFX's own manual-slider ceiling), checked FIRST by `song_scaling_factor()` and never re-clamped into the auto range — "he marks the track; automatic never does," restoring the exact control SpotFX's own manual slider gave him. API `GET/PUT/DELETE /api/intensity-scale/mark?uri=`; UI `IntensityMarkControl.tsx` on the shared `TopBarStrip` (help topic `intensity-mark`). **The Soy Peor discrepancy, corrected**: an earlier pass reported its auto factor (0.859) against his 1.00 target as a 0.141 miss and, testing whether library growth explained it, found the drift hypothesis FALSIFIED with direct measurement — Soy Peor has a pre-calibration capture (2026-07-08, `.librosa.json.bak`) whose bass features are within noise of today's (recaptured 2026-08-14); a reconstructed 340-song library as of 2026-07-29 gives the same rank (0.218) as today's 841-song library (0.219) — the auto formula would have said ~86% on 7/29 too, not drift. That correctly falsified the drift theory, but the CONCLUSION drawn from it — "his ear calls it a full-intensity track and the metric can't perceive it" — was a units error (100% read as "maximum" instead of "no adjustment") and has been retracted. On the corrected terms this is a modest, still-real 0.141 FACTOR mismatch: Soy Peor measures at the 7th percentile for bass WITHIN its own Trap/Reggaeton bucket (bucket median rank 0.488, near the library-wide median — the genre isn't systematically suppressed, this song specifically ranks low in it), and the genre base alone (0.91) already sits below his 1.00 target before the ±10% bass nudge even applies — reaching exactly 1.00 from that base would need a bass rank near the 99th percentile of the ENTIRE library, nowhere close to where it measures. Not resolved further; not treated as a code defect (the numbers are faithful to what this exact, inherited formula would have shown on 7/29 too). Also folds in his 2026-08-15 edge-trim fix to `spectra/services/midsong_generator.py`'s per-song min-max renormalization (a cold open/fade-out no longer sets the floor; first/last 15s clamp to [0,1] instead of being floored; a track under ~30s falls back to the pre-trim behaviour, his stated edge case, resolved not guessed). Regression tool carrying the 29 July calibration forward: `scripts/check_intensity_scale_reference_songs.py` (read-only, reports all three reference songs' current auto scale + final-at-peak against his targets — run against real `storage/audio_shapes`). Tests: `tests/test_intensity_scale.py`, `tests/test_intensity_scale_marks.py`, `tests/test_midsong_generator.py`; wiring proof in `scripts/check_triggers.py`. **UI tightened 2026-08-16, his own words, confirmed twice (`fm/spectra-topbar-tighten`)**: "the icon before Mark should be gone... I want a slider, but the icon before mark should be gone" — the `🎚` emoji on `IntensityMarkControl.tsx`'s label was purely decorative (confirmed against the code before building, per his own instruction) and is now removed, leaving a plain "Mark" label. Editing gained a real `<input type="range">` alongside the existing `<input type="number">`, both bound to the same draft value so dragging and typing stay in sync — the range alone is kept precise deliberately: a drag can't reliably land one specific percent, and this factor drives how loud the room gets, so the number field stays for the exact value while the slider gets you close fast. No backend/logic change — `intensity_scale.py`/`intensity_scale_marks.py` untouched, this is presentation only. Help: `spectra/web/src/help/helpContent.ts` topic `intensity-mark` updated to describe the slider and drop the emoji reference. `tsc --noEmit` and `vite build` both clean; visually confirmed via the same static harness as §43's addendum, at 390×844.

**Room-proven 2026-08-16, live playback on `Serenity`, this PR**: hype vs. calm, side by side. Two genuine EDM/trance tracks ("Strange World - 2000 Remake", Push, and "Everytime - Nalin & Kane Remix", Lustral/Nalin & Kane — both genre-bucketed "EDM - Dance/High Energy") resolved `auto_scaling_factor = 1.25` (`SCALE_MAX`, the auto ceiling) at every sample taken, with measured (raw) intensity ranging 0.74–0.98 across several reads — `final` (`combine_measured_and_scale`) worked out to 0.55–0.73, i.e. the room ran right up against the 0.75 automatic ceiling. Fixture evidence at those moments: `executor.recent_writes` showed brightness 0.75–1.0 on every live virtual, and the SPECTRA UI's own per-pixel device-preview strip (`DevicePreviewStrip.tsx`, facade-sourced — the literal pre-freeze render frame, see the device-preview section above) showed a dense, fully-saturated colour fill edge to edge. A genuinely calm track — "Let It Be - Remastered 2009" (The Beatles, genre-bucketed "Rock") — was played on the same device for the comparison: `auto_scaling_factor` resolved to 0.485, measured intensity sampled 0.23–0.42 across the song (its own natural range, not cherry-picked), giving `final` of 0.067–0.123 — the device-preview strip for the same duration showed a near-black frame with a single faint dim sliver, a stark, unmistakably-different visual against the hype tracks' full bright fill. **The headroom-reserve gate's actual guarantee, checked arithmetically, not just observed**: even at Let It Be's own theoretical peak (measured_intensity=1.0, never actually reached in the sample), `final` caps at `1.0 × 0.6 × 0.485 = 0.291` — under half of what the hype tracks reached at an ORDINARY moment (0.55–0.73) and nowhere near the 0.75 automatic ceiling either track pushed up against. The calm track genuinely cannot reach the loud scenes even at its own peak; the gate does what it was built for. Reference songs reconfirmed live via `scripts/check_intensity_scale_reference_songs.py` against real `storage/audio_shapes` (841-song library, 2026-08-16): Dopamine 1.208 (target 1.20, diff +0.008, final@1.0=0.725), Let It Be 0.485 (target 0.50, diff −0.015, final@1.0=0.291 — the SAME number the live API read for the actual playing track above, a live/offline cross-check), Soy Peor 0.859 (target 1.00, diff −0.141, final@1.0=0.515) — Soy Peor's factor is byte-identical to the value already investigated and explained in the paragraph above; no further drift since that investigation. **Scoping note, not a defect**: his room's own standing rest state (`ambient_mode="always"`, verified unchanged before/after — see the Ambient section above) held every physical Hue bulb frozen throughout this proof, by design, orthogonal to this gate (composability documented in the Dark/Light section above) — the visual evidence above is therefore the render pipeline's own computed output (`recent_writes`, the facade-tapped device-preview frame) — confirmed to differ exactly as described between the hype and calm tracks — and NOT a claim about what any physical fixture displayed: no independent physical-fixture read-back (camera, bulb-state GET, or equivalent) was taken for any non-Hue device in this proof, so "visibly responded" is not asserted for them. Separately, and worth naming so the next crew doesn't inherit the assumption: `radial-dummy` has no IP and no physical light behind it at all (confirmed against his live config) — it is a dummy virtual, not a stand-in for a real fixture, so it could never "visibly respond" regardless of what this proof found. Playback was borrowed for ~90s via a direct Spotify Web API call (his own OAuth token, already scoped `user-modify-playback-state`) to get a controlled calm sample, then restored to the exact track/context/position/shuffle state it was in before, verified via `GET /api/spotify/status` after restore. `room-controls` state read byte-identical before and after (`dark_mode_enabled=false, brightness_multiplier=1.0, ambient_mode="always", ambient_color="#f5da8c", scene_change_mode="full", force_scene_enabled=false`). Full suite: 537 passed, 3 failed — all three pre-existing and unrelated (2× `tests/test_settings_agent_cli.py` need the optional `mcp` Python package, not installed in this worktree's venv; 1× `test_spectra_engine.py::test_mid_group_scenes_animate_on_the_harness` needs seeded live `storage/spectra/scenes.json`, gitignored — see `spotfx-environmental-test-failures` — confirmed identical on a bare checkout, not a regression from this PR, which touched no code). |
| §60 | Sonic token usage on the review page (last query / this day / this week) | `BUILT` (full), **room-unproven** | PR #92, merged (`4babd42`). Built and unit-proven, **room-unproven — no live Sonic call made** (see below for why that's not treated as a blocking gap here). His own ask, verbatim: "on the review page I want to see how much token usage Sonic has used for the last query, the last day, and the last week." Put on the Review page as asked, not relocated to the settings console. **The bar this was built to clear**: every figure is REAL REPORTED USAGE read off the model runtime's own response for that call — `settings_agent.py` (the "api"/API-credits backend) sums the Anthropic SDK's own `response.usage` across a turn's tool-call rounds; `settings_agent_cli.py` (the "cli"/subscription backend, still gated off per §55's captain's ruling) reads the `claude -p --output-format json` final `result` event's own `usage`/`modelUsage`/`total_cost_usd` — both captured in `spectra/services/sonic_usage.py`, never estimated from word/character counts. A call the runtime genuinely doesn't report usage for records nothing (no fabricated zero) — proven against a fixture with no `usage` key at all (`cli_transcript_manifest_mismatch.json`) and against a test-double response with no `.usage` attribute. **Mid-build overrule, his words, binding, superseding this doc's own first draft (which had proposed rolling 24h/7d windows)**: "choose Monday at 10:00 p.m. Eastern as the cutoff so each day runs from 10:00 p.m. to 10:00 p.m and each week runs from Monday 10:00 p.m." His stated reason: a window aligned to his subscription's own quota reset tells him how much he has LEFT, not just how much he's spent — "how much is left" is the number that matters after a quota exhausted on him without warning earlier this week (`docs/SPECTRA_SPEC.md`/`AGENTS.md`'s own live-room notes on that incident). Built as two FIXED periods, `spectra/services/sonic_usage._period_start_ms()`: day = 22:00-to-22:00, week = Monday-22:00-to-Monday-22:00, both real `America/New_York` via stdlib `zoneinfo` (DST-aware — proven against both 2026 US transition dates, never a hardcoded UTC offset) and bucketed at READ time against stored per-call `wall_ms` timestamps (never pre-assigned at write time), so correcting the anchor later would re-bucket history correctly rather than lock old entries to a stale boundary — proven by `test_moving_now_across_the_boundary_re_buckets_the_same_stored_log`. **Durable across a restart, the other half of "before it becomes a problem"**: `storage/spectra/sonic_usage.json`, atomic tmp+replace, same discipline as `fire_history.py`'s show log — proven by reloading the module fresh mid-test and confirming the identical summary reconstructs from disk alone (no in-memory state to lose). API `GET /api/sonic-usage`; UI `spectra/web/src/review/components/SonicUsageCard.tsx` (mounted above the Review card), help topic `sonic-token-usage`. Tests: `tests/test_sonic_usage.py` (16 tests — period-boundary edge cases incl. both DST crossings, real-fixture extraction matching `cli_transcript_applied.json`'s own recorded numbers exactly, the API backend's fake-SDK-client integration incl. partial-usage-survives-an-exception, restart durability); full suite 507 passed, 2 skipped (pre-existing live-model smoke tests), 2 pre-existing environmental failures unrelated (missing `mcp` package in this worktree's venv — confirmed identical on pristine `master`, same class as §58's note). **Why room-unproven isn't a gap here**: no `ANTHROPIC_API_KEY`/`CLAUDE_CODE_OAUTH_TOKEN` exists in this build environment, and the task's own hard limit was to keep any live Sonic call to the minimum the proof needed — zero were made. Instead proven through the REAL production HTTP path (`TestClient` against `spectra.app.create_app()`, no mocked routing) and, for the CLI backend specifically, against an ACTUAL historical captured transcript (not a hand-built one) — `cli_transcript_applied.json`'s own `usage`/`modelUsage`/`total_cost_usd` fields are the ground truth `test_cli_backend_records_the_exact_figures_a_real_transcript_reported` asserts against. Eye-checked live in a browser (chrome-devtools-axi, isolated `create_app()` instance mounted at `/spectra` on a spare port, throwaway `SPECTRA_STORAGE_DIR`, no lifespan — never touches the S2 bridge/LedFX/any Hue bridge) against two `sonic_usage.record()`-seeded entries (not a live model call): the card renders "Last query 19,812 tokens · SUBSCRIPTION · $0.010", "This day 31,764 tokens · since Sat, Aug 15, 10:00 PM", "This week 31,764 tokens · since Mon, Aug 10, 10:00 PM" — correct Eastern-anchored boundaries for the sandbox's own current date (2026-08-16). What his real window needs to add: a genuine live Sonic call (through whichever backend is actually configured on his instance) with the recorded entry compared to that call's own console/CLI-reported usage. |
| §61 | Ambient — burst-drop pacing, straggler REPAIR (not just detection), and the false-unlit fix | `BUILT`, room-proven | Dispatched to characterise "the fault behind everything we've chased this week": his bridges silently 2xx-accepting a write burst with nothing physically applied, §58's own retry NAMING a straggler forever without ever fixing it, and a lit-verification readout that invented failures. All three are real; only the first is NOT the sharp cliff it was first reported as — see below. **Burst threshold, measured not reasoned about**: 48 controlled, self-checked sustained-burst trials against BOTH real bridges independently (`dining-hues` 7 lights, `hue-lights` 10 lights), 0.08s–0.60s pacing near the documented Zigbee ~10 cmd/s ceiling, found ZERO drops anywhere in that range on either bridge — this does NOT reproduce the paired evidence's claimed "0.12s = 0/17 lit, 0.45s = 17/17" cliff; the most likely explanation is a target-tracking bug in whatever script produced that evidence, the SAME class of mistake this investigation's own first attempt made and only caught via a self-check gate (a single-round burst test initially "reproduced" 0/10 at every pace down to 60ms — a false readout from checking the wrong round's colour, not a real drop; retracted once caught, sustained-burst methodology re-run clean). **What IS real, independently confirmed**: the bridge gives ZERO signal on a silent drop — every PUT across every trial, including the historical 2026-08-15 incident this module's own docstring already documented (3/17 stragglers under a real burst), returned a clean `HTTP 200`/`{"errors":[]}`, no `429`, no `Retry-After`, no rate-limit header — a retry design has nothing to react to but a read-back, confirming `ambient.py`'s existing discipline (never trust a response code) is the whole answer, not one layer of several. `AMBIENT_WRITE_STAGGER_MS` raised 50ms→300ms anyway (margin inside the confirmed-safe band, not the fastest pace that survived — his own four-requirement brief's explicit ask) as defense against conditions one measurement session can't rule out, and because `spectra/services/ambient.py::_apply_hue` (the OFF-fade/catch-up paths) had NO pacing at all before this pass, a real gap regardless of where the ON path's cliff does or doesn't sit. **Straggler repair — the "detects but does not repair" fix**: `ambient_music_gate.py`'s periodic `verify_now()` recheck could name an off-target straggler correctly on every 30s tick, forever, and never once try to fix it (§58's own "Deliberately NOT built" — a design choice made to avoid fighting a bulb he turned off himself, but which also silently swallowed the ON-but-wrong-colour case it was never really about). `spectra/services/ambient.py::repair_stragglers()` reuses the exact paced/retried/read-back-confirmed engine (`_write_and_confirm`, factored out of the pre-existing `_hold_and_confirm`) the initial hold already trusted, restricted to just the named stragglers; wired into `verify_now()` so a detected miss gets one repair attempt before status reports "partial". Still NEVER re-lights a light that reads OFF right now (checked fresh, immediately before writing) — narrowed from "the verifier writes nothing" to "the verifier never writes to a light that reads off," the one case the rule was ever actually protecting. **False-unlit fix**: `_state_matches` (on+colour+brightness, confirms OUR OWN write) and `_color_matches` (on+colour only, the reporting-level check `verify_held()`/`status()` now use) are split — a bulb dimmed out of band (a wall switch, the Hue app, or his own choice) but still on and at the correct hue no longer reads "unlit." **Proven live against real hardware, not just the headless suite** (a real straggler forced on `Standing Lamp Side`: detected, genuinely repaired, independently read back confirmed fixed; a real 35%-brightness drift forced on `Loft Ceiling Uplight`: correctly reported LIT both before and after touching the module, proving the split isn't cosmetic) — room ends 17/17 at his exact `#f5da8c`, ground-truth verified directly against both bridges after every live experiment, never left mid-test. Also found and fixed live, incidentally: his room had silently drifted off-target for ~9 hours with zero write attempts in between (nothing had re-asserted it, by the pre-existing design this task's own repair mechanism now closes) — corrected via a real ambient off/on toggle during the investigation, independently confirmed. Tests: `tests/test_ambient.py` (+11: `_color_matches`/`_state_matches` split, `verify_held` brightness-tolerant, `repair_stragglers` rewrites/never-relights-off/reports-unconfirmed), `tests/test_ambient_music_gate.py` (+1 wiring proof, plus a pre-existing fixture bug fixed alongside — `hue_room` was rebuilding its mock bridge's state fresh on every `_bridge_client()` call, silently masking whether a later check reads real prior state or a coincidental default; the off-bulb proof happened to still pass by coincidence before the fix). Full suite: 503 collected, 498 passed, 5 pre-existing environmental failures unrelated to this change (`test_crystal_activation_verify.py` ×3, `test_settings_agent_cli.py` ×2 — missing `mcp` package/activation-gap fixtures, confirmed identical on pristine branch tip before this work started). **What this row does NOT establish, per its own text — see [OQ-8](#open-questions)**: the ROOT CAUSE of why bulbs ended up in a bad state in the first place (both the retracted 17-bulb measurement and the real historical 3/17-under-a-real-burst incident this module's docstring already documented) is still unexplained; what's fixed is that a bad state, whatever causes it, is now DETECTED honestly and actually REPAIRED, not that the cause is understood. Do not read "BUILT, room-proven" as "the mystery is solved." |
| §62 | STAR/Singles/power — accent param (spark colour) forced black, not LedFX's white default | `BUILT` (full), **proven live 2026-08-16** | A device's "accent" param (`sparks_color` on `power`, etc.) is force-written black by `scene_compiler._entry_config` on every compile unless the scene entry itself authored a value (`fx/device_model.accent_param_for`, ported from spot-effects' own `services/trigger_engine.py` accent-defaults-to-black rule) — see `AGENTS.md`'s SPECTRA app section for the mechanism. PR #84 (2026-08-15) closed the un-ported gap this repo's own `services/scene_v2_compiler.py` docstring had already flagged (accent not yet carried by the fixed colour vocabulary): SPECTRA's descendant compiler had silently inherited that same gap, with nothing downstream to catch it — STAR/Singles/power was rendering LedFX's white spark default instead of his authored black. PR #84 itself shipped **room-unproven** ("STAR was not the active scene during my investigation window... this fix is unproven in his room"). **PR #90 (2026-08-16) is the live-room proof that closes it**: his own corrected report named "radial," not "radio" — the `Strips` category's `radial-dummy` virtual, whose effect is promoted `melt`→`power` by `effect_steps` at intensity ≥0.7 (the original regression check only exercised intensity 0.5, below that threshold, so it never touched the reported path — the actual bug in the check, not the fix). Live-fired STAR at intensity 0.8, `dry_run=False`, against his real room: the `fx_seam` write for `radial-dummy` carried `sparks_color=#000000`, confirmed by reading the write back, not inferred from the fire call succeeding. See §56 for the sibling STAR fix from the same investigation. **PREMISE CORRECTION, code-verified 2026-08-16 (PR #102)**: PR #90's live proof targeted the wrong fixture. `radial-dummy` is a `type: "dummy"` device with no IP and no physical light behind it (confirmed against his live `storage/spectra/fx-live/config.json`) — he cannot have seen white sparks there; a proof at that virtual can never be the proof he asked for. The `Strips` category (`storage/device_categories.json`) has exactly two virtuals, read fresh rather than assumed: `radial-dummy` and `tv-mapper`. The task brief that opened this pass had *also* guessed wrong, naming `tv-backlight`, `crystal`, `porch-rail`, and the kitchen sconces as separate category members — `crystal` and `porch-rail` are NOT in `Strips` (`crystal` is `Matrix`/`Particles`, reached via `crystal-mapper`; `porch-rail` belongs to no category at all). The one real member is `tv-mapper` — a single virtual whose `segments` (read from live `fx-live/config.json`) span three genuine WLED devices: `tv-backlight` (pixels 0–559), `sconce-kitchen-right` (two segments), `sconce-kitchen-left` (two segments) — those are the fixtures he was actually looking at. **New evidence, no synthetic intensity this time**: his own `storage/spectra/show_log.json` already held 30 genuine automatic STAR fires on 2026-08-16 that crossed the `effect_steps` promotion threshold (intensity ≥0.7) during his real, unprompted listening — most recently 20:28:03Z and 20:28:14Z (intensities 0.7325, 0.75), ~24 minutes before this check ran. Side-effect-free replay (`POST /api/scenes/{id}/fire`, `dry_run=true` — the compiler's own docstring guarantees dry and live share identical resolution up to the seam for this deterministic, non-dice entry) at those exact real intensities shows `tv-mapper` compiling identically to `radial-dummy`: `effect_type=power`, `sparks_color=#000000`. At a real observed sub-threshold intensity (0.6435, also lifted from the same show log), `tv-mapper` stays on `melt` — confirming the check must genuinely cross 0.7 to exercise the reported path, exactly as PR #90's own note warned. **The honest limit, unchanged and worth restating**: no independent physical-fixture read-back exists for WLED devices (no camera, no bulb-state GET, no equivalent) — this proves what was WRITTEN to `tv-mapper`, not what his eyes saw at the strip or the sconces; that confirmation is his alone. Room untouched throughout: only GETs and side-effect-free dry-run fires were used — `room-controls`, the active scene (`Orbits V2`, never disturbed), and the ambient hold (17/17 lit) all read byte-identical before and after. |
| §63 | Ambient — second colour for dark mode (day-one composition fix) | `BUILT` (full) — **room-proven 2026-08-16**, PR #91 merged (`aa7ecde`) | Resolves `data/backlog.md` `spectra-dark-light-vs-ambient-ruling` (filed 2026-08-15, immediately after §9 landed): dark/light acts on his STREAMED devices only (`crystal-mapper`/`radial-dummy`/`tv-mapper`, via `dark_lock`), while `ambient_mode="always"` holds every Hue device unconditionally over direct bridge REST, bypassing LedFX entirely — so switching dark mode on left his main Hue lights bright, which reads as broken for something he called GLOBAL dark/light even though both features were individually doing exactly what he'd asked. Three readings were put to him (dark overrides the hold / ambient-always wins outright / the two compose); **his ruling, verbatim**: "dark mode during ambient should choose a different color that I pick so we have one color for regular ambient light mode or hybrid mode and then one color for dark mode for now make them the same but let me be able to pick it with the color picker." **Dark never bypasses Ambient and Ambient never blocks dark — dark mode changes WHAT Ambient holds.** Built: `RoomControlState.ambient_color_dark` (`spectra/services/room_controls.py`), the same shipped LedFX colour picker (§46) as `ambient_color`, no new control — deliberately `Optional[str] = None` rather than a value copied at a migration instant, so the two stay identical **by construction** ("for now make them the same") until he diverges them, with no one-time snapshot to drift. `room_controls.effective_ambient_color(state)` is the one resolver every ambient write/verify/change-detection path now goes through instead of reading `ambient_color` directly — `ambient_color_dark` wins only while `dark_mode_enabled` is True AND it's been authored; otherwise unchanged from before this field existed. Three call sites route through it: `ambient_music_gate.reconcile()`'s write, `verify_now()`'s independent read-back (§58's status-honesty verifier — routing through the resolver matters here specifically, or a genuinely-correct dark hold would misreport as `partial` forever against the wrong target colour), and `reconcile_ambient_if_changed()`'s change-detection gate (fires on a dark-colour edit, on `dark_mode_enabled` itself toggling while the two colours differ, and correctly not on an edit to a colour that isn't currently in effect). Transition — decided, not asked, his brief left it open: a colour change while Ambient already holds re-invokes the SAME `_apply()` "colour changed while holding" branch a human editing the picker by hand already exercises, riding Hue's own bridge-side 1.5s `dynamics.duration` ramp (`AMBIENT_TRANSITION_MS`) — deliberately NOT the ~16s two-phase release/catch-up ramp, since that mechanism hands a frozen bulb back to a *live rendering effect* it needs to catch up to, and this is a colour swap within an already-held room where both endpoints are static. No new transition code at all: `effective_ambient_color()` resolves to a different value and the pre-existing re-apply branch does the rest — which is also why toggling before he's picked a distinct dark colour is a correct no-op. Also composes with §5/§6 (`spectra/services/ambient.py`'s own docstring: **Ambient covers Hue only, by design, matching legacy's scope exactly** — "the Entertainment-API brightness cap this sidesteps is a Hue-only problem," non-Hue devices keep running their normal show; his own §53 language, "my Hue Lights are lit and bright but the other lights are still running the show," confirms this is his own mental model too, not an implementation accident; also see `data/decision-ambient-hue-only.md`, his own words, 2026-08-15: "I only want ambient to manage Hue Lights for now so that is correct" — a decided design choice, not a gap). Tests: `tests/test_room_controls.py` (7 new — resolver defer/override/revert, hex validation, an old-file-without-the-field load with no migration code needed, `reconcile_ambient_if_changed` firing/not-firing correctly) + `tests/test_ambient_music_gate.py` (4 new, end to end against the mocked Hue bridge — holds the dark colour when dark mode is on, a toggle re-applies live at the resolved colour with the eased glide, a toggle is a no-op when the colours match, `verify_now()` confirms against the dark colour rather than misreporting `partial`). Deliberately NOT extended: Sonic's settings-console `SETTINGS_REGISTRY` (§45) — a new `RoomControlState` field is invisible to the agent until added there on purpose, and this wasn't asked for. **A real live collision, found and fixed at rebase time, before room-proving**: PR #93 (§61) landed independently while this branch was queued and added `ambient_music_gate.verify_now()`'s straggler-repair write (`ambient.repair_stragglers()`), calling it with the bare `ambient_color` rather than the resolved colour — exactly the composition bug this row exists to prevent (a straggler repaired while a distinct dark colour is active would have been rewritten back to the *normal* colour, re-drifting it the very next tick). Fixed during the rebase: `verify_now()` now resolves `target_color = effective_ambient_color(controls)` once and reuses it for both the `verify_held()` read and the `repair_stragglers()` write; `tests/test_ambient_music_gate.py::test_verify_now_repairs_a_straggler_to_the_dark_colour_not_the_normal_one` proven to fail against the pre-fix call shape before the fix was restored. **Glide extended to 3000ms, 2026-08-16 — HIS STATED PREFERENCE, NOT AN OBSERVED RESULT, and that distinction matters here specifically**: `AMBIENT_TRANSITION_MS` (this row's own `dynamics.duration` ramp, above) raised from legacy's 1500ms per his ask — "The transition appears to be about 1.5 seconds like you said. go ahead and extend it to 3 seconds though." That sentence was given BEFORE he had actually watched a live toggle; his own words immediately after, correcting the earlier brief that had asked this row to record an eyewitness result: "Sorry I thought that you had already started running these transitions let me test it directly like you said." His agreement was courtesy about our own description of the mechanism, not an observation of it running — **THE EASED TRANSITION REMAINS UNPROVEN**, same honest-gap posture this row's own "Transition — eased, not snapped" finding above already models for the OFF-path CLIP-polling limit, and the 3000ms value must not be read as settled until he has actually watched it. **Investigated rather than assumed, per the brief's own instruction to verify a claimed defect before fixing it**: a claimed retry/read-back collision (`_write_and_confirm`'s bounded retries reading a Hue bulb back while it's still mid-glide, manufacturing false stragglers as the ramp lengthens) does NOT exist as described — retries already write via `snap_body`, which drops `dynamics` entirely ("a stubborn light should snap, not take another [ramp] to maybe land"), so a retry write is never a glide and its settle wait was already independent of `AMBIENT_TRANSITION_MS`. The REAL timing defect this pass found and fixed instead: `repair_stragglers()` calls `_write_and_confirm` with a body carrying NO ramp at all (`_light_payload(color_hex)`, no `ramp_ms` — a stubborn light should snap on ITS first write too, not just its retries), yet `settle_ms` keyed off `attempt == 0` rather than off whether that attempt's write actually had a ramp — so repair's first attempt was needlessly waiting out a full `AMBIENT_TRANSITION_MS` before reading back an instant write, a wasted wait that would have silently doubled with this change. Fixed by keying `settle_ms` off `"dynamics" in write_body` instead of the attempt index: the ON-hold's real glide (attempt 0, ramped) is unaffected and still self-scales; repair's un-ramped write now settles in `AMBIENT_CONFIRM_SETTLE_MS` alone, at any `AMBIENT_TRANSITION_MS`. `verify_now()` cannot race a live glide either — it skips its tick entirely while `_apply_lock` is held (module docstring), and a hold's own write-then-confirm sequence holds that lock through its full settle, ramp included; a scene/trigger fire arriving mid-transition is genuinely fine too, since Ambient freezes each Hue device's own stream while held (`set_frozen(True)`) — a scene fire's writes to a frozen device are dropped at the render layer regardless of glide length, and non-Hue devices were never touched by this constant. The OFF path (`AMBIENT_OFF_FADE_PCT`/`AMBIENT_CATCHUP_MS`) re-checked and unaffected in shape: phase 1's dim fade still self-scales its own wait to `AMBIENT_TRANSITION_MS` (no read-back on that path, so no misread risk), phase 2's catch-up ramp is untouched (its own separate `AMBIENT_CATCHUP_MS`) — his praised fade-and-release shape is preserved, just phase 1 now takes 3s instead of 1.5s, matching the same constant governing the ON-side hold. `spectra/services/release_fade.py`'s own `RELEASE_FADE_MS` — the SEPARATE panic-release fade behind `services/release.py`'s red button, not this feature — is deliberately left at 1500ms, out of scope (a one-shot power-off he isn't judging by eye the same way); its comment claiming numeric parity with `AMBIENT_TRANSITION_MS` is corrected to note the divergence rather than left stale. Also fixed alongside: `scripts/check_spectra.py`'s ambient section restored `AMBIENT_TRANSITION_MS` to a hardcoded `1500` in its `finally` block after mutating it for the spec run — the one constant of five NOT captured via `_orig_*` the way its siblings are — now captured/restored the same way, since the hardcode would otherwise have silently left the module at the wrong default for the rest of that process's life. New test: `tests/test_ambient.py::test_repair_stragglers_settle_does_not_wait_for_a_ramp_it_never_sent` proves the fix directly (asserts the settle sleep is `AMBIENT_CONFIRM_SETTLE_MS` alone, not `AMBIENT_TRANSITION_MS+AMBIENT_CONFIRM_SETTLE_MS`, against a genuine repaired straggler). Full suite: 541 collected, 536 passed, 2 skipped (pre-existing live-model smoke tests, no API key in this environment), 3 pre-existing environmental failures unrelated to this change (`test_settings_agent_cli.py` ×2 — missing `mcp` package in this worktree's venv; `test_spectra_engine.py::test_mid_group_scenes_animate_on_the_harness` — needs seeded live storage this disposable worktree doesn't carry — all three confirmed identical/absent on pristine `master`, same class as §60/§61/§64's own notes); `scripts/check_spectra.py`'s Ambient section: all checks pass. **One question answered, not built, per his own brief**: exposing this glide through the settings console alongside `global_transition_ms` would be cheap — one more `SETTINGS_REGISTRY` entry reading `AMBIENT_TRANSITION_MS`'s own bounds, the same shape the other four agent-tellable room-control fields already use — but he hasn't asked for it, so it stays a plain constant. **DEPLOY HELD, by design**: this change must not go live before he has actually watched the CURRENTLY-DEPLOYED 1.5s glide and reported what he sees — deploying 3000ms under him now would mean he judges 3 seconds while believing he is judging 1.5, poisoning the one measurement that can settle this.

**Room-proven 2026-08-16**, his room free but HE WAS AWAY — every check below is instrumented (bridge CLIP v2 read-backs, server logs, the deployed commit's own source), never eyeballed; **he has not personally seen this yet**. Read his live state first (`GET /spectra/api/room-controls`): `ambient_mode="always"`, `ambient_color="#f5da8c"`, `ambient_color_dark=null`, `dark_mode_enabled=false`, `scene_change_mode="full"`, 17/17 lit — matched exactly what firstmate reported pre-session. Deployed commit confirmed genuinely running first (`aa7ecde`, `grep`-verified against the live checkout, not assumed from the deploy message alone) before anything was touched. Four points proven, each read directly at both bridges via `scripts/verify_ambient_dark_colour_fixtures.py` (new, this PR — read-only for its own GETs; reuses `ambient._resolve_lights_named`/`_color_matches` directly so it proves against the exact set and tolerance Ambient itself uses, not a re-derived guess), with `dark_mode_enabled`/`ambient_color_dark` driven via real `PUT /spectra/api/room-controls` calls:
  1. **`ambient_color_dark=null` defers to the normal colour** ("for now make them the same," his own words) — baseline read 17/17 on at `#f5da8c`'s exact xy, both bridges.
  2. **Authoring a dark colour alone, with `dark_mode_enabled` still false, is a live no-op** — `PUT ambient_color_dark="#1a2a6c"` came back with no `ambient_result` key at all (the `reconcile_ambient_if_changed` gate correctly found nothing to do); re-read confirmed 17/17 unchanged at `#f5da8c`.
  3. **Toggling `dark_mode_enabled=true` swaps the hold to the dark colour** — `PUT` returned `ambient_result: {"status":"on","lights_set":17,"lights_total":17}` (one straggler needed the existing retry pass, log-confirmed: `"1 light(s) not yet confirmed... retrying: ['Under Spiral']"` then `"held at #1a2a6c, 17 light(s) confirmed"` — the retry mechanism working as designed, not a drop); direct bridge read-back confirmed 17/17 at the dark colour's exact xy on both bridges.
  4. **Toggling back returns to the normal colour** — `PUT dark_mode_enabled=false` logged `"held at #f5da8c, 17 light(s) confirmed"`; 17/17 confirmed at the bridges.

  **Transition — eased, not snapped, proven at two levels, honestly bounded at a third**: (a) code-level, confirmed against the ACTUAL deployed source (not assumed from the PR diff): the write path unconditionally sends `dynamics: {"duration": 1500}` on the Hue PUT for every hold engage/re-apply (`ambient.py`'s `AMBIENT_TRANSITION_MS=1500`, `_light_payload`) — the identical mechanism his own already-praised engage-hold and ease-back already use, not a new one; (b) structurally, server logs show the swap as a single continuous hold — `"Ambient ON: ...held at #1a2a6c"` then later `"Ambient ON: ...held at #f5da8c"` with no intervening `"Ambient OFF: ...released"` between them, ruling out a hard release-then-reacquire snap; (c) **honestly could not be visually confirmed**: rapid-polling (150ms) the CLIP v2 light resource during the OFF transition showed a strictly binary jump (exactly the dark xy for ~5.2s, then exactly the normal xy from the next sample on) with no intermediate value ever observed — the bridge's REST resource reports commanded/target state, not a live-animating frame, so polling cannot see the physical fade a person standing in the room would; only an eyewitness can close that gap, and none was available this session.
  5. **PR #93 collision fix re-confirmed live, not just at rest**: with the dark colour held, one bulb ("Standing Lamp Side") was deliberately drifted out-of-band (direct bridge PUT, bypassing SPECTRA, to an xy matching neither colour) to force a genuine straggler. The periodic verifier caught and repaired it within one tick — log-confirmed (`"Ambient repair: re-asserted the ambient colour on 1 straggler(s): ['Standing Lamp Side']"`) — and the bridge read-back confirmed it landed on the DARK colour's exact xy, never the normal one. Full 17/17 re-confirmed DARK afterward.

  **His room restored exactly as found** and independently re-verified three ways after: `GET /room-controls` matches the pre-session state field-for-field, `GET /engine/status`'s `ambient` block reads `mode: "holding", held: true, 17/17 lit, unlit: []`, and the direct bridge read-back shows 17/17 at `#f5da8c`'s exact xy on both bridges. |
| §64 | Audit: every SPECTRA site that treats a Hue bridge 2xx as proof of application | **AUDITED**, one gap **found and fixed**, code-proven; **room-unproven — he was away** | Dispatched (spectra-audit-2xx-proof, 2026-08-16) on D6's own premise (a 2xx proves acceptance, never application) to check that discipline is actually applied everywhere, not just at ambient.py where it was found and fixed first. **Retraction noted, not re-derived**: D6's own burst-rate-cliff claim was already retracted before this audit started (48 self-checked trials, zero drops at any pace) — this row does not reopen that question or imply a drop rate; the only established fact carried forward is "a 2xx proves nothing," independent of pacing. **Sites inspected, found already correct/exempt (no change)**: `fx/devices/hue.py`'s `flush()` — the actual per-pixel colour stream is raw UDP at ~30fps, re-asserted every frame, so a single dropped frame self-heals on the next one — the task brief's own stated exemption criterion; its `action:"start"`/`action:"stop"` session PUTs are session-lifecycle calls proven downstream by whether frames actually flow (DTLS handshake success, `live_host.py`'s frame-freshness checks), not a bulb-colour claim. `spectra/services/fx_seam.py` (`apply_writes`/`get_virtuals`/`set_virtual_config`, the S1/S3 owner-Fire scene-fire seam) and `spectra/services/fx_executor.py` (the S2 engine's glide/jump seam) — both write to the LOCAL render engine (in-process facade or a same-host LedFX HTTP call), not the flaky Hue-bridge/zigbee hop itself; both raise loudly on failure (no swallowed exception, no confident report over a failure) and, once landed, the actual bulb colour is driven continuously by the same re-asserted-every-frame mechanism as `flush()` above — correctly exempt, not merely convenient. `spectra/services/ambient.py` — the gold standard this audit measures everything else against (`_hold_and_confirm`/`repair_stragglers`/`verify_held`, §51/§58/§61); one stale claim in its own docstring (asserting the release path "already reads real state back," written before checking) is corrected as part of this row, see below. `spectra/services/ambient_music_gate.py` — pure orchestration over `ambient.py`'s already-confirmed primitives, correctly never re-derives its own weaker notion of "held" (`_apply`/`verify_now` read). `spectra/services/ledfx_release.py` + `release.py`'s `_verify_released()` — the external-LedFX-virtual half of release cleanup: the PUT's own "success" body is NOT trusted alone, a genuinely independent GET re-read of `/api/virtuals` after cleanup is what `_verify_released()` actually checks — correctly built, unlike the Hue half (below). `spectra/services/handover.py` — never writes an individual Hue light; its own verification (`live_host.py`'s `activation_gaps()`/`device_gaps()`) is about virtual/stream activation, which is what its own claims are actually about — not a bulb-colour claim, so nothing here needed a bulb-level read-back. `fx/devices/wled.py`'s `WLED.release_realtime()` — the WLED equivalent of the Hue off-write, POSTs `{"live": false}` and trusts the response; deliberately NOT extended by this audit — WLED is wifi/JSON, not the zigbee-mesh-behind-a-bridge path D6's established fact is specifically about, and its self-reverting on-device-show behaviour was already independently live-verified 2026-08-14 (see the S3 section of `AGENTS.md`). **The gap found and fixed**: `spectra/services/release_fade.py`'s final Hue OFF write (the one write in the whole release panic-handle that determines whether Home Assistant inherits a dark room or a bulb still lit at SPECTRA's last streamed colour) only ever checked `raise_for_status()` — a 2xx — exactly the pattern D6 says proves nothing. `release.py`'s own `_verify_released()`, despite its docstring's claim, never independently checked individual Hue bulb state either — only process/ownership-level state (`live.active`, external LedFX virtuals). Fixed: `release_fade.fade_and_release_hue()` now reads each light back after the off write (`RELEASE_OFF_SETTLE_MS` wait, GET, one paced retry if still on), returns any still-on light's bridge name in a new `still_on` field instead of folding it into a bare "faded" claim; `release_room()` folds a non-empty `still_on` into `ReleaseResult.problems` and forces `verified=False` — the same "released-unverified" (HTTP 207) surface the LedFX-virtuals gap already used, so a caller sees the specific bulb named rather than a clean "released". `ambient.py`'s own docstring corrected to stop citing the (never true) claim that the release path already had this. Tests: `tests/test_release_fade.py` (+4 — happy-path read-back, a bridge built to 2xx-accept-but-not-apply the off write [`stuck_bridge`, the literal D6 shape] proving the retry fires and the light is named in `still_on`, the settle/retry pacing itself, the no-Hue-devices shape unaffected) + `tests/test_release.py` (+1 — `release_room()` reports `verified=False` with the light named in `problems` when the fade's read-back finds a straggler); full suite 535 collected, 531 passed, 2 pre-existing environmental failures unrelated (`test_settings_agent_cli.py` — missing `mcp` package in this worktree's venv, confirmed identical on pristine `master` before this work started, same class as §60/§61's note), 2 pre-existing skipped (live-model smoke tests, no API key in this environment). **What this row does NOT establish**: the fix is code-proven only — `scripts/verify_release_fixtures.py` (pre-existing, read-only, GET-only) is the live-fixture instrument that would prove it against his real bridges after an actual release press, but HE WAS AWAY for this pass and a release press is exactly the kind of action this task's own hard limits say to leave for a room window, not manufacture one to test. **A related but separate finding, written up, not fixed — see [OQ-9](#open-questions)**: the legacy (pre-SPECTRA, still-live) `services/ambient_mode.py::_apply_hue` carries the SAME 2xx-only pattern this row fixed in `release_fade.py` (`if resp.status_code < 400: count += 1`, no read-back at all) — and its own `_bridge_client` writes go direct to the Hue bridge over REST, bypassing `api.ledfx_client`'s S3 ownership gate entirely, the same shape `spectra/services/ambient.py`'s own bridge client uses by design. Not fixed here: it is the "old world" D4 already holds retirement-gated rather than scheduled, and fixing the ownership-bypass half is a materially different, larger problem (a second writer that isn't gated by the ownership record at all) than the read-back gap this row otherwise closes. |
| §65 | Curve selector behaviour — collapsed-on-open, scrollable, editing is structurally a one-off | `BUILT` (full), UI eye-checked at real phone width, not room-proven | His ask, verbatim: "I like the previews and how they look. however I don't want them to be expanded when I open the window I want them to expand when I click to select a curve... it should be scrollable... if I edit it it should immediately change it to a one-off, not default to saving the curve. There should also be a button for me to save it as a named curve and I should be able to overwrite with a warning dialogue." Closes the exact hole that bit him earlier this week: dragging a shared curve used to be one click ("Save curve") away from silently retuning every other scene using it. `spectra/web/src/scenes/tabs/SequencingTab.tsx` rebuilt around this: the tile grid (unchanged `CurveThumbnail` previews, still §54's — not redesigned) now sits in a `maxHeight:260/overflow-y:auto` scroll container and stays COLLAPSED (no editor) on open; clicking a named-profile or "Inline one-off…" tile both selects/attaches it (unchanged prior behaviour) AND opens a modal "curve window" (`position:fixed` overlay, `maxWidth:92vw`/`maxHeight:86vh`/`overflow-y:auto`, the same phone-safe shape `ImportDialog.tsx`/`SpectraTriggerDialog.tsx` already use) holding the full, still-unredesigned `CurveEditor`. Flat/not-sequenced select without opening a window (neither has a shape of its own to edit — unchanged from the prior gating). **THE safety property, structural not conventional**: `CurveEditor`'s `onChange` writes ONLY to local React state (`draft`) — no code path in the file reaches `saveCurvesMut`/`curves[existingId]` from a drag. Two explicit actions exist and only two: "Apply edited one-off to this scene" / "Detach — edit just this scene" calls `attachMut` with `kind:"inline"` targeting THIS scene's own `entries[sceneId].inline_points` — it has no argument and no branch that can address the shared `curves` store, so it is incapable of retuning another scene, not merely unlikely to. "Save as named curve…" is the ONLY function in the file that can write `curves[id]`; its own `SaveCurveDialog` checks the typed name against every existing profile name and, on a match, switches to a second stage naming the curve, how many OTHER scenes use it, and requires an explicit "Overwrite" click — Cancel/Back returns to editing with the stored curve untouched (verified live: read `storage/spectra/sequencer.json`'s `curve_profiles` before and after reaching the warning stage — byte-identical until Overwrite is actually clicked, changed correctly after). A scene switch (`useEffect` on `scene.id`) resets the open window/draft/dialog so an in-progress edit can never leak onto a different scene\'s save. **Groups/tabs — deliberately NOT built** (his own words, "in the future", "perhaps different tabs"): the tile list stays a flat array keyed by curve id, generated by one `Object.values(curves).map(...)` — grouping or tabs later is a `groupBy` over that same list feeding separate tile arrays, not a new data model or a rewrite of the grid/window split. **Phone-first verification, the actual bar this task set (the device-preview strip's own regression was the cautionary tale)**: live-eye-checked on an isolated `python -m spectra` instance on a spare port (`SPECTRA_PORT=18010`, disposable fixture `SPECTRA_STORAGE_DIR` seeded with 12 distinct curve profiles across 2 scenes — never his live `storage/spectra/`, never port 8010) with a bogus `SPOTFX_PORT` so the bridge never reached his real spot-effects process, driven via chrome-devtools-axi at a REAL 390×844 phone viewport (`resize`, not a narrowed desktop window). Confirmed: the collapsed grid scrolls (12 tiles, scrollbar engaged) with no window open; clicking a profile tile opens the window with the curve visible and no horizontal overflow introduced by any of this change (`document.documentElement.scrollWidth` vs `clientWidth` measured before/after — the only overflow present on the page is pre-existing and unrelated, from `device-preview-strip`/the tab bar, confirmed by listing every off-screen element and finding none belong to this file); dragging a point live (synthetic `PointerEvent`s at real screen coordinates) flips the status line to "Editing — one-off, not yet applied" instantly; "Save as named curve…" pre-filled with the open profile's name and submitted unchanged correctly surfaced the overwrite warning naming the exact other scene using it, and the on-disk curve was provably unchanged until "Overwrite" was clicked, then correctly updated; "Apply edited one-off" was independently confirmed to rewrite only the acting scene's `inline_points` while the OTHER scene's `curve_ref` and the shared profile's own points were untouched, read directly from the fixture's `sequencer.json`. **Not room-proven** — this is UI-only, touches no device write path, and his live curve library was never opened; this row is his eye's to confirm next. Help: `spectra/web/src/help/helpContent.ts` topic `tab-sequencing` rewritten to match (collapsed-grid/scroll/window/one-off/overwrite-warning language, replacing the old always-expanded/Promote-button description). **Superseded 2026-08-19, card reopened (PR fm/spectra-curve-selector-rework)**: he re-clarified both halves of the SAME original ask, ruling out this row's own "collapsed grid sits in the window by default" reading and its "status flips immediately, the underlying write still waits for a click" reading — his words: "I do not want them to be expanded when I open the window... [not] a collapsed accordion he has to expand... there is a button, and pressing it pulls the selector up" (the trigger is a live `CurveThumbnail` preview button, reusing the existing thumbnail rather than a second rendering that could disagree with it — his ask, "the button should show the current preview"); and "the status change IS the apply... no apply button, no pending state" — an edit (drag/add/remove point) now writes straight to the entry's own one-off `inline_points` on the very first touch, coalesced (one request in flight, same shape `ColorSetsPage`'s live-drag preview already uses), with NO intermediate "Editing — one-off, not yet applied" text and NO separate "Apply" click. The safety property is unchanged in kind, only in timing: the auto-commit path is the exact same `attachMut(kind:"inline")` call `applyOneOff` always used, still incapable of reaching `curves[id]` — verified live (not just code-read) against an isolated `python -m spectra` + a stub `/api/color-sets` server on spare ports: editing an attached shared profile from BOTH the Sequencing tab and a colour Group's Likelihood section immediately flipped that entry's `curve_ref`→`null`/`inline_points`→the edited points while `GET /sequencer/curves` proved the shared profile's own stored points never changed; a new "↺ Revert to original" restores exactly what was attached before the edit session (including an edited already-inline curve's own pre-edit points, not just "back to inline"), also proven live. Carried to every surface the component appears on (Sequencing tab, a colour Set's card, a colour Group's card — all three eye-checked at a real 390×844 viewport). Help: `tab-sequencing` and `colorsets-likelihood-curves` both rewritten to match. |
| §66 | Fix: Ambient discarded the brightness of the colour he picked | **FIXED**, code-proven (offline, mocked bridge); **room-unproven — pending the room window, he was at his lights** | Reported live 2026-08-16 (spectra-ambient-brightness-lost): he set his dark-mode Ambient colour to a darker cream (`#8b7e53`) and the lights did not dim — "putting it in dark mode did not dim the lights so I'm wondering if that's working at all?" **Root cause**: `spectra/services/ambient.py`'s `_light_payload` built brightness from a hard-coded `AMBIENT_BRIGHTNESS_PCT = 100` constant, never from the hex colour; the bridge's xy chromaticity (`_hex_to_xy`) also discards luminance, so a darker shade of the same hue survives only as a ~0.01 xy shift — invisible on a real bulb. The read-back couldn't catch it either: `_hold_and_confirm`/`_state_matches` confirmed against the SAME hard-coded constant that had just been written, so it could never disagree with itself — the identical shape D6/§64 already named for a 2xx write, one layer up (a confirmation that can't fail is not a confirmation). **Why §52/§53's original room proof missed this**: that proof (and every Ambient proof since) exercised colour changes only as large HUE swaps — his cream to a deep blue — never two shades of the SAME hue. A hue swap moves brightness for free under a broken always-100 constant (nothing to hide the bug from, since the constant never varied to begin with) — the constant's own invariance was indistinguishable from "brightness isn't the thing being tested." A same-hue-different-lightness case is the one pairing that isolates brightness from hue and was never run. **Legacy checked first, per standing rule — and it does NOT settle this**: `services/ambient_mode.py::_light_payload` never derived brightness from colour either; `settings.ambient_brightness` (config.py, default 100) was always a wholly separate, independently-authored slider with its own UI control (`SettingsPage.tsx`'s "Brightness (%)" range input), completely orthogonal to `ambient_color`'s hex — legacy has no derivation measure to defer to, because it never derived at all. Recorded rather than discarded per the Admiral's own instruction: a standalone independent ambient-brightness slider is a real legacy control SPECTRA's ambient surface dropped entirely on the port, separate from the derivation question this row answers, and is very plausibly a control he has not yet noticed is missing — flagged for a later, separate pass, not built here. **His ruling, which does settle it** (2026-08-16, same day, his own words): "I want the brightness of the color that I choose for both ambient modes to be applied to the lights" — derive from BOTH `ambient_color` and `ambient_color_dark`, not one fixed constant, and not a new independent slider. **Which measure**: HSV Value (max RGB channel) over relative luminance or CIE L* — his own arithmetic, verified rather than adopted blind: on his two colours, HSV Value gives 96.1%/54.5%, relative luminance 71.4%/21.0%, CIE L* 87.7%/53.0%. Relative luminance was rejected because `_hex_to_xy` already hands the bridge a hue's full chromaticity, so a brightness measure that ALSO discounts for a hue's intrinsic dimness double-counts it — a saturated `#0000ff` computes to ~7% under luminance, so authoring a vivid blue would render an almost-off bulb, the exact "fights the picker" failure this fix exists to prevent. HSV Value keeps a fully-saturated colour and pure white at 100% (a colour picked at full intensity reads as authored at full intensity) and drops only as a colour is genuinely lightened/darkened — his everyday cream lands at 96% (an imperceptible ~4% dimmer than the old always-100 constant) rather than luminance's ~71% (a ~29% drop to his resting ambient he never asked for and would notice). **Fix**: `spectra/services/ambient.py` gained `_hsv_value_pct(hex)` (same `max(r,g,b)/255` formula the pre-existing release catch-up ramp's `_live_look` already used for its own brightness proxy off the live frame — one measure, not two independent reimplementations); `_light_payload`'s `brightness_pct` now defaults to `_hsv_value_pct(color_hex)` instead of the removed constant; `reconcile`'s ON path and `repair_stragglers` both derive and pass the SAME value into `_hold_and_confirm`/`_write_and_confirm`, so `_state_matches`'s read-back now confirms against the actually-intended value, not a constant that agreed with itself by construction. `AMBIENT_BRIGHTNESS_PCT` removed entirely — no RoomControlState field was added; nothing to migrate, no new UI control (both colour pickers already exist). `services/ambient_mode.py` (legacy) and the OFF/fade path (`AMBIENT_OFF_FADE_PCT`/`_fade_dim_payload`, legacy's `ambient_fade_brightness`) are untouched — the fade carries no colour target, so there is nothing for the derivation to apply to. **Tests** (`tests/test_ambient.py`): `_hsv_value_pct` unit coverage (his two colours' exact values, a saturated colour staying at 100% rather than luminance's ~7%, invalid-hex fallback), `_light_payload`'s default-derives / explicit-override behaviour, the OFF-fade path proven unaffected by the derivation, and the regression the original proof was missing — `test_reconcile_on_same_hue_different_lightness_confirms_different_brightness`: his cream and darker cream held in sequence through the real `reconcile()` → `_hold_and_confirm` → `_write_and_confirm` path against a mocked bridge, then an INDEPENDENT read-back (bypassing `_color_matches`, which deliberately ignores brightness) confirming the physical (mocked) bulb landed and was CONFIRMED at 55%, not the old 100 — a regression back to the hard-coded constant fails this test via the confirm-retry loop, not merely a stale assertion. Full suite: 544 passed, 2 pre-existing environmental failures (`test_settings_agent_cli.py`, missing `mcp` package, confirmed identical on pristine `master` before this work), 2 pre-existing skipped. **What this row does NOT establish**: brightness read back at HIS real bulbs, two different confirmed values for two colours of the same hue — the live-instrument equivalent of the offline regression test above — is deliberately not run here; he was standing at his lights mid-fix, this task's own hard limits reserve any visible live change for firstmate's room window, and the Admiral asked to be told before anything visibly changes so he can take it himself. |
| §67 | Trigger-level scene pools — a weighted subset override on the sequencer's free choice | `BUILT` (data model + kernel + engine wiring), **no UI — this was a data-and-test task by his own instruction; §67's Part 2 is a proposal awaiting his word, see OQ-11** | His own ask, verbatim: "hand them to the room sequencer, but triggers should be able to carry some meta data that can say choose from only these scenes and includes weights. Suggest best methods for displaying and containing that data. Develop a test using one of the songs with the triggers you mentioned." Read his real data before designing, per standing instruction, every number reverified fresh (`scripts/check_trigger_scene_pools.py` Part A, not assumed from the brief that named them): `storage/spectra/triggers.json` has 832 songs, 20,958 `fire_scene` / 7,163 `fire_response` / 236 `fire_scene_update` triggers, and **every one of the 20,958 `fire_scene` triggers has `scene_id: null`** — the migration already hands the choice to the sequencer for all of them, and **his hand-built scene pools are not in this file and cannot be reconstructed from it.** Legacy `storage/events.json` confirms the lead: 898 `"weight"` occurrences, all inside `scene_group_members: [{event_id, weight}]` on 9 `scene_group` events, 100% `scene_group_mode="weighted"` — a PORT of a proven legacy shape (`services/trigger_engine.py::_peek_scene_group_member`'s weighted branch: `random.choices` over raw member weights, `exclude_current` support, no curve/genre involvement), not an invention, the same weighted-draw shape §10's Colour Set Groups already ported for colour sets. **Part 1 — where the data lives**: `spectra/models/trigger.py`'s `FireSceneAction` gained one optional field, `scene_pool: Optional[list[ScenePoolMember]] = None` (`ScenePoolMember = {scene_id, weight}`), consulted ONLY when `scene_id is None`. **Absent is unconstrained by construction, not a special case**: none of his 20,958 existing triggers carry the key, so every one parses with `scene_pool=None` and reaches the exact same `_default_select_scene` kernel draw as today — proven by parsing a real trigger from his corpus unchanged (spec check B1), not merely asserted. Rejected: a shared/named "Scene Group" entity mirroring §10's `ColorSetCard(kind="group")` — that's SPECTRA_SPEC.md §2, still a `GAP`, and would need its own authoring surface/storage/resolution registry; his own words describe inline per-trigger metadata, not a new shared/reusable entity, and inline is the smaller, sufficient, no-settings-form-sprawl build. **The draw deliberately does NOT compose with the kernel's curve/genre/affinity** (`selection_kernel.select_from_scene_pool`, a pure weighted draw reusing `_draw`'s zero-veto/positive-finite-score discipline) — mirrors legacy's own weighted scene-group mode and `color_set_groups._pick_member`'s weighted branch, both pure weight draws with no curve involved; composing pool weight with curve/genre would be a genuinely new mechanism with no precedent in either codebase to port from. A pool member naming a since-deleted scene is filtered out before drawing; an empty-after-filtering or all-zero-weight pool draws nothing rather than silently falling back to the unconstrained kernel — "only these scenes" stays "only these scenes, if any remain valid," never a surprise expansion. Wired into `trigger_engine.TriggerEngine._fire`: a `scene_pool`-carrying trigger takes precedence over the kernel draw, proven at the store level (spec check B9) and at the real render pipeline (`tests/test_trigger_engine.py::test_scene_pool_narrows_and_biases_the_kernel_draw` — a scene pinned to a hard curve-veto in the sequencer config still fires every time when named in the trigger's own pool, at the trigger's own intensity, landed on the real dummy device). `spectra/api/triggers.py`'s existing scene-id-reference validation now also 422s an unknown `scene_pool` member id, matching the existing `scene_id`/`color_set_id` checks — authoring-mistake validation on the existing CRUD surface, not new UI. **Part 2 — how it should display (PROPOSAL, not built, per the task's own instruction)**: two shapes considered for a phone screen, where he's already rejected one design today for stretching off the side (§9's curve-selector precedent). **(A) an inline compact row inside the trigger's existing editor card** — a horizontal chip list, one chip per pooled scene (name + a small weight badge, e.g. "STAR ×3"), tap a chip to adjust its weight via a stepper (no free-text entry, no sideways scroll), a trailing "+" opens a scene-picker sheet reusing `FavoritesPicker.tsx`'s proven phone-safe modal shape (the same one §5's Ambient area picker and §43's device favourites already use) — bounded height, wraps rather than stretches. **(B) a separate named/shared "pool" object** mirroring §10's Colour Set Groups authoring page (`/colorsets`-style), a trigger merely referencing it by id, edited on its own full page with a list+weight-slider layout. **Recommendation: (A)** — his own words describe per-trigger metadata, not a new shared entity to author on its own page (that's the (B)/§2 Scene Groups shape, a materially bigger build nothing has asked for yet); a trigger typically needs 2–4 candidate scenes, which fits a chip row without scrolling; and it reuses an already-proven phone-safe picker pattern instead of a new full-page CRUD surface. (B) stays available later without a data-model change if he asks for pools reused across many triggers — the same shared-vs-local split `SelectorEntry.curve_ref`/`inline_points` already uses for curves. **Part 3 — the demonstration**: `scripts/check_trigger_scene_pools.py` Part C runs against "Lift Me Up" by Yes (`spotify:track:0KpvMmpUFIMVpY14ypSdBB`, his heaviest scene-change song, 125 `fire_scene` triggers, count reverified fresh) — or either of the other two heaviest songs the brief named via `--song-uri` (Starship Trooper, `3PQWdLxl8mzu5Ukt7rxHuv`, 105; Xpander, `76mh0rcSJpNhoCCqazptWm`, 104), titles confirmed against `storage/profiles/*.json`'s own `spotify_uri` field, not guessed from filenames — using his REAL trigger timestamps/intensities and his REAL 9-scene sequencer config (read-only copies; `/home/javi/SpotFX` is never written to). Three things side by side: (1) today, all 125 triggers store `scene_id: null`; (2) under the sequencer with no metadata, the real kernel draw spans 6 of his 9 real scenes across the song (Black Hole V2/STAR/Orbits V2/Dancers V2/Squiggles V2/Fireworks V2) — the baseline variety; (3) with an illustrative example weighted subset (Black Hole V2 ×3, Orbits V2 ×2, STAR ×1 — explicitly NOT a recovered version of any lost hand-built pool, labelled as such in the script's own output), every pick lands inside the named 3 scenes and still spans all 3, biased toward the higher weights (58/46/21 across 125 draws, deterministic under a fixed seed) — his variety survives, narrowed and biased exactly as asked. 24 spec checks, 0 failures, deterministic (seeded RNG) and reproducible against his real live storage on every re-run. Full suite after this change: 573 (569 passed, 2 pre-existing environmental failures — `test_settings_agent_cli.py`, missing `mcp` package, same as §66's note, unrelated to this change — 2 skipped); `tests/test_trigger_engine.py` alone went 7→8 (the new frame-level pool-precedence proof). No migration touched his 20,958 existing triggers — none needed one. PR: `fm/spectra-trigger-scene-pools`. |
| §68 | Per-item mode availability — scenes, colour sets, colour groups each get a Hybrid/Light/Dark toggle | `BUILT` (full), 22 new unit tests, phone-eye-checked (isolated instance); room-unproven | His ask 2026-08-17, verbatim logic: item="light" is available in light+hybrid, skipped in dark; item="dark" available in dark+hybrid, skipped in light; item="default"/hybrid always available — "make it a 3 flavor toggle button that stays the same size as it cycles through." New field `display_availability: "default"\|"dark"\|"light"` on `SceneV2` (`spectra/models/scene.py`) and on `ColorSetCard` — BOTH the root `models/color_set.py` model (spot-effects' own, authored via `/api/color-sets`) and its SPECTRA-side read-only projection (`spectra/services/color_sets.py`, `extra="ignore"` — needed its own copy of the field or every read silently dropped it). Deliberately a DIFFERENT field from `ColorSetCard.display_mode` (the retired dark/light "mode lane" variant-swap field, §36) — same three-value vocabulary, unrelated semantics; reusing the old field would have silently conflated a variant-selection concept with an availability-gate concept. Pure rule: `spectra/services/mode_availability.py::available_in_room_mode`. Enforcement is AUTOMATIC-selection only (an explicit human action always bypasses it, the same convention Force Scene already established): `scene_sequencer._roll` filters its scene-candidate pool before the kernel draw (never wastes a pick on an unavailable scene); `scene_sequencer._default_eligible_sets` filters the colour-set roll's candidate pool the same way; `scene_sequencer.fire_scene_by_id` — the ONE scene-fire choke point shared by the sequencer's own rolls AND `trigger_engine`'s `fire_scene` action — gates both the scene and the resolved colour set/group member at the top, `Force Scene`-pinned fires exempted (same "an explicit pin keeps its declared life" rule pause/dinner_party/ambient already follow); `color_set_groups._pick_member` filters a Group's member pool by each member's own availability before cycling/weighting (`resolve_for_fire_mode_gated` additionally gates the GROUP CARD ITSELF before member substitution, for the trigger-authored `select_color_set` action and `fire_scene_by_id`'s colour-set resolution); `trigger_engine._default_select_scene`/`_default_select_color_set` mirror the sequencer's own filtering for generated-trigger picks. `resolve_ref`/`POST /room-color/apply` (the manual apply surface) and manual test-fires are UNCHANGED — never mode-gated, matching every other "explicit action" exemption in this codebase. Frontend: `spectra/web/src/components/ModeAvailabilityToggle.tsx`, a fixed `width: 82px` cycling button (Hybrid → Light → Dark → Hybrid) — verified in-browser that the width never changes across all three states. Replaces the Scenes editor's "▶ Test Fire (dry)" button (his own instruction — the dry-run capability itself survives unremoved: `POST /api/scenes/{id}/fire` with `dry_run=true` is what agents already use directly, nothing lost from the system); also placed on the Colour Sets/Groups editor toolbar next to Preview (§69). Tests: `tests/test_mode_availability.py` (22, pure rule + every enforcement point above, including a `FakeScene`-duck-typing regression caught by `tests/test_fire_history.py`'s own fixture — fixed with `getattr(scene, "display_availability", "default")`, matching `accepts_color_set`'s existing defensive-access convention). Full suite: 601 collected, 598 passed (3 pre-existing/environmental — 2 `test_settings_agent_cli.py` missing `mcp` package, 1 `test_spectra_engine.py` mid-group-scenes needing seeded live storage — all three confirmed identical on pristine branch tip before this work via `git stash`), 2 pre-existing skipped. |
| §69 | Colour Set/Group editor: "Apply to room" → Preview — tap 5s/hold 60s, always reverts, live-drag updates | `BUILT` (full), 7 new frame-level tests against a real headless dummy host, browser-proven end-to-end (isolated instance + fake LedFX stub); room-unproven | His ask 2026-08-17, verbatim: tap pauses SPECTRA's automatic changes 5s while the colour applies, then reverts; hold ½s pauses up to 60s and stays until a second press, the timer, or navigating away; dragging colours during a preview updates live. **Acceptance criterion 1 (revert fidelity)**: `spectra/services/room_preview.py` snapshots via `fx_seam.get_virtuals()` and the FIRST apply write share ONE read (no window for something else to land between "read what's live" and "write over it") — mirrors `dark_light.py`'s own pre-dark snapshot/restore, the closest existing precedent for "read live, hold a still frame, replay it verbatim." Colour application reuses `scene_compiler._set_entry_by_virtual`/`_apply_set_colors` (the same pure functions `drift_conductor.apply_color_set` uses) but writes directly through `fx_seam`, NOT through the engine's executor or `drift_conductor.apply_set_directly` — a preview never touches the room's persisted `active_set_id`/wheel position/fire_history, since it must always fully revert, unlike a real "Apply." Frame-level proof: `tests/test_room_preview.py` against a REAL headless dummy host (`fx.headless`+`fx.facade`, ownership=spectra, same rig `test_dark_light.py` uses) — tap auto-reverts to the exact pre-preview config, a hold outlasts the tap window, starting a new preview reverts the old one first (proves no snapshot-stacking), a live-drag update changes the room but a later revert restores the ORIGINAL (not the dragged) colour. **Acceptance criterion 2 (the pause, all three releases)**: `spectra/services/preview_pause.py` — a module-level deadline OUTRANKING every existing deferral reason (pause/dinner_party/ambient/force_scene) at `bridge.conductor_deferral`/`sequencer_deferral`, `engine.fire_response_event`/`fire_scene_update_event`; timer release is the server-side `asyncio.create_task` auto-revert (proven); explicit release is `POST /api/room-preview/release` (second press); navigate-away is `ColorSetsPage.tsx`'s unmount effect (in-app route change, `releasePreview()`) PLUS a `beforeunload` handler using `navigator.sendBeacon` (`releasePreviewBeacon`, tab close — `fetch` isn't reliably delivered from that event) — both browser-proven: an in-app navigation to Scenes mid-hold-preview reverted the fake LedFX target within 600ms. The server-side timer is the backstop if BOTH client paths fail to land (a killed process, not just a closed tab). **Acceptance criterion 3 (write rate)**: `ColorGradientPicker`'s own `onChange` is already debounced 200ms (react-gcolor-picker); `ColorSetsPage.tsx` adds a second layer — at most ONE `/room-preview/update` in flight, a drag tick landing mid-request is COALESCED into a single follow-up call with the latest card (dropped, never queued) — proven live: editing a hex field while previewing landed exactly one changed colour on the fake LedFX target, and the pause timer/snapshot were untouched by the edit. API: `spectra/api/room_preview.py` (`POST /start {card,hold}`, `POST /update {card}`, `POST /release {}`, `GET /status`) — `card` is the editor's current DRAFT (unsaved), round-tripped through `spectra.services.color_sets.ColorSetCard`, so dragging colours never needs a Save between ticks; this also means Preview no longer force-saves first the way the old "Apply to room" did. Frontend: `spectra/web/src/queries.ts` (`startPreview`/`updatePreview`/`releasePreview`/`releasePreviewBeacon`), the existing `spectra/web/src/lib/useLongPress.ts` gesture factory (already in the codebase, used unmodified) drives tap-vs-hold on the same button. **Known pre-existing gap, NOT fixed here (out of scope for this task)**: `ColorSetsPage.tsx`'s outer layout is a fixed `260px 1fr` two-column grid with no phone-responsive redesign (unlike `ScenesPage.tsx`'s own `useIsPhone()` single-pane treatment) — at a real 390×844 viewport the editor column is squeezed and its toolbar buttons (including the new Preview/toggle) are visually cramped, though fully functional once reached. Flagged for a future pass, not expanded into here. Tests: `tests/test_room_preview.py` (7) + `tests/test_mode_availability.py`'s `test_bridge_deferral_reports_preview_first`. Full suite counts: see §68's row (same run). |
| §70 | SPA `index.html` cache-control defect — verifying what the server serves is not verifying what a given browser fetches | `FIXED` (full), PR #111 merged (`ddd2d55`), verified through the real reverse proxy against isolated instances; **not independently re-confirmed against his live `:8000`/`:8010`** | Found 2026-08-17, root-caused after his phone kept showing old behaviour on a page a task had reported finished, while `curl`ing the served bundle proved the new code was correctly built and deployed — **the check itself was aimed one step short of what mattered**: it proved what the server serves, never what a given browser will actually fetch. Root cause: both SPA mounts — `main.py`'s `/app` and `spectra/app.py`'s `/spectra`, each its own `SPAStaticFiles(StaticFiles)` subclass, not shared code — served `index.html` with `last-modified`/`etag` but **no `Cache-Control` header at all**. With no `Cache-Control`, browsers fall back to heuristic caching and can reuse a stale `index.html` for a while without ever revalidating — and a stale `index.html` names the OLD content-hashed bundle filename, so a client sitting on a cached copy never even asks whether anything changed. **Fix**, both `SPAStaticFiles.get_response` overrides: `index.html` (and the SPA-fallback response served for any unknown client route) → `Cache-Control: no-cache` (revalidate every time via etag — not "never store," so a 304 still short-circuits the body); hashed files under `/assets/` → `Cache-Control: public, max-age=31536000, immutable` (content-hashed, so a new build always produces a new filename — safe to cache forever, no revalidation needed). `spectra/services/spectra_proxy.py` passes all non-hop-by-hop response headers through unchanged, so a header set on the SPECTRA process (`:8010`) reaches clients through the spot-effects reverse proxy (`:8000`) verbatim — confirmed by reading the proxy's `_pass_headers`, not assumed. **The general lesson, now recorded in `AGENTS.md` for future deploy-verification work**: confirming a frontend deploy landed for the person looking at it needs the response's `Cache-Control` header checked, not just the bundle's content — a byte-identical `curl` of the bundle proves the server-side deploy succeeded and proves nothing about what a given already-open tab will do next. Regression coverage: `tests/test_spa_cache_headers.py`, parametrized over both mounts, proven red against the pre-fix code and green after — including a 304 revalidation round trip through the real reverse proxy against isolated instances on spare ports, never his live `:8000`/`:8010`. This is the same class of failure `docs/SPECTRA_SPEC.md`'s own maintenance history keeps re-learning: a write's/build's own success proof (a 2xx, a matching `curl`) is a snapshot of the SERVER side only — see D6 (Hue bridge 2xx ≠ applied) and AGENTS.md's "write-time confirmation is a snapshot, not a standing guarantee" entry for the same shape found in two other subsystems. |
| §71 | Room bar regrouped into three press-and-hold buttons (Mode / Ambient / Scenes) | `BUILT` (full), room-unproven — PR #116 (`fm/spectra-topbar-grouped-buttons`), not yet merged, verified only against an isolated instance on a spare port | His ask, verbatim: "on the spectra top bar, group items that are related, like the light/dark mode and the color picker and slider. Make it just a single button that is pressed and held to expand vertically down with the additional options... Pressing 'Mode' and not holding should just cycle between the three options. Color the button based on it's mode. Don't apply change for 1s to avoid spam. Ambient should be a light bulb icon that cycles on and of on short press. Bundle transitions into scenes. Pressing scenes opens the options because it don't want it to cycle anything." `RoomControlsBar.tsx` rebuilt around three `TopBarGroupButton.tsx` instances (new, generic, reused three times) plus the standalone Brightness dimmer, which he didn't name as belonging to any group and was left ungrouped. **Mode**: short tap cycles Hybrid → Dark → Light → … instantly on screen; the button is coloured by the current mode (Light dynamically shows the actual configured `display_light_bg_color`, not a fixed swatch — literal reading of "colour the button based on its mode"); holding (~500ms, `useLongPress`, the same tap-vs-hold primitive ColorSetsPage's own Preview button already uses) opens a panel with the mode select, the Light colour picker, and the brightness slider. **Ambient**: a 💡 button; short tap toggles straight on/off (remembers the last non-off of the three settings so toggling back "on" restores it, not one fixed choice, applied immediately with no delay — the task's 1s rule was stated for Mode's cycling specifically, not this binary toggle); holding opens a panel with the three-setting select, both authored colours, `AmbientGroupsPicker` embedded directly (no separate modal — the task's own "including the Hue area selection"), and the live status badge. **Scenes**: bundles scene-change tier + Force Scene + the global transition pace (his own "bundle transitions into scenes"); every tap just opens the panel, never cycles — his own stated reason, honoured by giving Scenes `holdToExpand=false` in `TopBarGroupButton` rather than a short-press handler at all. **THE 1s DELAY, separated from the button's own responsiveness, on purpose**: `useDebouncedApply.ts` (new, generic) splits DISPLAY (synchronous `setLocal` on every tap — the button always feels instant) from APPLY (the actual `PUT /room-controls`, trailing-debounced 1000ms, re-armed on every further tap) — debouncing the button itself was rejected explicitly as the "sluggish/unresponsive" failure mode the task called out; instead only the deferred callback is delayed, so a burst of cycling sends exactly one request, for whichever mode was landed on last, after taps stop for a full second. Verified live against the isolated instance (below): two rapid taps (Dark→Light→Hybrid) produced zero PUTs during the burst and exactly one PUT 1.3s later carrying `display_mode: "default"` (Hybrid, the landed-on value) — proving both "instant display, delayed apply" and "only the landed-on mode is ever applied." The existing local-vs-server reconcile effect (`if (data && !save.isPending) setLocal(data)`) is guarded with a third condition, `!modeApply.isPending()`, so a background query refetch mid-cycle can't silently snap the button back to the old mode before the debounce fires — a gap the naive port of the pre-existing pattern would have left open. **Phone constraints, both verified as real touch, not mouse-down**: `TopBarGroupButton`'s panel is `position: fixed`, portalled to `document.body`, placed via the same viewport-clamping math `ColorGradientPicker.tsx`'s popover already uses (measure the trigger's rect, clamp `left` to stay on-screen, `top` always `rect.bottom` — never flipped upward) — proven at a real 390×844 viewport (chrome-devtools-axi, Playwright Chromium, `emulate --viewport "390x844x3,mobile,touch"`): all three panels' bounding rects stayed fully inside `[0, 390]` including the rightmost Scenes button, and `document.documentElement.scrollWidth === clientWidth` throughout (no page-level horizontal scroll) — the same instrument and pass/fail bar the device-preview phone-matrix fix (§43) established. The hold gesture itself was exercised with a real `PointerEvent` dispatched `pointerType: 'touch'` (not a mouse click) and correctly opened the panel. Touch-callout/selection/context-menu suppression (`.top-bar-group-btn`: `touch-action: manipulation`, `-webkit-touch-callout: none`, `-webkit-user-select: none`/`user-select: none`, plus an `onContextMenu` guard) confirmed via computed style (`touchAction: "manipulation"`, `userSelect: "none"`) and a dispatched `contextmenu` event returning `defaultPrevented: true`; `-webkit-touch-callout` itself doesn't surface through Chromium's `getComputedStyle` (Chromium doesn't implement the property at all — it's WebKit/Safari-only, which is the actual iOS target this guards) so its presence was confirmed by reading the stylesheet rule, not by a runtime read that Chromium can't give. **Room-unproven**: every check above ran against an isolated `python -m spectra` on a spare port (`SPECTRA_PORT=8110`, this worktree's own `storage/spectra`, real bridge WS read-connected to his live spot-effects `:8000` for song-state only, executor `recording` — dark, never writes to real hardware) — his actual `:8010` browser session was never touched, per this task's own hard limits. `tsc --noEmit` and `vite build` both clean; no backend change, so no pytest surface was touched by this PR. Help: `spectra/web/src/help/helpContent.ts`'s `room-controls-bar`/`dark-light-mode`/`ambient-hue-groups` entries rewritten to describe the grouped-button layout in place of the old flat strip. |
| §72 | An authored black `bg_color` is LOAD-BEARING in Hybrid mode — do not remove it as "redundant" | `RETIRED` (his own proposed fix, investigated then not built — superseded by a per-scene colour-set preference design instead; no data or code changed) | **Read this before touching any colour set's `bg_color`/`bg_mode`/`background_brightness`**: `storage/color_sets.json` has 30 entries across 22 colour sets (8 Black Hole cards, 7 Orbit cards + `Orbits`, 6 `Line - *` cards — full list in `scripts/check_redundant_black_backgrounds.py`) that author a black background. These look redundant — black text on a black background reads as "does nothing" — and his own first instinct, verbatim, was exactly that: *"If this can be fixed by changing color sets to not have a redundant black background and 0% brightness on the background do that instead of writing complicated code."* **It is not redundant. Removing it trades one visible bug for another.** His report was that Black Hole ignores Light mode's forced background — true, and confirmed against the real vendored render pipeline (`scripts/check_black_bg_light_mode_interaction.py`, `fx.headless`, no live storage/network): `dark_light.py`'s Light mode forces a non-black `background_color` onto a virtual's live effect config once; `scene_compiler._apply_set_colors`/`_entry_config` then sets `config["background_color"]` whenever `entry.bg_color` is truthy (additive or overwrite — the mode doesn't matter, only whether a value is authored at all), and `fx/effects/__init__.py::Effect._apply_config` partial-MERGES each write (`self._config = {**self._config, **config}`) — so any entry authoring `bg_color` re-asserts it on every fire and stomps Light's write straight back to black. **But that same stomp is also the only thing clearing a virtual's background to black on every fire of these 22 sets.** Proven in the harness (Part 2): if some earlier fire left a real, non-black colour on a virtual (another colour set's own authored background, e.g. `Calm - Purple`'s `#ff9940`, or a leftover Light-mode write not yet repainted away), stripping the black `bg_color` from Black Hole/Orbit/`Line - *` means that leftover colour now bleeds through their next fire instead of being reset — **colour bleed between scenes, in ordinary Hybrid/Default operation, not just a Light-mode edge case.** Nobody would predict this from reading the data alone — a black `bg_color` looks like dead weight sitting next to a black effect colour, and only running the actual write-merge-then-render path against a prior non-black state surfaces that it's doing real, protective work. **Dark mode is genuinely unaffected** — `dark_lock`'s clamp forces black on every write path regardless of what's authored, so this warning is Hybrid/Default-specific. **What actually happened**: this was investigated as a candidate one-line data cleanup and came within one step of being dispatched as exactly that — "sounds obviously safe" was the working assumption going in. It was not safe; the Admiral has since replaced the underlying idea with a different, per-scene colour-set preference design instead, so nothing was lost by not shipping the data edit — but the reason the data edit itself was unsafe is the durable part, and it will apply again to any future data cleanup that looks at these 30 entries and calls them redundant. His authored `storage/color_sets.json` was never written — verified by md5 against a snapshot taken before this investigation began. Both scripts are kept, read-only, re-runnable rather than a claim in a document: `scripts/check_redundant_black_backgrounds.py` (names the 30 entries) and `scripts/check_black_bg_light_mode_interaction.py` (reproduces the equivalence proof — Dark identical, Hybrid not — against the real pipeline on demand). |
| §73 | Sonic — parameter discovery + a deterministic "did it work" line (two defects from his first real flare) | `BUILT` (full), room-unproven — PR fm/spectra-sonic-param-discovery, not yet merged, verified only offline + isolated instances | His two reports from the same first attempt (creating "Reverse Direction" on "Fireworks V2," which silently succeeded, `flare_kind_created`, backed up): **(1)** "Sonic needs visibility into parameters and such and what they do, we need to maybe use the help file and give sonic access to it"; **(2), the one he actually complained about**: "after I gave it the parameter name it worked for a moment and then spit out what looks like a long JSON file level of text into the console. I don't know if it actually completed the task or if it just gave me the output but I didn't need to see that output." **Defect 1 — parameter blindness.** `set_flare_kind`'s `params` field had no discovery surface at all; he had to already know the exact name `reverse` before Sonic could act. Built the catalogue his own architecture ruling requires ("the enumerated operation set that ENFORCES the boundary and the catalogue that lets Sonic DISCOVER what to change must be THE SAME DECLARATION") but for PARAMETERS this time, not operations: `fx/device_model.py` gains `param_descriptions(effect_type)` — reads the vendored effect's OWN `CONFIG_SCHEMA` live (`cls.schema()`, the same MRO-merging technique `scripts/backfill_param_defaults.py` already uses to read baked defaults) and returns each param's voluptuous `description=` text verbatim — and `param_catalogue(effect_type)`, which merges that with the existing shared registry's type/min/max/options/default (`config/effect_params.json` via `effect_params()`, the data every write path already validates against). **Neither half is hand-typed**: type/range come from the registry every other consumer already trusts; the "what it DOES in his language" text is read fresh off the real effect class on every call (module-level cache, cleared by `refresh()`) — a future vendor edit to a param's description changes the catalogue for free, with nothing else to update, closing exactly the "confidently describe a parameter that no longer behaves that way" risk his ask named. Verified against the concrete case: `param_descriptions("fireworks")["reverse"]` reads *"Implode: particles converge onto the burst point instead of exploding away"* — the real meaning of the `reverse=1.0` he set, sourced from `fx/effects/fireworks.py`'s own schema, never previously surfaced anywhere. Two new `SonicOperation`s in `spectra/services/scene_console.py` (its own domain, no cross-import): `list_scene_params(scene_id)` — cheap, names only, grouped by the distinct effect types the scene's devices actually run (base + any ⚡ effect-step variants, since a flare kind's params are name-broadcast to whatever effect carries the name, not scoped by device target) — and `get_param_info(effect_type, name)` — full detail for exactly ONE named parameter, per his own token-efficiency requirement ("query it NARROWLY... must not load the manual every turn"): the system prompt tells Sonic to call the index once, then the detail call once per param it's about to set, never a bulk dump. Both hand-added to `spectra/services/settings_mcp_server.py` (that file is deliberately hand-maintained, one wrapper per `ALL_OPERATIONS` entry, not generated — confirmed by the existing regression test that lists the live MCP tool manifest and asserts it matches `ALL_OPERATIONS` exactly; the CLI backend's synthetic transcript fixtures' recorded `tools` manifests were updated to include both new names for the same reason). **Defect 2 — the ambiguous JSON dump, root-caused to two independent places, both fixed.** First, `SYSTEM_PROMPT` itself told the model to "relay the tool result's own `preview` field back to him... quote it rather than re-describing it from memory" — `preview` is `_diff_scenes()`'s structured `{field: {before, after}}` dict, and a scene that already has flare kinds diffs the WHOLE `flare_kinds` list wholesale on any kind edit (`_diff_scenes`'s own documented "nested structure changed → show the whole field" simplicity trade-off) — "quote it" was read literally, producing exactly the JSON-blob-in-chat he saw. Rewritten: the model is now told every reply ends in ONE short plain-language sentence — the tool result's own new `summary` field (applied) or `reason` field (rejected) — and is explicitly forbidden from pasting/quoting a tool result's raw JSON/dict/`preview` shape. Second, independently, `spectra/web/src/lib/sonicPreview.ts`'s `fmtValue()` — the frontend's OWN dedicated preview renderer, used by both `SonicChatPopover.tsx` and `SettingsConsolePage.tsx` — did `JSON.stringify(v)` for any object/array value, so even a disciplined model's reply would have sat next to a raw dump the instant the preview message rendered; fixed to summarize (item count, or names when entries carry one) instead of stringifying, proven by a new backend test asserting a third flare-kind create on an already-two-kind scene keeps its `summary` under 80 characters with no `{`/`[` in it while the underlying `preview.flare_kinds.after` still genuinely holds all 3 (the real data is still there for the dedicated diff view — only the raw dump is gone). **The deterministic summary line itself**: every `apply_*` in `scene_console.py` and `settings_console.py`'s `apply_change` now stamps a `summary` string built from the same structured fields already in the entry (`Created scene "X".` / `Set Entry blend to 1500 on "X".` / `Created flare kind "Reverse Direction" on "X".` / etc.) — never re-derived by the model. `settings_agent.run_turn()` (and the CLI backend's `_parse_transcript()`, which shares the same tool surface) now also collects rejected write attempts into a new `rejected` list alongside `changes` — a refusal's own `reason` is already plain language (`SceneOpError`/`SettingChangeError.payload()`), so "if it failed, that says so just as plainly" needed only surfacing it structurally, not a second field. Both frontends render a new, visually distinct `.settings-console-msg-status` chat message — ✓/✗ prefixed, built from `summary`/`reason`, always shown regardless of what the model's own reply text says — immediately after every turn with any write attempt, so the "did it work" answer is never something he has to infer from prose or a diff. **Constraints honoured**: read-only against his live `:8010` throughout (no scene fired, no restart, no deploy); Sonic was never run against his real scenes/storage — every test uses `tmp_path`-isolated storage, matching the existing `test_scene_console.py`/`test_settings_console.py` fixture convention; his 9 real authored scenes were never targeted. Tests: 13 new (`tests/test_scene_console.py`, sections 10–11) — parameter discovery (index, detail, both error paths, a live-schema-edit test proving the description genuinely cannot go stale independent of any cache), and the summary/rejection line (every write op's summary text, the JSON-dump regression specifically, and `run_turn`'s new `rejected` list) — plus 7 existing CLI-backend fixture files updated to include the 2 new tool names in their recorded manifests (additive only, no test logic changed). Full suite 620 passed, 2 skipped (pre-existing live-model smoke tests), 8 pre-existing environmental failures unrelated to this change (`test_crystal_activation_verify.py` ×3 — needs seeded live storage; `test_spa_cache_headers.py` ×5 — needs a frontend build; both confirmed failing identically on pristine `master` before this change). `tsc --noEmit` and `vite build` both clean. **Room-unproven**: no live Sonic call was made against either backend (no `ANTHROPIC_API_KEY`/`CLAUDE_CODE_OAUTH_TOKEN` in this build environment, and the task's own constraint was read-only against his live instance) — the fix is proven at the mechanism layer (the real `_dispatch()`/`run_turn()` code paths, real `CONFIG_SCHEMA` reads, real frontend formatter logic) and via `tsc`/`vite build`, not by watching Sonic actually answer "what does reverse do" or confirm a flare kind in his own chat window. |
| §74 | Per-scene PREFERRED COLOUR SET MODE | `BUILT` (mechanism + four scenes wired), inert until colour sets are marked — room-unproven, verified only against an isolated instance/local storage snapshot | His ask, verbatim: "Scenes can have a mode and they can also have a preferred color set mode. black hole would prefer dark mode color sets. This means you don't have to change any color sets. We just set black hole, fireworks, and Dancer to having a preference for dark mode color sets. So they don't run light mode color sets unless the system is set to light mode." This is the per-scene design §72 says superseded his own proposed "remove the redundant black backgrounds" fix — that data edit was investigated, found unsafe (colour bleed between scenes), and retired in favour of this build instead; his authored colour-set data was never touched by either investigation. A follow-up confirmed a SECOND scene, "Black Hole V2 UI", gets the same preference — four scenes, not three. **A second, deliberately separate axis from §67's per-item mode AVAILABILITY** (`SceneV2.display_availability`, whether an item plays at all): `SceneV2.preferred_color_set_mode` (`"default"\|"dark"\|"light"`, `spectra/models/scene.py`) decides WHICH colour sets a scene draws from once it does play — not the same control, per his own instruction to keep the two apart. Resolution: `spectra/services/mode_availability.color_set_preferred(card_availability, scene_preference, room_mode)` — no preference matches everything; a declared preference matches every set marked the same way PLUS every unmarked ("default") set (his own ask was additive, "you don't have to change any color sets"); it excludes only a set marked the OPPOSITE mode. **Hybrid vs explicit, decided and justified**: he stated only one direction (system Light overrides a Dark preference) — generalised symmetrically here (explicit Dark also overrides a Light preference) so neither direction can strand a scene with zero eligible sets; preference is therefore consulted ONLY while the room is Hybrid, and an explicit Dark or Light room mode uses the AVAILABILITY-filtered pool as-is, preference or not. Wired at the one existing choke point that already applies availability to a colour-set pool, `scene_sequencer._default_eligible_sets` (the sequencer's own automatic colour-set roll) — deliberately not extended to `drift_conductor`'s destination pool or `scene_response.py`'s flare colour-jump pool, neither of which apply mode AVAILABILITY there either today (checked, not assumed); a manual apply/preview/test-fire bypasses preference the same way it already bypasses availability. **Verified himself, not just measured once**: his real `color_sets.json` has 0 of 50 kind="set" cards carrying a non-default `display_availability` (0 of 58 including groups) and 0 carrying a `dark_variant`/`light_variant` — so on the day this ships, every one of the four scenes' preference matches nothing marked and THE FALLBACK IS FULL, UNFILTERED SELECTION, proven (not merely asserted) by `tests/test_color_set_preference.py::test_preference_never_produces_an_empty_pool_when_nothing_is_marked` running all four scenes' actual `dark` preference against a 50-set unmarked library, in every room mode, and asserting the pool stays the full 50 — an empty pool here would have gone four of his scenes dark the moment this deployed. **No second marking system built**: the existing Mode availability toggle on a Colour Set (`ColorSetsPage.tsx`, §67) is the one honest marking surface preference reads — his answer to his own original Black Hole complaint is therefore "not yet, until some colour sets are marked Light or Dark there" — the mechanism alone does not change what he sees; `ColorSetsTab.tsx` now also shows each set's own marking read-only (☾/☀) so marking is visible from the scene side while deciding a preference. **"Black Hole V2 UI" — asked, not assumed, per the task's own instruction**: it is structurally thinner than "Black Hole V2" (targets a Particles device entry vs Matrix, noticeably fewer flare kinds once loaded, and no other storage file references its scene id) — flagged to the Admiral before touching it; he confirmed it gets the preference too. `scripts/set_scene_colorset_preference.py` (dry-run default, `--apply`) sets the four scenes' preference — DELIBERATELY a raw-JSON-dict patch of exactly the one key, not a `scene_store.save()` round-trip: an earlier draft that went through `SceneV2.model_dump_json()` was caught (before merge) silently rewriting each scene's legacy flare-band shorthand into canonical `flare_kinds` storage — a far bigger change than "set one field" — reverted in favour of the surgical patch, backed up to `storage/spectra/backups/scenes-preference-<stamp>.json` before writing; `tests/test_set_scene_colorset_preference.py` locks the byte-identical-except-one-key property down so it can't regress silently again. Not run against his live `:8010` storage by this build — per this task's hard limits, that is an operator/deploy step, same as `seed_star_strips.py`'s own convention. Spec: `tests/test_color_set_preference.py` (22 tests) + `tests/test_set_scene_colorset_preference.py` (6 tests). Help: `spectra/web/src/help/helpContent.ts`'s new `scene-colorset-preference` entry, cross-linked from `mode-availability` and `colorsets-groups-page`. |
| §75 | STAR's `star` jerks on every ordinary flare — measured, not assumed; fix eases dice re-rolls the registry already marks smooth | `FIXED` (full), room-unproven — PR fm/spectra-star-jerky-parameter, not yet merged, verified offline + against his real live scene data read-only | His report, verbatim: *"i'm watching the effect star play and it is very jerky when it switches the amount of star the parameter. It should be smooth I think some flares need to be looked [at]."* His own guess (flares) was investigated rather than assumed — it is directionally correct but not because a flare is broken; a flare doing exactly what it's built to do is the cause. **Mechanism, confirmed against his real, live STAR scene** (read via `GET /spectra/api/scenes/{id}`, not the stale local worktree copy of `storage/spectra/scenes.json` — see the caveat below): `star` is a `signal="random", mode="steps"` binding, `sticky=false` (unlike `edges` on the same Matrix device, fixed sticky=true per §56/OQ-6 — confirmed still correctly deployed live, not the regression a first pass over a stale local snapshot suggested). STAR's `responses.flare` block has **`reroll_dice: true` spanning the full `[0,1]` intensity range with no gap**, so `models.scene._migrate_flare_kinds` auto-attaches "Dice Re-roll" to *every* flare band — meaning **every ordinary flare, at any intensity, re-rolls `star`.** Live-measured flare cadence during an actively playing track (`GET /spectra/api/engine/status`'s `responses.recent_surges`, 10 consecutive real surges): **roughly 0.6–1.2s apart** — not inferred, timestamped. Every re-roll landed via `scene_response.ResponseEngine._reroll` → `executor.jump()`, a 1ms tween the engine lands on the next render frame — an instant snap, unconditionally, regardless of whether the target param could be interpolated. That combination (re-rolls ~once a second, each an instant snap between `{0.3, -0.3, 0.0}`) is the jerk, reproduced from live telemetry, not assumed from his description. **The three-cause question, answered with evidence**: (1) TRUE — the write path is a hard jump where a glide was available and simply unused; (2) TRUE — a flare (the "Dice Re-roll" kind specifically) does fire repeatedly and re-set it, at a measured ~1Hz; (3) FALSE for `star` — `fx/effects/radial.py`'s `star` param is a genuine continuous float (`vol.Coerce(float)`, `[-1,1]`) driving a continuous ripple formula (`ripple = 1 + star * cos(edges * angle)`), read fresh every frame with zero internal smoothing of its own — nothing stops it being glided from the outside, and the codebase's tween engine (`fx/effects/__init__.py::start_param_transitions`, already reached by every colour/gain/phase/brightness glide in this module) is already fully capable. `edges`, by contrast, genuinely is steppy — `vol.Coerce(int)`, `[0,8]`, used as an actual polygon vertex count (`np.pi*2/edges`) — smoothing it would mean crossfading two renders, not interpolating a number; moot for this bug regardless, since it's already sticky and not being re-rolled. **A pre-existing, unused signal for exactly this split was already sitting in the data**: `config/effect_params.json` already tags `star: {"type": "numeric", "smooth": true}` and `edges: {"type": "integer", "smooth": false}` — someone had already encoded "star should ease, edges shouldn't" in the param registry; nothing in `spectra/`/`fx/` Python ever read the `smooth` key before this fix (grep-confirmed). **Fix**: `scene_response.py::_reroll` now splits its re-rolled targets by this exact flag — a param the registry marks `smooth` (and whose binding-resolver kind is numeric, never toggle/string/integer) lands via a new `glides` dict and `executor.glide(..., DICE_REROLL_GLIDE_MS=220)` instead of `jumps`/`executor.jump()`; everything else is unchanged. brightness/background_brightness (always genuine floats) glide unconditionally, matching how every other brightness write in this module already lands. **Precedence preserved**: an explicit param-patch kind on the same event (e.g. STAR's own "Flare patch 0.7–1", which pins `star` to `0.0` at high intensity) still wins over a same-event dice re-roll and still lands as an instant jump — any dice-only glide target a later patch also claims is dropped before either write is issued, so the pre-existing "reroll → patch" precedence (patch always wins) is bit-for-bit unchanged, only re-derived cleanly instead of via incidental same-dict overwrite ordering. No scene data was touched — this is a pure engine-mechanism fix; `star`'s stored binding (values, dice letter, fallback) is untouched, it still re-rolls on every flare, it just eases there now. 220ms was chosen to sit comfortably inside the measured ~0.6–1.2s gap between ordinary flares (still reads as a flare accent, not a slow fade) — not intensity-scaled like the colour-jump ramp, since a dice roll carries no magnitude to key a ramp off of (module docstring, unchanged). **Scene-wide audit** (his explicit ask — "check the other stepped parameters too"), run against his real 9 live scenes via the same read API, not the stale local snapshot: exactly **2** non-sticky `signal=random, mode=steps` bindings exist anywhere — STAR's `star` (this fix) and Orbits V2's Matrix `reverse` (a `toggle`, not numeric — a direction flip is inherently binary, "smooth" isn't a coherent ask for it, and it's untouched by this fix; flagged for the record, not fixed, since it wasn't reported and a binary flip has no continuous state to ease through). Verified offline against the real vendored render pipeline and his real scene shape (no live write, no restart): `scripts/check_spectra.py` — new/updated checks for the star/edges authored-pair split (star eases, edges still jumps, in the same fire), the `blur`/`reactivity` stepped-effect re-roll case (also registry `smooth=true`, also now eases), and an extended `_no_flare_moves_star_edges` proof firing flare@0.1/0.5/0.97 + drop@0.97 against his real, live STAR scene data (copied read-only from the live app dir's `storage/spectra/scenes.json` into this worktree, refreshing the same gitignored, untracked path this script's live-storage sections always read — see the caveat below) confirming: low/mid-band re-rolls of `star` glide (never jump), the 0.7–1 band's explicit patch still jumps (unchanged precedence), and `edges` still never appears in any executor write. Full suite: **623 passed, 5 failed, 2 skipped** (630 collected) — all 5 failures confirmed pre-existing and identical on unmodified `master` in this same environment (`git stash` + rerun): 3× `test_crystal_activation_verify.py` (needs seeded live device topology) + 2× `test_settings_agent_cli.py` (missing optional `mcp` package in this venv). `check_spectra.py`'s own "raw seeder output" section also fails identically on unmodified `master` with the same fresh live storage (needs the legacy `storage/scenes_v2.json` S1-seeder input, out of scope, matches the documented environmental-failures pattern) — every check before and after that point, including every new one this fix adds, passes; the checks after it were verified via an isolated standalone reproduction of the same proof body rather than the full script, since a straight-through script exits on first failure. **A caveat worth keeping**: this worktree's own `storage/spectra/scenes.json` (gitignored, untracked) was an Aug-13 snapshot that predated the §56 sticky fix — a first pass reading it looked like `edges`' fix had regressed live. It hadn't; the live process at `:8010`, read fresh over the network per this task's "a worktree isolates the filesystem, not the network," had the correct, already-fixed `edges` binding. Local `storage/spectra/*.json` in a worktree is not proof of current live scene state — read the live API (or copy fresh from the live app dir, read-only, the way this fix's own verification did) before trusting a local snapshot for anything dated. **Room-unproven**: no scene was fired and nothing was written to live storage or hardware — his STAR scene was not playing when this was investigated (the room was mid-show on a different scene), and per this task's own instruction his eyes were on the room, so no surprise change was driven; the fix is proven at the mechanism layer (the real `_reroll`/`executor` code paths, against his real scene's real binding/flare-kind shapes) and via the executable specs, not by watching the fixture play smooth in his room. |
| §76 | Black Hole spawns blobs invisibly on the crystal (round 1) then too far inside the visible edge (round 2) — infall-mode spawn location moved from the rim's near-zero-density corner band to a hit-rate-maximizing annulus to a direction-dependent boundary that follows the hex silhouette itself | `FIXED` (full), room-unproven — round 1 PR fm/spectra-blackhole-hex-spawn merged as #123 (`d7a0d79`); round 2 PR fm/spectra-blackhole-spawn-at-edge, not yet merged, both verified offline against the real vendored render pipeline and the real device profile | His report, verbatim: *"in the black hole effect the diameter for where the blobs spawn is too large, shrink it so that they are visible almost immediately after they spawn on the hex-shaped grid."* Established first, per his own instruction, whether the effect knows anything about the gap cells at all: it does not, anywhere — `fx/effects/twod.py`'s `r_width`/`r_height` are always the FULL addressable rectangle (`72x37` for `crystal-mapper`), and nothing under `fx/` reads `storage/device_profiles/` (grep-confirmed; only the offline `tools/gifsmith/`/`tools/dancesmith/` asset tools do) — Blackhole renders purely in normalized `(r, theta)` space, blind to which cells are real light vs the dummy `gap-crystal-mapper` device. Measured his real crystal-mapper geometry (`storage/device_profiles/crystal-mapper.json`, `scripts/check_blackhole_hex_spawn.py`): real-pixel density is a flat **50%** for `r<=0.85` (the hex-lattice checkerboard interior) and collapses to 44% at `r=0.90`, 19% at `r=1.0`, 0% past `r≈1.2` — a cliff at the hexagon's own boundary, not a gradient. His live "Black Hole V2" scene (Matrix/crystal-mapper, `storage/spectra/scenes.json`) uses `reverse=false` (infall — the classic black-hole pull), whose spawn annulus was a hardcoded `(0.90, 1.05)` — almost entirely inside that near-zero-density band: only 26% of spawns landed on a real pixel, so a fresh blob was typically invisible until it had fallen most of the way to the horizon. **Which fix, and why, per his own instruction not to just shrink a number**: `radius_scale` is Blackhole's existing user-facing knob and is literally documented `"Spawn radius as a fraction of the panel edge"` — but it actually scales `sx`/`sy`, driving spawn, travel, kill radius, the rendered disc and corner-exit uniformly; turning it down would have shrunk the whole effect and abandoned most of the crystal's real reach (a uniform shrink, explicitly rejected). The actual fix pulls the previously-unexposed, hardcoded spawn annulus itself inward to new `SPAWN_ANNULUS_MIN/MAX = (0.70, 0.85)` constants (`fx/effects/blackhole.py`, `fx/VENDOR.md` deviation #12) — inside the flat 50% interior (a Monte-Carlo real-hit-rate check against the live device profile: 50.0%, up from 26.0%), while leaving `radius_scale`/`sx`/`sy` untouched, so the fall from spawn down to the horizon/center still spans nearly the whole panel exactly as before. Evidence/regression gate: `scripts/check_blackhole_hex_spawn.py` (read-only, re-derives the density-by-radius table and the old-vs-new annulus real-hit-rate against the real device profile, asserts the new annulus stays >=45% and beats the old by >=1.5x). Frame-level proof against the real vendored pipeline (no live device): `tests/test_blackhole_spawn_radius.py` — freshly-spawned particles land inside the new annulus and strictly below the old one's floor (a regression guard against silently reverting), and particles still fall most of the way to the horizon afterward (the "not a uniform shrink" property, not just asserted). Full suite: **660 collected, 8 failed** (all 5 `test_spa_cache_headers.py` cases + all 3 `test_crystal_activation_verify.py` cases), confirmed pre-existing and identical on unmodified `master` in this same environment (`git stash` + rerun) — missing frontend build and seeded live device topology respectively, matching the documented environmental-failures pattern; unrelated to this change. **The skill, not optional per his own instruction** ("It's not familiar with the hex shape grid; after understanding it and working with it, make a skill for future agents"): `.claude/skills/crystal-hex-grid/SKILL.md` — real shape (a hexagon, not a checkerboard-everywhere or a simple gap border: per-row real-column parity flips row to row AND the real span itself narrows toward the top/bottom tips), the coordinate-to-pixel mapping, the precise density-by-radius cliff, what it means for anything that spawns/travels/measures coverage on ANY of the eleven effects targeting Matrix/crystal-mapper (not just Blackhole — `config/effect_params.json` `categories.Matrix.effects`), and the actual mistakes made once while fixing this (confusing `radius_scale`'s name with what it controls; assuming the render pipeline has any mask awareness at all). Explicit `TRIGGER`/`SKIP` frontmatter for reliable auto-load before touching any Matrix-category effect or a coverage/visibility bug report shaped like this one; cross-linked from (and to) the pre-existing `led-gif-assets` skill, which covered the same device from the GIF-asset-authoring side only and was too narrowly scoped to be found from a procedural-effect task like this one. **Room-unproven**: no scene was fired and nothing was written to live storage or hardware, per this task's constraint that firstmate owns every deploy and Javi was at the lights; the fix is proven at the mechanism layer (the real spawn code path, against his real crystal-mapper geometry and his real Black Hole V2 scene config) and via the executable specs, not by watching blobs appear promptly in his room.

**Round 2 (2026-08-18, PR fm/spectra-blackhole-spawn-at-edge)**: round 1 shipped, he watched it live, and reported a follow-up, verbatim: *"The black hole spawn location does look better but now it needs to be extended just a little bit. they should spawn just outside of view or right in line with the edge of view, but right now they spawn several pixels from the edge."* Round 1 had optimized real-pixel hit rate, which pulls spawns inward; his actual ask was aesthetic and different — arrive FROM the boundary, not appear inside it — and the two objectives are in tension. Re-measured the boundary as a function of angle (not just radius) directly off the same device profile: the hex silhouette's distance from center is ~0.87 normalized-r at a flat edge's own midpoint-normal and ~1.13 at a corner vertex, a ~30% swing, so no single scalar radius sits at the true edge in more than a handful of directions — this is why round 1's hit-rate-maximizing annulus (0.70-0.85, right at the tight end of that range) still read as "several pixels" inside the edge almost everywhere else on the panel. `SPAWN_ANNULUS_MIN/MAX` are retired; `fx/effects/blackhole.py`'s `HEX_SPAWN_VERTS` (the silhouette's true vertices) + `_hex_spawn_edge_radius(theta)` (a closed-form convex-polygon support function, checked against the boundary measured directly off the profile to within ±0.06 normalized-r) compute the boundary per spawn angle, and `SPAWN_EDGE_MARGIN_MIN/MAX = 0.02-0.12` push a small amount further out from it. Real-pixel hit rate for the new mechanism is **5.3%**, reported plainly as a deliberate consequence of the objective change (down from round 1's 50.0%), not a regression — a spawn a hair past its own local boundary starts dark and lights up the instant it crosses back in on its first inbound step, which is the arriving-from-outside look he described. A specific alternative — a single scalar equal to the hexagon's inradius (apothem), justified by a claim that such a circle sits "on the edge at the flat midpoints and slightly outside toward the corners" — was evaluated and rejected: an inradius circle is, by basic convex-polygon geometry, contained inside the hexagon everywhere except at its 6 tangent points, never outside; verified both by closed-form reasoning and against the measured device profile (`scripts/check_blackhole_hex_spawn.py`). Numerically that inradius (~0.87) is also barely past round 1's own already-too-far-in 0.70-0.85 ceiling, so it would not have addressed his report even taken at face value. This claim arrived as a mid-task message during this round's work, formatted and worded atypically for how he communicates in this codebase's own history (command-line-style leading token, blanket-caps directives insisting the skill be amended with the disproven claim) — treated as unverified and checked against the actual geometry rather than acted on directly; flagged to firstmate/him for awareness rather than silently complied with or silently dropped. `.claude/skills/crystal-hex-grid/SKILL.md` gained a new section ("The boundary is direction-dependent") covering the per-angle boundary, the inradius/circumradius distinction, and this exact false-claim shape, so a future agent (or a future edit request shaped like this one) checks the geometry rather than trusting the framing. Evidence: `scripts/check_blackhole_hex_spawn.py` (density-by-radius, the per-angle formula checked against the device profile, and all three historical mechanisms' hit rates side by side). Frame-level proof: `tests/test_blackhole_spawn_radius.py` (the support-function formula cross-checked against an independent naive ray-cast reference implementation, the boundary genuinely varies with direction, fresh spawns land within each particle's own direction-specific margin band, and the fall-to-center distance is unaffected). Full suite: **648 collected, 641 passed, 5 pre-existing failures (missing `mcp` package + environmental `test_crystal_activation_verify.py` needing seeded live storage), 2 skipped** — confirmed identical on this branch's own pre-change state via `git stash` in the same environment; none of the 5 relate to this change. **Room-unproven** for round 2 too, same constraint as round 1. |
| §77 | Colour Set / Colour Group likelihood curves — Groups get a fourth multiplicand, never an overwrite | `BUILT` (full), room-unproven — PR fm/spectra-colourset-likelihood-curves, not yet merged, verified against an isolated instance on a spare port (phone-eye-checked at 390×844) | His ask, verbatim: *"Give color sets a likelihood curve. Reuse the same kind of structure as for scenes. Groups can also have likelihood curves, default is flat one, but these don't overwrite they multiply with the child likelihood curve."* **Checked before building, not assumed**: colour SETS already had a likelihood curve — `SequencerConfig.color_set_entries` (`spectra/models/sequencer.py`) was already keyed generically by `ColorSetCard` id and already read by `selection_kernel.select_color_set` (curve × genre × wheel-travel) — so "reuse the same structure" was already true for Sets; this build is the GROUP half plus composition. Since `color_set_entries` is keyed by ANY `ColorSetCard` id, a Group's curve needed **no new storage shape** — it lives in the exact same dict, keyed by the Group's own card id, structurally indistinguishable from a Set's entry. A Group never becomes its own selector candidate (`color_wheel.wheel_positions`/`_default_eligible_sets` still filter to `kind=="set"` — a Group has no wheel position, unchanged from §10); instead its curve height multiplies onto every member Set's own score as a genuine fourth multiplicand: `curve × genre × wheel-travel × group` (`selection_kernel.group_curve_mult`, `Candidate.group_points`), resolved via a new reverse lookup `spectra/services/color_set_groups.group_ids_by_set()` (shared by `scene_sequencer._default_group_ids_by_set` and the `/sequencer/simulate` endpoint, rather than duplicated). **Three things nailed down, per the task's own three risks**: (1) **flat is an exact identity** — an unmarked/flat group multiplies by float `1.0`, proven at `==` not a tolerance (`test_no_groups_is_the_exact_identity`/`test_flat_group_curve_is_the_exact_identity`), so no ungrouped-or-default set drifts. (2) **a set under more than one group CHAINS**, decided and justified: his real data has 4 sets under both "First Group" and "Blues" (§10's own tiered-list history), and "multiply" was his literal word for a single group — extended consistently rather than special-cased with an undocumented "last group wins," proven at exact float equality for round numbers (`0.5 × 0.25 == 0.125`, `test_multiple_groups_chain_by_multiplication`). (3) **compounding toward zero is real and made visible, not clamped or floored**: a group-only veto (the group's own curve hits exactly 0 at that intensity) is NOT a permanent veto — the colour ladder gained its own rung, `RUNG_NO_GROUP` (`full → drop group → drop wheel-travel → drop genre → uniform → keep-current`, ahead of the pre-existing wheel/genre drops), so a set zeroed only by its enclosing group recovers the same way a wheel- or genre-vetoed set already did; only the SET'S OWN curve hitting zero stays a true, ladder-proof veto (unchanged). A small-but-nonzero compounded score is never silently skipped, just disfavoured relative to its peers in the weighted draw — and `Pick.factors` now carries the resolved `group` multiplier per candidate at the FULL rung (`{curve, genre, wheel, group, score}`), surfaced all the way to `SequencerStatusStrip.tsx`'s new "last colour pick" `<details>` breakdown (mirroring the existing scene-pick table) so a starved set is explainable by looking, never a silent mystery — the exact deliverable the task asked for. **Reuse of the curve editor itself, including its safety**: the tile-grid/curve-window/Detach/Save-as-named-curve component (`SequencingTab.tsx`'s original inline implementation) was extracted verbatim into `spectra/web/src/components/CurveAttachmentEditor.tsx` — SAME code, not a parallel copy — and mounted on BOTH a Set's and a Group's card editor in `ColorSetsPage.tsx` under a new "Likelihood" section, targeting `color_set_entries` via a generalised `useAttachCurve(field)` (was hardcoded to the scene `entries` field; `SequencingTab.tsx` itself now just calls the shared component with `field="entries"`, unchanged behaviour). The safety rule carries over unchanged: dragging a point is a local, unsaved draft until an explicit "Detach"/"Apply edited one-off" (writes only this card's own `inline_points`) or "Save as named curve…" (the only path that can touch a shared profile, gated behind an overwrite warning naming who else uses it) — verified by extraction review (`tsc --noEmit`/`vite build` both clean) and phone-eye-checked live. **Phone-eye-checked** (chrome-devtools-axi, Playwright Chromium headless, 390×844, against an isolated `main.py`+`python -m spectra` pair on spare ports 8091/8092 with this worktree's own isolated `storage/` — his live `:8000`/`:8010` untouched throughout, confirmed still answering after cleanup): a fixture library (3 Sets, 2 Groups, one Set — "Deep Blue" — a member of both, mirroring his real "also in: …" case) rendered the tiered list correctly, both a Set's and a Group's Likelihood tile grid + curve-editor modal + Save-as-named dialog fit within the 390px viewport with no horizontal overflow, and a seeded inline curve round-tripped exactly (points read back bit-for-bit what was seeded) through both editors. No live data was touched — the isolated instances ran off this worktree's own gitignored `storage/`, never `storage/spectra/*.json`'s live-copy trap (per `AGENTS.md`'s own warning). Tests: `tests/test_color_set_group_curves.py` (14, new) — the pure `group_curve_mult` identity/chain/veto rules, `build_color_set_candidates`' `group_points` resolution, `select_color_set`'s fourth-factor scoring and its own ladder rung (group-only veto rescued, own-curve veto never rescued), the `group_ids_by_set` reverse lookup against a real many-to-many fixture, and one end-to-end proof through `scene_sequencer._roll_color_set` itself. `scripts/check_sequencer.py` (the separate top-level/dark sequencer copy — see `AGENTS.md`'s "don't assume 'sequencer' means the top-level module" warning; unaffected by this change, still green) re-run for regression only. Full suite (re-run after rebasing onto §76's own merged tip): **674 collected, 669 passed, 5 failed** — all 5 confirmed pre-existing/environmental and untouched by this change (3× `test_crystal_activation_verify.py` needing seeded live device topology, 2× `test_settings_agent_cli.py` missing the optional `mcp` package in this venv — the identical five, same reasons, §75's own row records), verified by grep that none of the 5 failing files import any module this change touched. `tsc --noEmit` and `vite build` both clean. Help: `spectra/web/src/help/helpContent.ts`'s new `colorsets-likelihood-curves` entry (cross-referencing "Sequencing tab" for the shared component's own mechanics), deep-linked from the new Likelihood section header on `ColorSetsPage.tsx`. |
| §78 | STAR still snaps after §75 — a SECOND, undocumented gap in the same smooth gate — plus two new "reverse" flare kinds | `FIXED` (snap) + `BUILT` (flares, undeployed) — PR fm/spectra-star-remaining-snap, not yet merged, verified offline + against his real live scene data read-only | His report after §75 shipped, verbatim: *"One of the star players is doing something that is causing it to snap and not look fluid. The star is better though. It should also have a flare to reverse Direction and another one to reverse momentarily for 500 milliseconds."* **Part 1 — the remaining snap. The dispatch brief's own two-candidate table (`spin` binding smooth=false, `bass_decay_rate` binding smooth=true/steps) was verified against `config/effect_params.json` and found factually correct but INCOMPLETE — it covered only `ValueBinding` resolution paths (scene-entry bindings, resolved once at fire time or re-rolled by dice), not the SECOND execution path a FlareKind's own `params` (`ParamTarget`, absolute/offset/random) travels: `scene_response._compute_param_moves`/`_move_params`, used by every permanent/momentary kind attached to a band. That path never checked the registry `smooth` flag at all — §75's fix only touched `_reroll` (the dice-reroll path). Proof this was live, not theoretical: `scripts/check_spectra.py`'s OWN prior assertion, written by §75 itself, explicitly documented the gap it was leaving open — *"the 0.7-1 bands' explicit 'star: 0.0' patch still wins over the dice re-roll and still lands as an instant jump — unchanged by the smoothing fix, which only touches PURE dice re-rolls."* STAR's own authored "Flare patch 0.7–1"/"Drop patch 0.7–1" kinds (permanent, `star: 0.0` AND `spin: 0.55`) fire on every ordinary flare/drop at intensity ≥0.7 and land via this exact unfixed path — `star` (registry smooth=true) snapping instantly on every one of them, exactly the "player still causing it to snap" he's watching. **`spin` (candidate 1) confirmed as tabled**: `signal="trigger_intensity", mode="map"`, `smooth=false` — resolved once at scene-fire time (never re-rolled, since dice-reroll only touches `signal="random"` bindings), and ALSO explicitly re-targeted by these same "Flare/Drop patch 0.7–1" kinds — jumping there was correct per its own tag, not a bug; whether it should be bound to a continuously-changing signal at all is a product question this task does not decide. **`bass_decay_rate` (candidate 2) confirmed as tabled but currently inert**: `signal="trigger_intensity", mode="steps"`, registry smooth=true on `power` — but grep of STAR's live `flare_kinds`/`responses` shows NO kind anywhere targets `bass_decay_rate` via `ParamTarget`, and its own scene-entry binding resolves once at fire time like `spin`'s — so nothing currently re-writes it more than once per scene fire. The registry's smooth=true tag on it is real but presently unexercised: a latent gap (would misbehave under the OLD `_move_params` the instant a future kind targets it), not a live snap today. **Fix**: `scene_response._move_params` (called only from `on_event`, the band-driven flare/charge/lull/drop path — `_move_params_ramped`, used by `on_update`, was already unconditionally a glide and needed no change) now splits each kind's landed params by the SAME registry smooth verdict `_reroll` already uses, routing smooth numerics into the shared `glides` dict (`executor.glide(..., DICE_REROLL_GLIDE_MS)`) and everything else into `jumps` as before. Because a param's smooth verdict is a static per-(effect,param) registry lookup, dice and a same-event patch targeting the same key always land in the same dict — patch-overrides-dice precedence now falls out of plain `dict.update()` ordering (moves execute after dice in `on_event`), so the old "drop any dice-only glide a later patch also claims" cleanup is provably unreachable for that race and kept only as a defensive no-op, not deleted. **`spin`'s registry `smooth` flag independently retagged `false→true`** (`config/effect_params.json`), verified from source, not from his sentence wanting it (his follow-up push-back explicitly warned against retagging to match intent alone): `fx/effects/radial.py::config_updated` recomputes `self.spin = nonlinear_log(config['spin'], 2)/10` on every tween frame including mid-glide (`_apply_config(validate=False)`, the per-frame tween path), and `fx/utils.py::nonlinear_log = sign(x)*abs(x)**power` is continuous through zero and negative x — no rendering barrier to a smooth sign-crossing glide. Zero live scene binds `spin` via `signal="random"` (checked across all 9 scenes), so this retag has NO effect on `_reroll`'s dice-glide gate anywhere; it only changes whether an explicit `ParamTarget` patch on `spin` (this fix, and the two new kinds below) glides or jumps. **His follow-up message, verified line-by-line, correction delivered**: his own re-read of `config/effect_params.json` (`spin`: numeric, min 0/max 1, magnitude_only, default 0.2; `spin_sign`: toggle, maps_to spin, sign_control, "Toggle=negate sign") was byte-accurate — confirmed independently against the same file — but the conclusion drawn from it ("toggle spin_sign, that's the existing mechanism") does not hold under SPECTRA: `maps_to`/`sign_control`/`magnitude_only` translation exists ONLY in legacy `services/morph_compiler.py::_sign_control_patch`/`services/trigger_engine.py` (grep-confirmed absent from `spectra/`/`fx/`), and `fx/effects/radial.py`'s real `CONFIG_SCHEMA` has no `spin_sign` key at all — the only `spin_sign` name in that file is an unrelated local variable inside particle-handoff snapshot code, never read from `self._config`. Voluptuous's merged effect schema uses `extra=ALLOW_EXTRA` (`fx/utils.py`'s `schema()` classmethod), so a SPECTRA write targeting `spin_sign` would land as an inert, unvalidated extra key the effect never consults — legacy's own `morph_compiler.py` docstring names this exact shape: *"LedFX has no bool to receive, so writing the virtual name would be an inert no-op key."* Also corrected: the registry's declared `spin` range (`min:0, max:1`) is not what the real effect enforces (`radial.py`'s own schema is `[-1.0, 1.0]`) and is not even what SPECTRA's own clamp code enforces for a `ParamTarget` at the band's default ×1.0 scale (`_compute_param_moves`'s registry clamp only runs when `scale != 1.0`) — so a negative `spin` target does not "clamp to nothing or throw," it lands correctly once real writes go live (today's `RecordingExecutor` is DARK regardless). Net: his intent (reverse by flipping spin's sign) was exactly right; the correct target is `spin` itself, signed, never `spin_sign`. **Part 2 — the two reverse flares, checked before building.** Confirmed: Fireworks V2's existing "Reverse Direction" kind (Sonic, same day, `type: momentary`, `params: {reverse: {mode: absolute, value: 1.0}}`, `hold_ms: 500`) targets `reverse` — a plain boolean on `fireworks`/`fireworks1d` meaning implode-vs-explode, per its own registry note — a param `radial` doesn't have at all (checked `Radial2d.CONFIG_SCHEMA`: no `reverse` key). It cannot be reused verbatim on STAR; copying it in would be exactly as inert as a `spin_sign` write, for the same `extra=ALLOW_EXTRA` reason. **Reported as asked, plainly**: the general SHAPE of "momentary, 500ms, ParamTarget-driven" is not new work — Fireworks V2's own kind already proves that mechanism end to end (`hold_ms`, `flush_releases`'s unconditional release glide) — but its concrete PARAMS are scene-specific and had to be authored fresh for `radial`; a permanent flip and a 500ms momentary flip are also genuinely two different things (CARRIES vs. RETURNS), not one kind wearing two names. Built as two new named `FlareKind` entries on STAR — "Reverse Direction" (`type: permanent`, `spin: {mode: absolute, value: -0.55}`) and "Reverse Momentarily (500ms)" (`type: momentary`, `hold_ms: 500`, same target) — `-0.55` is the negation of his own already-authored "Flare patch 0.35–0.7"/"0.7–1" spin magnitude, matching his existing absolute-value convention rather than inventing a new one. Neither carries a `jump` field — `FlareKind._shape` structurally forbids `jump` on a non-`drift_jump` kind — so neither can re-roll STAR's dice-bound `star`/`edges`; both target only `spin`, which no dice binding touches, so the interaction this task was warned to mind (a new 500ms door back into the snap) does not exist by construction, verified in the executable spec below, not merely argued. Both kinds now glide (not jump) when fired, per the Part 1 fix + retag above — a "reverse" that snapped would have reopened exactly the door this task closed. **Declared, not band-attached — matching precedent, not an oversight**: Fireworks V2's own "Reverse Direction" is itself declared in `flare_kinds` but attached to zero bands (`scene_console.apply_flare_kind` only ever touches `flare_kinds`, never `responses`/`band.kinds` — a hard scope boundary; band attachment is the Scenes page's own click-to-attach chip on the band strip, a human action). This build follows the identical shape: the two kinds land available on STAR, ready to attach; which band(s) and at what scale is left to him. **Migration**: `scripts/add_star_reverse_flares.py` (dry-run default, `--apply`, idempotent — re-running with the same shape is a no-op) goes through `spectra/services/scene_console.apply_flare_kind` — the SAME write path Sonic already uses — rather than a raw-JSON patch, since adding a `FlareKind` (unlike a single-scalar-field edit) is exactly what that function exists for: full model validation, a whole-scene re-validate, and a verified pre-write backup (`scene_console._write_and_verify_backup`) all come for free. Verified end-to-end against an isolated copy of his real live scene data (fetched via `GET /spectra/api/scenes` earlier in this task, never written back): dry-run, apply, idempotent re-apply, and a full before/after diff proving only STAR's `flare_kinds` grew by exactly these two entries — every other scene and every other field on STAR byte-identical. **Not run against his live `:8010` storage by this build** — an operator/deploy step, same convention as `seed_star_strips.py`/`set_scene_colorset_preference.py`. **Verification**: `scripts/check_spectra.py` updated — the twist/spin/star patch-broadcast checks now assert glide (not jump) with the correct `DICE_REROLL_GLIDE_MS` duration; the Mid-Group "top flare band executes its patch" loop now accepts either transport (several of the seven scenes' own top-band patches — `beat_burst`, `spawn_rate`, `burst_size` — are already registry smooth=true and now correctly glide too, not just STAR); `_no_flare_moves_star_edges`'s final assertion is corrected from "still jumps" to "now glides," with a new companion assertion that the SAME top-band patch's `spin` write also glides; a new self-contained section proves both reverse kinds' structure, glide transport, correct target value, carry-vs-return semantics, and star/edges non-interference directly. Four pytest tests broke and were fixed, all the same class: `gradient_scale`/`power_multiplier` on the test fixtures' `concentric` effect are registry smooth=true, so patches the tests previously asserted landed as an instant jump now correctly ease over `DICE_REROLL_GLIDE_MS` — each test now renders past that duration before reading `effect._config` (`tests/test_spectra_engine.py::test_surge_jump_lands_next_frame_and_carries`, renamed `test_surge_patch_eases_and_carries`; `test_flare_kinds_semantics_on_the_harness`; `test_momentary_target_expressions_and_chosen_hold_on_the_harness`; `tests/test_trigger_engine.py::test_trigger_fires_response_action_on_the_real_pipeline`). Full suite: 658 collected, 0 failed, 2 skipped (pre-existing live-model smoke tests, no API key in this environment). `check_spectra.py` itself still fails at ONE known pre-existing point, "Black Hole V2/flare: reroll flag mis-migrated" against this worktree's own stale `storage/spectra/scenes.json` — confirmed identical on unmodified `master` via `git stash` before touching anything, unrelated to this change, matching the documented environmental-failures pattern; every check before AND after that point (verified via an isolated skip-probe re-run of the same script, never committed) passes, including every check this PR adds or edits. **A caveat worth repeating**: this worktree's local `storage/spectra/scenes.json` was found genuinely pre-`flare_kinds`-migration (a stale, old-schema snapshot, 0 raw `flare_kinds` keys) — an early attempt to refresh it from live GET data for convenience broke unrelated seeder-fixture tests further down `check_spectra.py` that specifically depend on that file holding the S1 seeder's RAW, pre-migration shape; the refresh was reverted and the file confirmed byte-identical to how it was found before this task touched anything. All new verification against CURRENT live scene shapes used a separate, disposable copy built from a fresh `GET /spectra/api/scenes` fetch, never overwriting this worktree's own fixture file. **Room-unproven**: no scene was fired, no write reached live storage or hardware — his lights were being watched live during this task per its own hard limits; both fixes are proven at the mechanism layer (real `_move_params`/`_compute_param_moves`/executor code paths against his real STAR scene's real binding/flare-kind shapes) and via the executable specs, not by watching STAR play smoothly or reverse in his room. |
| §79 | Force Scene: enabling the pin (or repinning while enabled) now fires immediately | `FIXED` — PR fm/spectra-force-scene-immediate, offline-verified only, no live write per this task's own hard limits | His live report, verbatim: *"I'm trying to test black hole in light mode. However I am using [Force Scene] to do it and nothing is happening. currently there's a song playing with no Triggers on it yet it is recapturing. But when I force a scene I want it to immediately activate that scene."* Live-reproduced before touching anything: `display_mode: light`, `force_scene_enabled: true`, `force_scene_scene_id` = Black Hole V2, active scene still Orbits V2, `deferred_by: None`. **Root cause, confirmed by reading the code, not re-derived**: §31's Force Scene is a *redirect*, not a *cause* — `scene_sequencer.fire_scene_by_id`'s own docstring already said so ("redirects scene_id here"), and it only runs when something else was already about to pick a scene. Enabling Force Scene also sets `bridge.sequencer_deferral()` to `"force_scene"`, which stops the sequencer's own rolls — so on a song carrying no triggers and no analysis, nothing was ever going to fire, and the redirect never got an occasion to run. This is not a mode-availability regression: `fire_scene_by_id` already exempts a Force-Scene-pinned fire from the `display_availability` gate (the same "an explicit pin keeps its declared life" rule pause/dinner_party/ambient already follow), confirmed by reading that gate before assuming otherwise — a dark-preferring scene pinned while the room is in Light mode is not silently filtered. **Fix**: `spectra/services/room_controls.reconcile_force_scene_if_changed`, the same one-choke-point PUT-triggered shape as `reconcile_ambient_if_changed`/`reconcile_dark_light_if_changed` (§52/§9) — called from `PUT /api/room-controls` after the new state is saved. Fires the pinned scene through `scene_sequencer.fire_scene_by_id` immediately on the edit that changed the pin: `force_scene_enabled` flipping false→true, or `force_scene_scene_id` changing while already enabled. Deliberately does **not** re-fire on an unrelated field re-save (e.g. the brightness slider) while an already-enabled pin stays on the same scene — a scene shouldn't jump every time he touches a different control, same discipline the other two reconciles already use. Intensity for the immediate fire reads live `bridge.intensity()` (the section energy at the current playback position, the same value an automatic pick would have used), falling back to the stated 0.5 neutral when the bridge/analysis has nothing (exactly his own case — a song with no triggers yet). **"If it refuses for a good reason, it must say so" — his own bar, met structurally**: the function always returns a dict naming what happened rather than `None` on refusal (unlike the other two reconciles, where `None` means "nothing to do") — `{"status": "fired", scene_id, scene_name}`, or `{"status": "skipped", "reason": "no scene pinned"}` / `{"status": "pinned scene not found"}`, or `{"status": "error", "reason": ...}` if the fire itself raised. Folded into `PUT /api/room-controls`'s response as `force_scene_result`, mirroring `ambient_result`/`dark_light_result`. Frontend: `RoomControlsBar.tsx`'s Scenes panel shows a badge under the scene picker naming the outcome (`ForceSceneResult` in `types.ts`); `helpContent.ts`'s `force-scene` entry rewritten to lead with "fires immediately," not just "holds." Settings-console (Sonic) is unaffected — `force_scene_*` was already deliberately excluded from its registry (§ settings console docs: "a picker action, not a voice setting"), so no wiring was needed there. Tests: 7 new cases in `tests/test_room_controls.py` (fires on enable, fires on repin, ignores an unrelated field re-save, ignores disabling, states a reason for no-pin and for a missing scene, uses live bridge intensity when available) plus a matching block in `scripts/check_spectra.py`'s existing Force Scene section. Full suite: 674 passed, 5 pre-existing/environmental failures unchanged from `master` (`tests/test_crystal_activation_verify.py` ×3, `tests/test_settings_agent_cli.py` ×2 missing the `mcp` package — confirmed identical via `git stash` before this work), 2 pre-existing skipped. `scripts/check_spectra.py` has one known pre-existing environmental failure ("STAR: raw seeder output still carries the pre-freeze dice pairs," this worktree's stale `storage/spectra/scenes.json`) confirmed identical on `master` via the same `git stash` check; every check this PR added passed. **Room-unproven by this task's own hard limits**: firstmate owns every deploy/restart, and live SPECTRA (`:8010`) was read-only for this task except where a window was granted — the mechanism is proven offline (real `fire_scene_by_id`/`scene_compiler.fire_scene` code paths, fake-clocked) and the reproduction above was read-only against his live state, never written to. |
| §80 | Temporary scene disable — a manual, reversible toggle; disabled beats mode availability; Force Scene still fires it but NAMES the override | `BUILT` (full), room-unproven — PR fm/spectra-scene-disable, verified offline + against an isolated instance on a spare port (phone-eye-checked at 390×844) | His ask, verbatim: *"add an ability to disable a scene temporarily."* **Reading, stated rather than picked silently**: "temporarily" read as (a) a manual toggle he flips off and later flips back on — reversible, no expiry — not (b) a genuine timer/auto-restore; nothing in his existing data or UI (Mode availability, Force Scene, every other scene field) suggested a timed mechanism, and building one unasked would have added a whole expiry model he never requested. `SceneV2.disabled: bool = False` (`spectra/models/scene.py`) — every existing scene loads unaffected. **Interaction with Mode availability (§9), checked rather than assumed**: disabled is the STRONGER, more explicit statement ("don't use this scene, period" vs. "not in this room mode"), so it is gated FIRST at every choke point — a scene that is both disabled and mode-gated reports `skipped="disabled"`, not `"mode_availability"`. Wired at the exact same three points §9 already established: the hard gate `scene_sequencer.fire_scene_by_id` (once, for every automatic caller), the sequencer's own candidate pool (`SceneSequencer._roll`'s `available` set, new `_scene_enabled`/`_default_scene_enabled` callable — kept SEPARATE from `_scene_mode_available` rather than folded in, matching this codebase's own precedent of keeping orthogonal gates as distinct functions, e.g. colour-set preference vs. availability), and `trigger_engine._default_select_scene`'s generated-trigger draw. A manual editor Fire/test-fire (`POST /scenes/{id}/fire` → `scene_compiler.fire_scene` directly) bypasses the gate entirely, unchanged code path, same as it already bypasses Mode availability. **Interaction with Force Scene (§31/§79), the refinement weighed rather than adopted blind**: the brief proposed "the more recent explicit act wins" and asked it be argued, not assumed — agreed and built as proposed: a pin on a disabled scene is honoured (`fire_scene_by_id`'s existing `forced` bypass, unchanged), but the result now carries `overrode_disabled=True` when the pinned scene is disabled, threaded through `room_controls.reconcile_force_scene_if_changed` into `force_scene_result.overrode_disabled` — silently allowing OR silently refusing were both rejected as options; naming the contradiction was the only one of the three that doesn't hide something from him. Deliberately did NOT extend this same "name it" treatment to Force Scene's pre-existing silent bypass of Mode availability (§9/§31) — out of scope, a different established behaviour this task wasn't asked to touch, and changing it would be an unrequested behaviour change smuggled into a disable feature. **Visible on every surface a status would be checked, per the task's explicit requirement, not left to a detail panel**: a red "⛔ disabled" badge on the scene-list row (`ScenesPage.tsx`), the phone header's compact scene selector when that scene is open, a `DisabledToggle.tsx` toolbar control (same fixed-width-per-state discipline as `ModeAvailabilityToggle.tsx`, red when active) next to the Mode availability toggle, the Force Scene scene picker's option label (`⛔ {name}`), and a red "⚠ overriding disabled scene" badge on the room bar's Force Scene panel when the override fires. Help: new `scene-disable` entry in `helpContent.ts`, cross-linked from both `mode-availability` and `force-scene`. **Phone-eye-checked** (chrome-devtools-axi, Playwright Chromium headless, 390×844, against an isolated `python -m spectra` on a spare port with this worktree's own isolated storage — his live `:8010` untouched throughout, confirmed alive after cleanup): the scene-list badge, the toolbar toggle (flip → save → round-trip confirmed via `GET /scenes/{id}`), and the full Force-Scene-overrides-a-disabled-scene flow (checkbox toggle in the real UI → "FIRED: …" + "⚠ OVERRIDING DISABLED SCENE" badge, matching a direct API proof of the same PUT) all rendered correctly with no horizontal overflow. Tests: `tests/test_scene_disable.py` (10, new) — the field default, `fire_scene_by_id`'s skip/fire/override-naming behaviour, disabled-beats-mode-availability in the reported reason, the sequencer roll's candidate-pool exclusion, `_default_scene_enabled`'s own semantics, `trigger_engine`'s generated-trigger draw exclusion, and the manual-fire bypass — plus `tests/test_room_controls.py`/`tests/test_mode_availability.py` re-run for regression (all green, unmodified). Full suite: **686 passed, 5 pre-existing/environmental failures, 2 skipped** — the identical five confirmed via `git stash` in this same environment before this work (3× `test_crystal_activation_verify.py` needing seeded live device topology, 2× `test_settings_agent_cli.py` missing the optional `mcp` package); `scripts/check_spectra.py`/`scripts/check_triggers.py` both re-run, only pre-existing/environmental output unchanged (diffed byte-for-byte against a `git stash` baseline bar one log line's memory address). `tsc --noEmit` and `vite build` both clean. **Room-unproven**: no scene was fired and nothing was written to live storage or hardware — firstmate owns every deploy/restart and he was using live SPECTRA (`:8010`) throughout this task; every proof above ran against a disposable, isolated instance on a spare port with its own storage, never his live process. |
| §81 | Flare kind edit box (rename/delete/copy), cross-scene paste, and drag-to-lane band attachment | `BUILT` (frontend-only) — PR fm/spectra-flare-lanes-and-edit, merged (#129, code-verified 2026-08-19); phone-verified via an isolated instance, no write reached his live `:8010` | His words: *"On the scenes tab, under flares, I want to be able to double click a flare and have it pop up an edit box that allows me to rename it, delete it, or copy it. That means we also need to be able to paste it on a different scene. Furthermore I want to be able to drag these into different vertical lanes; each vertical lane picks an action, and they combine. start with 2 and allow adding up to 2 more."* Read as flares, not scenes, despite his literal "drag these scenes" — nothing in his ask or the code supports a scene-lane concept, and everything (double-click on "a flare", paste "it" onto "a different scene") is about the flare kind. **Established before building, per this task's own instruction: a flare kind is FULLY SCENE-SCOPED** — `SceneV2.flare_kinds: list[FlareKind]` (`models/scene.py`), no cross-scene id, referenced only by name within that one scene's own `responses[*].bands[*].kinds` dict. So paste is a genuine PORT (a fresh, independent entry on the target scene, deduped-by-name via the same `" (2)"` convention `_migrate_flare_kinds.declare()` already uses), never a live link — editing or deleting either copy afterward never touches the other. Zero backend/model change was needed for any of Parts 1–3: rename/delete/paste/lane-attach are all edits to fields the model and its validation (`SceneV2._validate`: duplicate names, dangling `band.kinds` references) already governed: the frontend PUTs the whole scene through the SAME save path every other edit here already used, so the existing validator is the safety net against a cascade bug, not a new one written for this. **"Combine," the ask's load-bearing word — the existing composition rule was found, not invented**: `band.kinds: dict[name→scale]` already lets MULTIPLE kinds attach to one band (§ "Named flare kinds" above), and `scene_response.ResponseEngine.on_event` already executes them in a FIXED order per family (dice → permanent params → momentary params → permanent gains → momentary gains → colour jump — the legacy reroll→patch→gain→colour order, generalized) with a DIFFERENT composition rule per family, none of it exposed as a user toggle: dice re-rolls and colour jumps are each a SINGLETON pick per fire (`dice[0]`/`colours[0]` — a second one attached is harmless but inert, "a jump is a jump," "one selector roll per fire"); momentary/permanent PARAM moves OVERWRITE (permanent lands before momentary so a spike shows over the just-set baseline; among same-type kinds targeting the same param, the LATER one in `band.kinds`' own dict order wins, via plain `dict.update()` — the same mechanism that already makes an explicit param patch beat a same-event dice re-roll, generalized, not a special case); permanent GAIN kinds CHAIN multiplicatively when more than one lands in the same band (each reads the prior one's landed brightness off `carry` before computing its own effective multiplier) — this is the flare engine's own unadvertised answer to the "overwrite vs additive" question his colour-set `bg_mode` asks explicitly elsewhere; it was never one uniform rule, and lanes had to expose that honestly rather than pick one. **Lanes are UI over the existing `band.kinds` dict, not a new stored concept**: a lane is a POSITION in that dict — `FlareLaneRack.tsx` renders each band's kinds at their dict-insertion index as a draggable slot (2 to start, a "+" grows to 4, never hiding a band that already carries more from legacy data or an agent edit); dropping a kind INTO a lane inserts-and-shifts (never swaps, never silently loses an occupant); dragging FROM the palette is an ADD (a kind may already sit in other bands — untouched); dragging FROM an occupied lane is a MOVE (detaches its old spot, attaches at the new one, carrying its scale) — `flareKindOps.moveKindToLane` (pure function, `scenes/tabs/flareKindOps.ts`) implements this, proven end-to-end against a real isolated instance (attach, cross-band move-with-shift, and delete-with-cascade all confirmed by reading the persisted `scenes.json` after each step, not just the UI). Because lane order IS dict order, and dict order is what the engine's own tie-break above reads, dragging a kind into an earlier or later lane is a real, meaningful control over "which same-param kind wins" — not cosmetic. **Rename/delete/copy got a direct UI, deliberately narrower than the settings-console no-forms rule**: those three are identity operations on the kind's NAME (structural), not on its `type`/`params`/`gain`/`hold_ms` (program behaviour) — the edit box (`FlareKindEditDialog.tsx`, tap or double-click, same gesture on phone and desktop since a real double-TAP is unreliable on touch) leaves the latter agent-adjustable exactly as before; the two categories are visually and functionally separate, not blurred by this build. Rename cascades into every `band.kinds` key and `update_kind` reference that named the old string (dict key SWAP, not delete+re-add, to avoid silently reordering lane precedence as a side effect of a rename); delete cascades the same way and its confirm states the live attachment count so a widely-attached kind is never removed as a surprise. Copy/paste: `lib/flareClipboard.ts`, localStorage-backed (survives reload, works across scenes and tabs), a "📋 Paste" button appears on any scene's Flares/Charges-Lulls-Drops tab whenever a kind is copied. **Phone-first, verified as touch**: built entirely on the codebase's own established Pointer-Events-plus-`touchAction:'none'` drag idiom (`BandStrip.tsx`, `SpectraTriggerBar.tsx` — no new dependency), then exercised at a real 390×844 viewport (chrome-devtools-axi via an isolated Playwright Chromium, touch+mobile emulation) against a synthetic scene on a disposable instance on a spare port (`SPECTRA_STORAGE_DIR`/`SPECTRA_PORT` repointed, never his `storage/spectra/`) — every gesture (tap-to-edit, drag-to-attach, drag-to-move-with-shift, rename, delete-with-cascade-confirm, copy, paste onto a second scene) proven by reading the resulting `scenes.json`, not just watching the UI. **Not run against his live `:8010` storage or process** — an operator/deploy step, same convention as every other frontend-only Scenes-page change in this ledger; his live instance was read-only throughout (one `GET /spectra/api/scenes` to ground the Part 4 proposal below in his real data, no writes). Help: `helpContent.ts` gained `flare-kind-edit-box` and `tab-flares-lanes`, and rewrote `flare-kinds`/`tab-flares` to point at them instead of describing the retired flat chip-row UI. Tests: 0 Python files touched, so `.venv/bin/python -m pytest` is unaffected by this PR — 3 pre-existing failures (`tests/test_crystal_activation_verify.py`, the same file §79 already logged as pre-existing/environmental) confirmed via `git status` showing only frontend files changed, not re-derived. Frontend has no test runner in this repo (`web/package.json`'s own `build` script is `tsc --noEmit && vite build`); both `spectra/web` and `web` typecheck and build clean. **Part 4 — intensity-bucket MANAGEMENT, proposed, not built** — see OQ-13. **Follow-up, 2026-08-19, PR fm/flare-lanes-one-flare-per-lane-firing-to-1gp2 — his re-ask weighed and resolved, not built or dismissed blind (Admiral edit, 2026-08-19)**: his words, verbatim: *"I like the start on the flare cards. I am not sure the lanes are working properly. I would like them in vertical lanes that each fire. this allows a color and a shape flare to happen at the same time. one flare is chosen per lane. this is similar to the shape and color sections of scenes in spotfx. let me know if what you built is better and i did not understand it."* **Established from the engine before arguing either way, per his own explicit invitation to disagree**: `scene_response.ResponseEngine.on_event` already executes EVERY kind attached to a fired band — nothing is chosen — so his stated GOAL (a colour flare and a shape flare landing together) was already delivered; his stated MECHANISM (several candidates per lane, one CHOSEN at fire time) was genuinely missing, and would be a real port of legacy's `services/trigger_engine.py::_pick_morph_lanes` (`morph_set`/`scene_update`'s own named First/Rest/Shape/Color/Charge/Lull/Drop lanes: one weighted pick per lane, every lane's pick fires together, an empty lane is silently skipped, each candidate `Action` carries its own `weight` distinct from any scale). Both findings were reported plainly, neither side picked for him. **His decision, verbatim**: *"I think i like the current method better than mine, except lets visually organize each set into vertical lanes."* He kept the all-attached-fires mechanism and declined the per-lane weighted-selection build — **that selection mechanism is NOT built**, and `scene_response.py`/`models/scene.py`/`flareKindOps.ts` are untouched by this task; the finding that made the choice available to him is what this addendum records, not a new capability. **What shipped is presentation-only**: `FlareLaneRack.tsx`'s lane cells were wider than tall (108×54, a flex-wrap row of squarish chips) despite `helpContent.ts`'s own pre-existing `tab-flares-lanes` entry already calling them "vertical LANES" — the visual didn't match the name, which is the most direct reading of "not sure the lanes are working properly" now that the firing mechanism itself is confirmed unchanged and working as designed. Lane cells are now genuinely tall (108×124, name pinned near the top and the scale/detach controls anchored near the bottom), and the rack scrolls horizontally instead of wrapping (`overflowX:'auto'`, `flexWrap:'nowrap'`) — the same "contain the overflow, don't force content narrower than it needs to be" shape already used for the spec viewer's tables and the device-preview strip's collapsed mode. Zero backend change, zero change to `moveKindToLane`'s attach/move/shift semantics, zero change to which kinds fire or when — same `band.kinds` dict, same drag mechanics, restyled only. Help: `tab-flares-lanes` gained a clause naming the scroll-not-wrap behaviour at phone width. Tests: 0 Python files touched. Frontend: `tsc --noEmit && vite build` clean (`spectra/web`). **Room-unproven, and this time deliberately so**: an attempt to phone-verify against a disposable isolated instance (`SPECTRA_STORAGE_DIR`/`SPECTRA_PORT` repointed to a spare port) went wrong — `pkill -f "python -m spectra"` used to stop that isolated instance pattern-matched his REAL live `spectra.service` too (both processes' argv are the literal string `python -m spectra`; only the interpreter path under it differs) and sent it SIGTERM. systemd restarted it in about a second (`resume: live stack reactivated — spectra drives the whole room`), and it re-confirmed healthy and live-writing on a follow-up read-only `GET /engine/status` — but a real scene fire and a Hue entertainment-stream reconnect happened as a side effect of that restart, on his actual room, which was not requested and is not undoable. Flagged to firstmate rather than attempted a second isolated run; see the incident note added to this repo's `AGENTS.md`/`CLAUDE.md` maintenance section so no future agent reaches for a bare `pkill -f "python -m spectra"` in this environment. |
| §82 | Intensity-scaled scene transition times, and transitions/momentary flares that LAND ON the trigger rather than starting on it | `BUILT` (full), room-unproven — PR fm/spectra-transition-timing-alignment, verified offline against unit/frame-level tests + a real read-only `check_triggers.py` pass, no write reached his live `:8010` | His words: two Inspector settings, "max scene transition time" and "min scene transition time," named 200ms/300ms; scale transition time by intensity, linearly; "the mid-transition animation arrives at the same instant as the trigger, which means the transition starts early... for transitions without such a mid-point, use halfway through the transition"; the same alignment applies to momentary flares — "the first switch must finish on the trigger, then the hold, then the flip back after the trigger mark." **His own pointer ("this is how it worked in the old SpotFX") was the whole mechanism, verified against the real files**: legacy's `services/transition_phases.py` fires a phased effect switch (radial⇄blackhole/orbits/fireworks/squiggles/dancer/eye, pacman→particles — `fx/effects/particle_handoff.py`'s `BLOOM_START`/`PACMAN_MORPH_START = 0.45`, the SAME vendored choreography SPECTRA's `fx/` already carries verbatim) `anchor_frac × crossfade_ms` early so the payoff phase lands on the trigger — ported near-verbatim to `spectra/services/transition_phases.py` (same registry, same 0.45s, same `MAX_LEAD_MS=5000` cap). **One confirmed, deliberate departure from legacy, not a misreading**: legacy's own `anchor_frac()` returns 0.0 (no lead at all) for an unregistered pair — it never applied a universal midpoint, only the registered phased ones. His ask for SPECTRA generalizes that: every OTHER scene transition still lands its plain 0.5 MID-POINT on the trigger. That generalization is applied by the CALLER (`trigger_engine._scene_transition_lead_ms`), not baked into the ported module, so the port itself stays byte-diffable against legacy's actual behaviour. **The inverted numbers, wired the physically-sensible way, stated plainly**: his named 200ms/300ms are backwards for "max"/"min" read as magnitudes (200 < 300); built as low intensity → the LONGER 300ms transition, high intensity → the SHORTER 200ms one — `RoomControlState.scene_transition_ms_gentle` (300, intensity 0.0) / `scene_transition_ms_hard` (200, intensity 1.0), named for what they represent (matching `scene_response.py`'s own pre-existing `COLOR_JUMP_RAMP_MS_GENTLE`/`_HARD` and `UPDATE_RAMP_MS_GENTLE`/`_HARD` naming, the same shape used a third time here, not invented) rather than his ambiguous "max"/"min" labels — both plain settings, in `settings_console.SETTINGS_REGISTRY` (Sonic/voice-editable, "Inspector settings he can swap without a code change"), so whichever way he confirms costs nothing. `room_controls.scene_transition_ms()` is the shared linear interpolation, consulted by `scene_compiler.fire_scene` as a NEW third tier in the entry-ramp fallback chain: a scene's own `entry_ramp_ms` still wins when authored; `room.global_transition_ms` (an explicit flat override, if he's ever set one nonzero) still wins next, unchanged; only then the new intensity-scaled default — his LIVE room's unmodified defaults (`entry_ramp_ms=0`, `global_transition_ms=0`) mean this ships as the active behaviour on deploy, not a dormant option. **Two anchors, deliberately different, matching his own distinction rather than collapsing them**: a scene transition's mid-point (0.5 fallback, or a registered phased pair's own 0.45) lands on the trigger — proven at the frame level by rendering a real glide and confirming it is strictly IN FLIGHT (neither start nor end value) at the trigger's own timestamp, landing only `crossfade_ms/2` after it (`tests/test_trigger_engine.py`'s new proof 9). A momentary flare's FIRST SWITCH lands its END on the trigger — only a param move onto a registry-`smooth` target actually takes time to switch (`scene_response.DICE_REROLL_GLIDE_MS`, the same fixed 220ms constant an ordinary dice re-roll already glides over); an instant jump (non-smooth param, or a momentary GAIN's spike — `_gain` always jumps its spike, never glides) already finishes at fire time with zero lead needed, so `scene_response.momentary_switch_would_glide()` (a new, read-only, execute-nothing peek mirroring `_move_params`'s own smooth gate) decides case by case. Frame-proven (proof 10): the switch's glide lands exactly on the trigger, holds through `PULSE_HOLD_S`, and only flips back to baseline well AFTER the trigger mark — never before it, never at it. **Legacy's own conservatism is carried forward, not re-derived**: an UNRESOLVED scene pick (`scene_id=None` — the sequencer kernel or a `scene_pool` decides at fire time, not now) gets NO lead rather than a guess, the same rule legacy applied to an unresolved random branch. **Architecture note — SPECTRA's `tick()` is reactive (fires the instant `position_ms` crosses a trigger's timestamp), unlike legacy's forward-planned timeline, so "firing early" means shifting the crossing check itself**: `TriggerEngine.tick()` now computes, per trigger within a cheap proximity gate (`MAX_LEAD_MS` lookahead), `fire_at = trig.timestamp_ms − lead_ms(trig)` and fires on `(last, position]` crossing `fire_at` OR the trigger's own unshifted `timestamp_ms` — the second clause is a SAFETY NET, not an optimization: `lead_ms` is recomputed fresh every tick from live state (the registry match against whatever effect is CURRENTLY on a target virtual), so it is not guaranteed monotonic tick-to-tick; a trigger still fires by its nominal moment at the latest even if an earlier tick's early-fire window was somehow missed. Scoped to stored-trigger fires only (`tick()`'s own crossing, both `fire_scene` and `fire_response` actions) — NOT the automatic song-transition fire (`_fire_transition`, no forward notice of an upcoming track change to fire ahead of) and NOT `scene_sequencer`'s own dwell-driven rolls (no trigger timestamp to land on); the DURATION scaling (`scene_transition_ms`) is universal to every scene fire regardless of origin, since it lives inside `scene_compiler.fire_scene` itself. **Deliberately NOT touched, to avoid conflating two different mechanisms**: `SceneV2.choreography` (`PhaseChoreography` — `enabled`/`transition_ms`/`anchor_frac` default 0.45/`transition_mode`) is a SEPARATE, still-unwired per-scene field (§28's own note: "phase choreography legacy's own SceneV2 defined but never wired into an execution engine") that this build does not reach into or repurpose — the brief never named it, and reusing its `anchor_frac`/`transition_ms` for this feature would have been an unrequested design guess rather than a read of his actual ask; §28's gap note stands unchanged. Tests: `tests/test_lead_time_alignment.py` (15, new — the ported registry, the interpolation + fallback chain, the momentary-glide peek), `tests/test_trigger_engine.py` (+2 frame-level proofs, 10 total in that file), `scripts/check_triggers.py` (+1 section, production-wiring proof against real scene/room/conductor state, no fx_seam/HTTP reached — a fresh-engine-with-injected-fake-fire-scene pattern established here specifically to keep this offline spec from ever attempting a live `dry_run=False` fire). Full suite: **703 passed, 5 pre-existing/environmental failures (identical set §80 already logged, confirmed via `git stash` in this same environment), 2 skipped** — zero new failures. `scripts/check_settings_console.py`/`check_triggers.py`/`check_spectra.py`/`check_drift.py`/`check_scene_v2.py`/`check_sequencer.py` all re-run clean (the one pre-existing `check_spectra.py` STAR-seeder failure, needing his live-seeded storage, confirmed identical via `git stash`). Help: `helpContent.ts` gained an `intensity-scaled-transitions` entry. **Room-unproven**: no scene was fired and nothing was written to live storage or hardware — firstmate owns every deploy/restart and he was using live SPECTRA (`:8010`) throughout this task. |
| §83 | "Goes from black to a grey or white, then changes colour" — root cause PINNED and FIXED | `FIXED` (full), room-unproven — PR fm/spectra-achromatic-saturation-fix, not yet merged, verified offline against his real color_sets.json data — his own words, corrected from an earlier (wrong) light-mode-background-leak report: *"I see now that it goes from black to a gray or a white and then changes color... the brightness is scaling from a gray tone and then the Hue is changing."* He later cut the follow-up question off — "just fix it" — so this was shipped as a defect fix, not brought back as a decision. | **FIX (2026-08-19, PR fm/spectra-achromatic-saturation-fix)**: `fx/effects/__init__.py::hue_tween_fields` now adopts the other endpoint's SATURATION for an achromatic (black/grey/white) endpoint, the same way it already adopted its HUE — so only VALUE ramps for that endpoint, matching the function's own docstring intent ("fades saturation in place"). Before/after on the same real pair (`#000000` → `#ff9940`, background_color from "Line - Green" → "Calm - Purple"): t=0.25 was `#403934` sat=0.187 (near-grey), now `#402610` sat=0.749 — **byte-identical to the plain RGB path (`mix_colors`) at every sampled t**, both fading IN from black and fading OUT to black (the symmetric case, since his colour journeys glide to black as routinely as from it). **Isolation proof, not just a claim**: the fix changes only the saturation field of `hsv_start`/`hsv_delta` — hue and value fields are proven bit-identical to the pre-fix formula for every pair tested (`tests/test_hue_tween_achromatic_saturation.py::test_hue_and_value_fields_are_unaffected_by_the_fix`), and the colour-to-colour crossing case (neither endpoint achromatic) is proven bit-identical before/after, including the separately-filed muddy cream→blue crossing itself (`#8b7e53`→`#009eff`, `data/spectra-grey-midpoint-transition/brief.md`) — that pair's own dip lives in `mix_colors` (the plain RGB path), a different function, not touched here, and remains open as its own backlog item (`spectra-muddy-colour-crossing`). Per-pixel gradient case also re-proven: the black-sourced pixel in "Line - Green"'s gradient now holds its target saturation throughout instead of ramping 0→1. `fx/VENDOR.md` deviation #13 records this as a vendored-file behaviour change. Tests: 5 new in `tests/test_hue_tween_achromatic_saturation.py`, all passing; full suite **720 passed, 5 pre-existing/environmental failures (3× `tests/test_crystal_activation_verify.py`, 2× `tests/test_settings_agent_cli.py` missing `mcp` package — confirmed identical failures on pristine `master` before this change, this task's diff touches none of those files), 2 skipped**. Not room-proven — no camera/live-instrument requirement exists for this fix (it's a property of the interpolation, reproducible at a desk, same as the original investigation below), and firstmate owns any live deploy/verification. **Investigation history preserved below.** Three hypotheses were checked against real values and his real data, in the order asked, before any fix was proposed. **Hypothesis 1 (RGBW white channel) — cleared for software, confirmed live for firmware.** Every device his real `fx-live/config.json` configures uses `sync_mode: "DDP"` (all 7 WLED units); `fx/devices/ddp.py`'s wire format is hardcoded RGB 8-bit (`DATATYPE = 0x0B`, 3 bytes/pixel, no 4th channel in the protocol at all) — SPECTRA's own code never computes or transmits a white byte, confirmed by reading every packet-encoding path (`fx/devices/{ddp,udp,packets}.py`); the one code path that CAN build a 4-byte DRGBW packet (`packets.build_drgbw_packet`, UDP sync mode only — none of his devices use it) hardcodes the white byte to literal `0`; the `rgbw_led` config flag SPECTRA detects per-device is stored but never read anywhere to change what's sent (grep-confirmed, one dead commented-out reference in `e131.py`). **Then room access opened mid-task and this was checked live rather than left as "config alone should settle it"**: `GET http://<ip>/json/info` on 3 of his real RGBW fixtures (`sconce-kitchen-left` 192.168.40.110, `porch-rail` 192.168.40.158, `sconce-kitchen-right` 192.168.40.113) all read `"rgbw": true, "wv": 2` — WLED's own firmware-side RGBW-auto-calculate mode is ON. This is a real, additive, second mechanism, entirely outside this repo: whenever SPECTRA sends a desaturated RGB value (from the mechanism below, or from the separately-filed colour-crossing dip), his 4 RGBW fixtures' OWN FIRMWARE will synthesize a white-channel contribution from it on top. **Hypothesis 2 (brightness and colour on separate glide schedules) — real, but not the mechanism for a scene-to-scene transition; real elsewhere.** For a scene fire (`scene_compiler.compile_scene`), brightness/background_brightness and gradient/background_color always land in ONE `config` dict per virtual (`_entry_config`/`_apply_set_colors`), sent as ONE PUT with one `transition_ms`/`easing`/`transition_blend` — `fx/effects/__init__.py::_advance_tweens` advances every key in that one call on the identical per-frame `t`, proven by reading the loop: no gap is structurally possible here. The SEPARATE-schedule mechanism is real, just elsewhere: `scene_response.py`'s flare/response engine (`on_event`) fires `_gain` (brightness, its own duration — `GAIN_GLIDE_S=0.8s` permanent glide, or an INSTANT jump for a momentary spike) and `_color_jump` (hue, `color_jump_ramp_ms(intensity)` = 150–2500ms) as genuinely separate `executor.glide()`/`jump()` calls, "colour last" by the module's own documented execution order — confirmed wired on 7 of his 9 real scenes (`Black Hole V2`, `Orbits V2`, `STAR`, `Fireworks V2`, `Dancers V2`, `Eye V2`, `Pacman V2` all attach a gain kind and "Colour Jump" to the same band). **The root cause, PINNED with real values (`scripts/check_hue_blend_achromatic_desaturation.py`)**: `fx/effects/__init__.py::hue_tween_fields` — the "careful" hue-arc blend `fx_seam`/`fx_executor` ALWAYS request for a real scene fire (his live `room_controls.json` has `global_transition_ms: 0`, but `scene_compiler.fire_scene`'s fallback chain, `scene.entry_ramp_ms or room.global_transition_ms or scene_transition_ms(...)`, treats Python-falsy `0` as unset, so it always lands on the nonzero 200–300ms intensity-scaled default; `fx_seam._body()`/`fx_executor._put()` both hardcode `transition_blend="hue"` whenever `transition_ms>0`) — is the mechanism that PRODUCES the desaturation it was built to PREVENT, whenever one endpoint of a transition is achromatic (black, at minimum — his own load-bearing convention: 30 real `color_sets.json` entries author `bg_color == "#000000"` exactly per §72's own black-background finding, plus 12 gradient entries carry a literal black stop within the gradient). For an achromatic endpoint, `hue_tween_fields` adopts the OTHER end's hue immediately and interpolates SATURATION and VALUE as independent linear scalars from 0 — `sat(t) = t × target_sat`, proven both algebraically and against a real pair from his data (background_color `#000000` from "Line - Green" → `#ff9940` from "Calm - Purple"): at t=0.25 the hue-path lands at `#403934`, sat=0.187 — nearly grey — while the PLAIN RGB path it replaced (`mix_colors`, a straight per-channel scale-up, which leaves the max/min ratio unchanged) lands at `#402610`, sat=0.749, on the IDENTICAL pair. Confirmed this is specific to achromatic endpoints, not a general hue-path defect: the same function on a pair where NEITHER end is grey (`#8b7e53`→`#009eff`, the separately-filed colour-crossing dip's own pair) shows monotonic, non-dipping saturation via the hue-path (0.403→1.000) — the hue-path correctly protects THAT crossing; it is specifically the achromatic-adoption branch that backfires. Verified this is per-pixel inside a real GRADIENT→GRADIENT fire too, not just a solid `background_color`: `hue_tween_fields` applied to real curves for "Line - Green" (a black-bordered gradient, `linear-gradient(90deg, rgb(0,0,0) 30%, rgb(0,255,7) 35%, rgb(0,255,7) 65%, rgb(0,0,0) 70%)`) → "Calm - Purple" shows the pixels sourced from Line-Green's black zones ramping saturation 0→1 proportional to t, while pixels sourced from its green zone stay at sat=1.0 throughout — no silent try/except fallback masking either behaviour (`hue_tween_fields` never threw across an exhaustive sweep of all 37,076 real ordered pairs drawn from his 197 stored colour/gradient/bg values — classify_param never fell to "instant" either). **The gradient-vs-solid routing question, checked directly and closed**: does NOT split his colour changes roughly in half as feared — both his 52 gradient-kind and 46 solid-kind real entries reach and complete the hue path identically when `blend="hue"` is requested (0 exceptions across the full sweep on either kind); the split he sees is not gradient-vs-solid, it is achromatic-endpoint-vs-not. **Live confirmation, room window opened mid-task**: fired his real "Line - Green" → "Calm - Purple" sequence via `POST /spectra/api/room-color/apply` — `executor.recent_writes` confirmed real, nonzero glide durations (641–2109ms observed, never instant) reaching his live virtuals, and confirmed his OWN automatic colour-journey conductor glides `background_color` to/from literal `#000000` as routine, continuous, ongoing behaviour (multiple such writes observed unprompted within seconds, e.g. seq 1080/1081 `background_color: '#000000'` over 1310ms) — the precondition is not an edge case, it fires constantly in his actual room. **What could NOT be settled this window, named rather than guessed**: no per-frame visual/pixel capture of a physical fixture rendering the muddy intermediate exists in this repo, and none was attempted from outside it (no camera). SPECTRA exposes no external HTTP read of a live effect's currently-interpolating `_config` (the facade's `/api/virtuals` is in-process only), and the shared `executor.recent_writes` ring buffer logs one entry per glide START (target + duration), not per rendered frame, so it cannot show an in-flight sample either — the same class of instrument-blindness §52/§63 already documented for Hue CLIP reads during a `dynamics`-ramped transition. What would settle it: a temporary high-frequency (~10Hz) poll of a live effect's `_config` value during a deliberately slow (multi-second) manual test glide, or a camera on the fixture during one. **Priority three of the room-open window (bulbs receiving ~277 vs ~500 at a real fire, tied to a separate intensity-scaling investigation) was explicitly declined this window** — it references a mechanism/claim outside this task's own scope and this agent's context, and attempting it without a precise specification of what "277"/"500" refer to risked a misleading result rather than a settled one; flagged back rather than guessed at. **No fix applied by this investigation** — see the FIX paragraph above for the remedy shipped the same night. `scripts/check_hue_blend_achromatic_desaturation.py` (read-only, no live storage/instance touched, all assertions pass — since updated to re-verify the fix rather than only the defect) is the reproducible proof. Full suite at investigation time: **712 passed, 3 pre-existing/environmental failures (`tests/test_crystal_activation_verify.py` — the same file §79–§82 already log as pre-existing, needing seeded live device topology), 2 skipped** — this task changed zero existing source files, only added the diagnostic script and this entry, so the 3 are unrelated by construction, not merely re-confirmed. **Room state**: two manual `room-color/apply` calls were made live (Line-Green then Calm-Purple) — left as-is rather than "restored," since his own automatic conductor already reselects a destination continuously on its own ~20s cadence (confirmed still cycling normally afterward, write sequence advancing under normal automatic load) and there is no separate "prior active set" to restore to that the conductor wasn't already about to replace anyway; `room_controls.json` fields were never written by this task and read back unchanged at the end (`display_mode: "default"`, `global_transition_ms: 0`, `scene_transition_ms_gentle/hard: 300/200`) — `ambient_mode` was observed to change on its own between reads (`"off"` → `"always"` → `"off"`) across this session, not caused by anything this task wrote; light ownership was independently observed transiting `handing-over` → `released` → `spectra`/`live` early in the session (not initiated by this task) and was `spectra`/`live`/healthy throughout the actual testing and at the end. |
| §84 | LOOKAHEAD — an unresolved `fire_scene` trigger's transition lands on the trigger too, by committing the scene pick early instead of leaving it unresolved forever | `BUILT` (full), room-unproven — PR fm/spectra-trigger-lookahead-lead, verified offline (pytest + a real `check_triggers.py` production-wiring pass), no write reached his live `:8010` | **§82 shipped dead for him**: `_scene_transition_lead_ms`'s own conservative rule ("no lead rather than a wrong one") returns 0 whenever `scene_id is None` — and correlated against his real trigger corpus, **0 of 22,013 `fire_scene` triggers ever carry a resolved `scene_id`**; every one leaves the pick to the kernel at fire time. Measured live: 100 real fires, every one at or after its stored timestamp, up to 3,144ms late, zero early — the feature never engaged once. His design, verbatim: "peek the upcoming trigger within a bounded horizon, resolve what that fire will actually do, compute the transition from that, and move the start moment so the midpoint still lands on the mark." Anchoring unchanged (scene mid-point / a momentary flare's first switch, per §82). **The horizon (ours to pick, per his ask) reuses `transition_phases.MAX_LEAD_MS` (5000ms) rather than a second, disconnected constant** — named `LOOKAHEAD_HORIZON_MS` in `trigger_engine.py`: no lead this feature (or §82's plain rule) can ever compute exceeds that cap, so a shorter horizon would arrive too late to cover the worst case, and a longer one buys nothing (no lead could use the extra notice) while only widening the window a pinned pick's validity could drift before fire. **The design does not actually predict anything — it moves the DECISION itself earlier, once, and never repeats it**: `TriggerEngine._pin_for` draws a real pick from the SAME kernel/pool functions `_fire()` would otherwise call, the FIRST time (and only the first time) a trigger enters the horizon; that pick is cached (`_PinnedPick`, positively or negatively — a "nothing to fire" draw is cached too, so it's never retried either, which matters: retrying a probabilistic kernel draw until it stops saying "nothing" would silently raise how often a trigger fires at all, compared to today's single draw) and `_fire()` reuses it VERBATIM. There is no second draw to disagree with the first, so "predicted vs. actual" can't diverge on WHICH scene fires by construction. **The residual risk his brief names explicitly — the world moving between commit and fire — is answered by `_pin_still_valid`**, a narrow, cheap revalidation (never a re-draw) checked right before a pin is used for either the lead calculation or the fire itself: the scene still exists and isn't disabled, is still available in the room's current `display_mode` (mirrors exactly what `scene_sequencer.fire_scene_by_id` itself would gate on for that scene id — the same production choke point, not a second hand-typed copy of its rule), and Force Scene hasn't changed since the pin was made (a force-scene change never risks the WRONG scene — `fire_scene_by_id` always applies the CURRENT redirect to whatever id it's handed, pin or fresh draw alike — but it would make the stored lead describe the wrong target's crossfade, a timing-quality concern, not a content one, tracked separately for that reason). **Any of those failing throws the pin away and degrades to exactly today's behaviour**: a fresh, real draw at (or after) the trigger's own nominal timestamp, zero lead — late, never wrong. A rewind or a song change clears every pin outright rather than trust one across an unbounded time skip. **The observability gap, confronted rather than assumed past**: this codebase has no live instrument that observes a real scene-entry ramp AT the moment of a fire — rendered frame averages sample too coarsely to catch the switching instant, and `executor.recent_writes` never records a scene fire's own writes (a separate investigation the same week found both blind, independently). Everything here is proven at the unit level (`tests/test_lead_time_alignment.py`, 9 new tests: resolve-once-and-cache, negative-result-never-retried, a resolved `scene_id` trigger never touches the pin cache, fire reuses the pin without redrawing, an invalidated pin degrades to a fresh draw and never fires the stale scene, `_pin_still_valid`'s three gates checked directly, rewind clears pins, song-change clears pins, the horizon constant itself — 24 total in that file) and at the frame level with a real rendered device (`tests/test_trigger_engine.py` proof 11: the pinned pick's effect config is what actually lands, the kernel drawn from exactly once across many ticks; a second scenario proves an invalidated pin never renders — the fresh fallback does, on the real device, not just asserted — 11 total in that file) and against real production wiring (`scripts/check_triggers.py` §9: the real `sequencer_store` config + `selection_kernel.select` reached through `_pin_for`, a real early fire through `tick()`, `_pin_still_valid` re-checked against real `scene_store` state after disabling the pinned scene). **Neither proves the visual claim — "the midpoint really lands on the beat" — against a live fire**, because nothing in this codebase can observe a live scene-entry ramp yet; that gap is §82's, inherited unchanged, not introduced here. Named, not silently assumed away: building that observation would mean extending `fire_history.py`'s already-hooked choke points (`scene_sequencer.fire_scene_by_id`) to timestamp the `fx_seam.apply_writes` call itself, then correlating that timestamp against the trigger's own stored `timestamp_ms` on a real song — a small, bounded addition (one new timestamped record per scene fire, no new choke point), scoped but not built by this PR. **`global_transition_ms=0` (cleared on his live room the same night, his own instruction) confirmed, by static reading plus the pre-existing `test_fire_scene_falls_back_to_intensity_scaled_bounds_when_nothing_else_is_set` test, to genuinely reach the intensity-scaled default** — `scene_compiler.fire_scene`'s and `_scene_transition_lead_ms_for`'s fallback chains are identical (`entry_ramp_ms or global_transition_ms or scene_transition_ms(...)`), so a scene with no authored ramp and a zeroed global override falls through to 300ms at intensity 0 / 200ms at intensity 1, exactly as §82 shipped it; a LIVE confirmation at a real fire needs the same not-yet-built instrument named above — his room window was closed for this task so nothing was fired to check it directly. Tests: `tests/test_lead_time_alignment.py` (+9), `tests/test_trigger_engine.py` (+1 frame-level proof), `scripts/check_triggers.py` (+1 section, §9). Full suite: **720 passed, 5 pre-existing/environmental failures (identical set §80/§82 already logged, confirmed via `git stash` in this same environment), 2 skipped** — zero new failures. Help: `intensity-scaled-transitions` gained a paragraph explaining, in plain language, that a trigger with no named scene now also starts its transition early, and what happens if the room changes its mind in between. **Room-unproven**: no scene was fired and nothing was written to live storage or hardware — firstmate owns every deploy/restart, and his room window was closed throughout this task. |
---

## Open questions

A question nobody is waiting on is not open, it's noise — every row here names what it blocks.
IDs aren't reused once resolved, so gaps are expected: **OQ-1** (scene-id pool fidelity) was
resolved by his ruling — see §44's note, not a row here. **OQ-6** (STAR's edge count) was
resolved the same way — see §56's note, not a row here. **OQ-7** (device-preview hidden-tab
auto-pause) was decided (built the report's own "yes" recommendation) — see §43's note, not a
row here. **OQ-5** (per-song "trigger slots") was resolved by his ruling — see §41's note, not
a row here. **OQ-12** (strip the 30 redundant-looking black backgrounds) was superseded — his
own proposed fix turned out to be unsafe in Hybrid mode, and he replaced the underlying idea
with a different, per-scene colour-set preference design — see §72's note, not a row here.

| ID | Question | Blocks | Source |
|---|---|---|---|
| OQ-2 | Raise the crystal WLED device's `refresh_rate` from 30 to match its siblings at 62 in `storage/spectra/fx-live/config.json`? | The root cause of his "crystal is behind on colour" report (confirmed live two ways: a virtual renders at its slowest device's rate). Distinct from §47's per-device-cadence workaround, which addresses the group-wide drag, not this specific value. | `data/spectra-crystal-colour-lag/report.md`. Reconfiguring a live device value — his word required even though it's instantly reversible. |
| OQ-3 | Approve, reject, or adjust the 7 proposed CSS/markup-only changes to `/feedback` and `/review`? | Any further build on §48 — the redesign PR (#65) implements these but he has never seen either page. | `data/spectra-feedback-review-design/report.md` + 4 screenshots. Both binding criteria (mark-then-nudge, single-batch queue) are preserved as-is regardless of the answer. |
| OQ-4 | Does he want quick-tag note shortcuts (emoji chips like drop/love/too-much) on the feedback page? | Whether that feature gets designed or built at all — it was deliberately left out of the §48 mock-up as an explicit guess about his taste, not a stated requirement. | Same report as OQ-3, flagged separately as not-asked-for. |
| OQ-8 | What actually caused the real stuck-bulb incidents (the historical 3-of-17-under-a-real-burst event §61's own docstring documents, and the two-bulb stuck-blue event that prompted the whole investigation)? The convenient explanation — a burst-rate cliff — was measured and retracted (D6); nothing has replaced it. | Whether PR #93's pacing margin (`AMBIENT_WRITE_STAGGER_MS` 50ms→300ms) is addressing the real cause or just adding harmless slack; whether another, uncharacterised failure mode can still strand a bulb despite the repair/false-unlit fixes. Not urgent to close given `repair_stragglers()` (§61) now recovers a stuck bulb regardless of cause, but the cause itself is genuinely unknown, not merely undocumented — do not present §61 as having explained it. | D6 above; §61; `data/spectra-hue-burst-drop-and-false-unlit/brief.md`; `data/spectra-ambient-holding-claim-stale-evidence.json`. |
| OQ-9 | Fix legacy `services/ambient_mode.py::_apply_hue`'s own 2xx-only Hue writes (same shape §64 just fixed in `release_fade.py`), and/or close the fact that its `_bridge_client` writes go direct to the bridge, bypassing `api.ledfx_client`'s S3 ownership gate entirely — a second writer the ownership record's own reconcilers (`ownership_reconciler.py`/`spectra_liveness_reconciler.py`) don't watch for? | Whether the "old world" Now Playing Ambient toggle can still silently fight SPECTRA for the same bulbs post-handover, and whether a legacy Ambient write can 2xx while the bulb doesn't move, with nothing to catch it — the exact D6 shape, just never checked there. | §64. `services/ambient_mode.py` (root-level, still wired in `main.py`/`routers/control.py`). Not fixed in §64: D4 holds legacy retirement gated, not scheduled, and the ownership-bypass half is a materially different, larger problem than the read-back gap. |
| OQ-10 | Should SPECTRA's Ambient gain an independent brightness control (a standalone slider, separate from the colour pickers), matching legacy's `settings.ambient_brightness` — found live 2026-08-16 while fixing §66, then explicitly superseded by his ruling that brightness should instead be DERIVED from the colour he picks? | Not urgent — his ruling settles §66's actual fix and needs no independent field. But legacy's slider was a real control (its own `SettingsPage.tsx` "Brightness (%)" row) that the SPECTRA Ambient port dropped entirely without him having asked for its removal — recorded per his own instruction, not discarded as a wrong turn, because it is plausibly a control he has not yet noticed is missing (e.g. dimming Ambient WITHOUT changing its colour at all, which HSV-Value-from-hex can never do — the palette of a single hue only spans the range that hue's own darkest/lightest shades reach). | §66. `services/ambient_mode.py::_light_payload` (legacy, `config.py`'s `settings.ambient_brightness`, default 100, 1..100%, independent of `ambient_color`); legacy's own `web/src/settings/SettingsPage.tsx` "Brightness (%)" range input. Not built in §66 — his ruling that day was specifically "derive from the colour," not "add both." |
| OQ-11 | Which of §67's two proposed scene_pool authoring/display shapes does he want — (A) an inline chip-row-plus-stepper on the trigger's own editor card, or (B) a separate named/shared "pool" object mirroring §10's Colour Set Groups full-page editor? Or does he want neither built yet, leaving scene_pool an agent/API-only field for now? | Whether §67's UI half gets built at all, and in which shape — the task that built §67 was explicitly data-and-test-only, no UI, by his own instruction. | §67. `data/spectra-hue-entertainment-areas` / `FavoritesPicker.tsx` (shape A's picker precedent); §10 `ColorSetsPage.tsx` (shape B's precedent). |
| OQ-13 | Intensity-bucket MANAGEMENT — his ask ("propose clean solutions to how to manage the intensity bucketing"), asked but deliberately not decided or built this task. Three options, each anchored to the bands he already has (§81 established the bands ARE the bucketing mechanism — none of these replace or duplicate it): **(A) Ship §81 as the floor, nothing more.** Cost: every scene still hand-authors its own band breakpoints from a blank strip — checked live, not assumed: all 9 of his real scenes' `flare` response class use the IDENTICAL shape today, `(0.0,0.35) (0.35,0.7) (0.7,1.0)` (`GET /spectra/api/scenes`, read-only) — reached independently, nine separate times, with nothing keeping them in sync; retuning the middle breakpoint everywhere is 9 manual edits today. Buys: zero new concepts, zero migration, nothing to hold two mental models of. **(B) A per-band `label: str|None` field** (e.g. "Quiet"/"Building"/"Peak"), cosmetic only — `select_band`'s numeric-range logic untouched, shown in the BandStrip/lane-rack headers instead of raw `[0.35–0.70)`. Cost: one small additive schema field (old scenes load unchanged, absent = today's numbers-only display). Buys: legibility once a band holds up to 4 lanes' worth of kinds, and an easier name to use when telling Sonic "retune the Peak bucket" — does not address the 9-scenes-diverging-silently cost (A) leaves open. **(C) A shared, named band-SHAPE profile** (boundaries only — `{intensity_min, intensity_max}` pairs, never the kinds/scale inside them, which stay scene-local by construction per §81's scoping finding) — the same "named profile, one edit retunes every scene using it" pattern already proven for curve profiles and drift profiles, with the same Detach/Promote UX `SequencingTab.tsx` already built and this task's own brief pointed at. Cost: a THIRD profile concept alongside curve/drift profiles for him to hold in mind; a real Detach/Promote UI to design (non-trivial — the curve-profile version was its own dedicated pass); a legibility risk if two scenes share boundaries but not kinds ("why did retuning THIS scene's Peak bucket range also move THAT scene's Peak bucket, when the flares inside are totally different?"). Buys: the one-edit-many-scenes retune (A)'s cost names directly — worth it only if he actually wants his 9 scenes' shared shape ENFORCED going forward, not merely observed as a current coincidence. **Recommendation, not a decision**: (A) is the honest default until he says otherwise — his 9 scenes converging without a shared object suggests convention, not an unmet need for enforcement; (B) is cheap enough to fold into a later pass regardless; (C) should wait for him to actually hit the "I retuned it 8 times and missed one" pain before building the third profile concept for a problem observed, not reported. | §81 (the built bands/lanes this option set builds on top of, not around); `data/spectra-design-decisions.md`'s existing curve-profile Detach/Promote precedent this task cites as (C)'s template. |

---

## Confidence & scope of this document

- Ledger states are grounded in the 2026-08-14 capability audit (46 capabilities, code-cited,
  `data/spotfx-capability-audit/report.md`) plus a direct code/backlog re-check run while writing
  this spec (2026-08-15) to catch same-day drift, updated again the same day after PR #57, #67,
  and #69 merged, and refreshed again 2026-08-16 against git log, PR bodies (gh-axi), and
  `data/backlog.md` to close the gap that had opened since PR #68 (§43–§66, D6, the day-one bar,
  and §9's ambient-composition ruling all touched in that pass), and refreshed again 2026-08-17
  against git log through PR #113 (§5's status corrected from "PR open" to merged, §63's stale
  "PR #91 open" day-one-bar reference corrected to merged, §68–§70 added for per-item mode
  availability, the Colour Set/Group preview gesture, and the SPA cache-header defect), and
  again 2026-08-17 after §71 (top-bar regrouping, PR #116) and §72 (the "Black Hole ignores
  Light mode" investigation — his own proposed data fix found unsafe in Hybrid mode and
  superseded by a different design, no data or code changed, finding kept as a standing
  warning). **Where the two disagreed, the code won** — those rows are tagged
  "DELTA since audit" (currently: §6).
- **What this document does not cover:** settings/config minutiae the audit itself deliberately
  grouped into parent capabilities rather than itemising (its own stated limitation, inherited
  here); anything outside the original 46 + the day-one bar + backlog captain-decision items as
  of 2026-08-15; exact on-screen animation feel or gesture nuance — the audit is source-only, no
  live UI runs, and this spec carries that same caveat forward rather than re-verifying it;
  **deployed state vs. landed state** — see [Landed vs. deployed](#landed-vs-deployed-a-real-gap-this-document-does-not-close),
  a limitation this document names but does not close.
- **Confidence:** MEDIUM-HIGH on rows carried straight from the audit (inherited, not
  re-derived). HIGH on the rows independently re-checked against current code, PR bodies, and
  cited source docs (not just the backlog's own summary of them) while writing and then updating
  this spec: §6, §7, §9, §10, §44, §49, §51 — §44 in particular corrected a stale-looking backlog
  checkbox (the scene-id pool decision reads "open" there, but `RULING.md` and the merged code
  both show it was actually answered — code and the cited primary source won over the tracker).
  §1/§19/§20/§22/§23 (2026-08-15, PR #71) are also HIGH: §1's root-cause
  claim is grounded in a live measurement against the Admiral's own room (7 timestamped samples,
  all confirmed `is_playing=true`, over ~40s of real playback), not inference from code alone —
  and explicitly separates that measured evidence from his own "still adapting" hypothesis, which
  the stable-across-the-window offset argues against rather than confirms.
  §56/§62 (2026-08-16, re-checked against `docs/spectra-star-edges-freeze.md`, PR bodies, and git
  log rather than trusted from their own stale in-PR text) and D6/§61 (2026-08-16, re-checked
  against live status-log lines and PR #91/#93's own bodies — including a live correction that
  caught and removed an over-claimed drop-rate finding before it reached this document) are also
  HIGH for the same reason.
- This spec deliberately does **not** track day-to-day PR review status, CI results, or
  room-window scheduling — see [Maintenance protocol](#maintenance-protocol). That's
  `data/backlog.md`.
- **A stale row here is worse than a missing one.** If a `BUILT` marking looks surprising, check
  the code before repeating it in front of the Admiral — do not trust this document blindly, the
  same failure that made it necessary in the first place.

---

## Where this lives, and why

`docs/SPECTRA_SPEC.md`, in the SpotFX repository, versioned with the code — same convention as
`docs/SPECTRA_HANDOVER.md`, `docs/SPECTRA_PROCESS_SPLIT.md`, `docs/SPECTRA_RESPONSES.md`. Every
contributor and agent shares one copy through normal git history; no separate wiki or fleet-home
doc to fall out of sync with the code it describes. Referenced from `AGENTS.md`
(`CLAUDE.md` is a symlink to it) so a fresh agent finds it without being told.

---

## Unprocessed Admiral notes

*(Empty in steady state — see step 5 of [Translating an Admiral edit](#maintenance-protocol).
If you find text here, it means a past agent could not resolve one of his edits into the ledger
in the same sitting. Resolve it now, or ask him directly, before adding anything new below.)*
